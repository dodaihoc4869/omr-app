-- LÙI của server/dat-2109-lop-ke-thua.sql: gỡ khai báo kế thừa kho lớp (lớp mới tách lại chỉ thấy dạng của chính các em; bảng đệm tự nạp lại lần mở game kế tiếp).
DELETE FROM cau_hinh WHERE khoa = 'lop_da_hoc_ke_thua';

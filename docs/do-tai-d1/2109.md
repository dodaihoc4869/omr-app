# Đo tải D1 — 23:12 tối 21/09 (Code 3, M2 của Boss)

Nguồn: `wrangler d1 insights omr --time-period 1d|1h --sort-by time|reads --limit 20 --json` (chỉ đọc số liệu phân tích, không tải D1) + `EXPLAIN QUERY PLAN` từ xa (chỉ đọc, không chạy truy vấn). Worker sống lúc đo: 5c9a8a6e (21:38). **Cửa sổ 1 ngày gồm giờ cao điểm TRƯỚC các bản đệm** (385b015c, 014c892a, 5c9a8a6e) nên số 1 ngày là "trước", số 1 giờ là "hiện nay".

## Hiện nay (22:12–23:12, ban đêm) — top 10 theo thời gian cộng lại chỉ 20 s
Mười truy vấn đứng đầu tổng 20 s/giờ; đứng đầu là nạp kho câu (9,4 s, 307 lượt), rồi hai truy vấn quét `su_kien_hoc WHERE luc >= ?` (5,8 s + 2,6 s). Không còn `cau_hinh` 3 cờ, `mat_khau`, ghi `student_notice` trong top 10 ⇒ các đệm đã hết tải hai truy vấn nhỏ chạy nghìn lần đó.

## 1 ngày (trước đệm) — top theo thời gian, gom họ truy vấn
| # | Họ truy vấn (chủ mã) | Tổng | Lần chạy | Dòng đọc/lần | EXPLAIN | Đề xuất |
|---|---|---|---|---|---|---|
| 1–2 | nạp câu theo cặp `json_each(?) JOIN game_v2_question` (`game-v2-bank.ts` readScope pha 2) | 545 s + 485 s | 23,1k + 16,9k | ~820 | dùng PK `(ma_de,qid)`, TEMP B-TREE ORDER BY — kế hoạch đã tối ưu; tốn ở SỐ BYTE json (câu 2–30 KB), không ở kế hoạch | KHÔNG thêm chỉ mục. Đẩy 3d9b059 (đệm kho NHẸ, chỉ nạp đầy đủ vài câu được rút): Code 1 đo −51…−63 % dòng đọc |
| 3–6, 10, 17 | nạp kho cũ `ORDER BY cursor LIMIT ?` (trước 26dfb13, 16:30) | 84+69+64+59+39+26 s | ~8,4k | 3,7–10k | `game_v2_question_dang` + de_kho + game_v2_index, TEMP B-TREE | Đã thay bằng 2 pha; số này là dư âm buổi sáng |
| 7 | đếm câu làm mỗi em mỗi ngày (`cau-da-lam.ts`/`ke-hoach-ngay`) | 58 s | 5,2k | 8,1k | `idx_skh_em_ngay_qid`, tốt | Đệm kế hoạch ngày (dem-ke-hoach.ts, đã có) |
| 8, 14 | quét `su_kien_hoc WHERE luc >= ?` (`gv-bang-tin.ts`, `thi-dua-hom-nay.ts`, `cau-da-lam.ts`) | 53 s + 26 s | 840 + 1,4k | 16k + 5,6k | `idx_skh_luc (luc>?)` rồi GROUP BY tạm; `LIMIT -1` cố ý chống hợp nhất | Đây là truy vấn còn đứng đầu ban đêm. Đệm 30–60 s trong mô-đun cho `gv-bang-tin` (app thầy nhịp 30–120 s) — KHÔNG thêm chỉ mục phủ (bảng ghi nhiều nhất, nhân đôi chi phí ghi) |
| 9 | `cau_hinh WHERE khoa IN (?,?,?)` | 40 s | 115,8k | 4 | PK | Đã đệm 30 s (reset-toan-app). Không còn top 10 giờ này ✔ |
| 11, 23 | nạp câu theo `qid IN json_each` (khoảng 175–371 dòng) | 37 s, 24 s | 10,9k, 21,7k | 175, 371 | `game_v2_question_qid` | Ổn; dồn về M3 (gộp 2 lần đọc dạng trong 1 lệnh nộp) |
| 12, 16 | tiến bộ/lên bậc theo em (`ke-hoach-ngay-d1.ts`) | 33 s + 9 s | 14k + 3,1k | 6,7k–9,5k | `idx_skh_em_ngay_qid` + 2 truy vấn con `idx_skh_em_qid_ngay` phủ | Tốt về kế hoạch; giảm bằng đệm kế hoạch (đã có) và bỏ 3× đọc trùng ở M3 |
| 13 | `SELECT mat_khau FROM hoc_sinh` (xác thực) | 32 s | 154k | 1 | PK | Đã đệm token 60 s ✔ |
| 14b | thông báo `Bài tập mới từ Thầy` (`notifications.ts`) | 30 s | 38k | 586 | `SCAN e` + `SCAN mom_bai` trong UNION (không đẩy điều kiện xuống) | M3: viết lại thành 2 truy vấn theo `sbd` (`idx_btvn_em`) — kiểm bằng EXPLAIN trước |
| 15 | danh sách ca cho kho (`game-v2-bank.ts`) | 29 s | 26k | 813 | `SCAN ca` + 3 truy vấn con `luot` | Đệm 30 s theo ca (mô-đun) |
| 18 | `INSERT OR IGNORE student_notice` | 26 s | 234k | ghi | — | 1 giờ nay 3,6k lần (0,4 s): đã hết; M3 thêm cờ "đã sinh thông báo" 60 s theo em |
| 20 | `INSERT INTO nam_kt_cau` (hồ sơ năm) | 26 s | 12k | 48 | — | Đã sửa 176c540 (chỉ ghi dòng đổi) |

## Theo dòng đọc (top) — thêm so với bảng trên
`luot` danh sách học sinh app thầy (index.ts, 160 lần × 73k dòng = 11,7M): `SCAN luot` + GROUP BY. `nam_kt_cau` đếm theo trạng thái (gv-bang-tin, 529 × 16,8k): `idx_nkc_em_dang` quét cả bảng. `su_kien_hoc` theo `ngay_vn` không `sbd` (473 × 30k). Cả ba đều là truy vấn của app thầy ⇒ đệm mô-đun theo nhịp thầy (M3), không phải chỉ mục.

## Chỉ mục chỉ-thêm — kết luận
1. `idx_luot_sbd_nop (luot(sbd,trang_thai,nop_luc))` — ĐÃ soạn ở acfa523, CHƯA chạy --remote (chờ Boss + lời thầy). Bench Code 1: profile 2.485 → 289 dòng/lượt nhờ dấu `+nguon` (không cần chỉ mục cho `su_kien_hoc`).
2. Tuỳ chọn: `game_v2_question(dang, ma_de, qid)` phủ cho 2 truy vấn liệt kê khoá không cần json (mục #12/#20 của bảng đọc: 1,4k + 1,9k lần, 27 s + 22 s/ngày). Bảng có cột json to nên mỗi dòng đọc = 1 trang; chỉ mục phủ ~60 byte/dòng. Chỉ đáng khi đệm kho nhẹ (3d9b059) chưa đẩy được.
3. KHÔNG đề xuất chỉ mục phủ cho `su_kien_hoc(luc, …)`: bảng ghi nhiều nhất, nhân chi phí ghi; dùng đệm.

import HomNayScreen from './HomNayScreen'

// TRANG CHỦ = màn HÔM NAY BẢN 3 (Bảng tin — thầy chốt 21/09): trang chỉ đọc, đúng MỘT khung nhìn, không cuộn ⇒ không thêm gì bao quanh.
// Nút "Lấy bản mới" đã dời sang Cài đặt (NutCapNhatApp); bản dự phòng HomNayCu vẫn tự mang nút này.
export default function ExamHubScreen() {
  return <HomNayScreen />
}

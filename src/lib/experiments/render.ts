// Shared, offline, teacher-authorised in-question illustrations. No runtime keyword guessing.
import {EXPERIMENTS} from './verified'
import {EXPANDED} from './expanded'
import {SCENES} from './catalog'
import {renderScene} from './scene'
export function experimentKey(text:string):string{let a=2166136261,b=5381;for(const c of text.normalize('NFC').replace(/\s+/g,' ').trim()){a=Math.imul(a^c.charCodeAt(0),16777619);b=Math.imul(b,33)^c.charCodeAt(0)}return `${a>>>0}-${b>>>0}`}
export function experimentHtml(text:string):string{
 const scenes=EXPANDED[experimentKey(text)];if(scenes)return scenes.map(id=>SCENES[id]?renderScene(SCENES[id]):'').join('')
 const kind=EXPERIMENTS[experimentKey(text)];if(!kind)return ''
 const thermal=kind==='no2'
 const title=thermal?'Cân bằng NO₂ ⇌ N₂O₄':'Tán xạ hạt alpha qua lá vàng'
 const svg=thermal?`<svg viewBox="0 0 600 230" role="img" aria-label="Ba ống nghiệm kín: đối chứng, làm lạnh và làm nóng"><g fill="none" stroke="currentColor" stroke-width="2">${[70,260,450].map(x=>`<path d="M${x} 35v125q0 35 35 35t35-35V35Z"/><path d="M${x-5} 33h80v12h-80Z" fill="currentColor"/>`).join('')}</g>${[70,260,450].map((x,i)=>`<path class="ex-gas ex-gas-${i}" d="M${x+3} 48v112q0 32 32 32t32-32V48Z" fill="#9b4a27" opacity=".4"/>`).join('')}<g fill="currentColor" text-anchor="middle" font-size="17"><text x="105" y="222">Đối chứng</text><text x="295" y="222">Nước đá</text><text x="485" y="222">Nước nóng</text></g></svg>`:`<svg viewBox="0 0 600 230" role="img" aria-label="Hạt alpha đi qua lá vàng; một số bị lệch, rất ít bật ngược"><rect x="290" y="20" width="8" height="175" fill="#d0a238"/><g fill="none" stroke="#c28d24" stroke-width="2" class="ex-rays"><path d="M35 65H560M35 105H560M35 145H560"/><path d="M35 85H280Q310 85 360 40L440 15"/><path d="M35 125H280Q315 125 250 190L150 210"/></g><g class="ex-alpha" fill="#e29c24"><circle cx="65" cy="65" r="5"/><circle cx="110" cy="105" r="5"/><circle cx="150" cy="145" r="5"/></g><g fill="currentColor" font-size="17"><text x="30" y="28">Chùm hạt α →</text><text x="265" y="222">Lá vàng mỏng</text></g></svg>`
 const note=thermal?'Làm lạnh: màu nâu nhạt đi. Làm nóng: màu nâu đậm hơn. Các ống luôn kín; đây là minh hoạ định tính, không biểu diễn nồng độ hay thời gian thực.':'Phần lớn hạt đi thẳng; một số lệch hướng và rất ít bật ngược. Sơ đồ phóng đại đường lệch để dễ quan sát, không thể hiện tỉ lệ hạt thực tế.'
 return `<details class="ex-demo" open><summary>Quan sát thí nghiệm: ${title}</summary><style>.ex-demo{margin:16px 0;padding:16px;border:1px solid currentColor;border-radius:16px;color:inherit;font:inherit}.ex-demo summary{cursor:pointer;font-weight:700}.ex-demo svg{display:block;width:100%;max-height:300px;margin:12px 0}.ex-demo p{font-size:.8em;line-height:1.5}.ex-demo[open] .ex-gas-1{animation:ex-cold 4s ease forwards}.ex-demo[open] .ex-gas-2{animation:ex-hot 4s ease forwards}.ex-demo[open] .ex-alpha{animation:ex-alpha 3s linear 3}.ex-demo[open] .ex-rays{animation:ex-show 2s ease forwards}@keyframes ex-cold{to{opacity:.1}}@keyframes ex-hot{to{opacity:.85}}@keyframes ex-alpha{to{transform:translateX(380px)}}@keyframes ex-show{from{opacity:0}to{opacity:1}}@media(prefers-reduced-motion:reduce){.ex-demo *{animation-duration:.01s!important;animation-iteration-count:1!important}}@media print{.ex-demo{display:none}details.ex-original>summary{display:none}details.ex-original::details-content{content-visibility:visible;display:block}details.ex-original>*:not(summary){display:block!important}}</style>${svg}<p>${note}</p><p>Đóng rồi mở lại để chạy lại. Hình gốc của câu vẫn được giữ để đối chiếu dụng cụ và kí hiệu.</p></details>`
}

export function experimentOriginal(text:string,html:string):string{return html&&EXPERIMENTS[experimentKey(text)]?`<details class="ex-original"><summary>Xem hình gốc của đề</summary>${html}</details>`:html}

export function experimentCanReplaceImage(text:string):boolean{return Boolean(EXPERIMENTS[experimentKey(text)])}

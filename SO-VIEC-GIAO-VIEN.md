# SỔ VIỆC — LÀN GIÁO VIÊN (Code 1, phiên mới) · GĐ 6 phần GỌI LÊN BẢNG

Phiên **Code 1** (không phải Coder 1 cũ). Việc do phiên **0.Planer** điều phối giao ngày 19/09/2026, theo sự uỷ quyền
của thầy ("nghe theo điều phối 100%"). Sổ này riêng của làn giáo viên; `SO-VIEC.md` thuộc phiên khác, không sửa.

## Yêu cầu nhận (nguyên văn tóm từ tin của 0.Planer, 19/09 ~16:30)

- [x] (a) `diemHopCau` (`src/lib/ho-so-lop.ts`): `pSai = min(1, lan_sai/2)` từ hồ sơ thật; "đã chữa" 0,35 = `trang_thai='da_khac_phuc'`; `pYeu` từ `nam_kt_dang`; CHẶN CỨNG: em có `bac='biet'` ở dạng đó không nhận câu 2 sao; câu `can_day_lai` → bắt buộc lên bảng. GIỮ: `TRONG_SO` cộng đúng 1,0; SAI ở nhà > CHƯA LÀM > ĐÚNG; em chưa có dữ liệu vẫn có cơ hội; sàn 20 em/90 phút; không 2 em một câu; tất định.  | bằng chứng: `tests/ho-so-lop-nam-kt-1909.test.ts` 38 test + `tests/ho-so-lop-nap-may-chu-1909.test.ts` 6 test xanh; đột biến (tắt chặn cứng → 6 đỏ; tắt bắt buộc dạy lại → 2 đỏ; Vòng 2 `break` thay `continue` → 1 đỏ; bỏ `dsQid` khỏi yêu cầu → 1 đỏ) đều bị bắt rồi khôi phục; 15 tệp test liên quan (326 test) so nền: MỚI ĐỎ [] · HẾT ĐỎ []; `npx tsc -b --noEmit` sạch. Commit: xem Nhật ký.
- [x] (b) Bảng phân công Engine E (buổi chữa / tờ máy chiếu) trong `src/screens/GoiLenBangScreen.tsx`: nút Đạt / Không đạt gọi ĐÚNG hàm `ghiLenBang` mà Engine C đang dùng (~dòng 1835-1844); kết quả ghi về máy chủ (`nguon='len_bang'`).  | bằng chứng: hàm `ghiKetQua` MỘT đường cho cả `cham` (Engine C) và `chamBuoi` (Engine E) → cùng `ghiLenBang(cauHinh.url, cauHinh.mat, {sbd, chuyenDe, dat, qid})`; `tests/goi-len-bang-buoi-cham-1909.test.tsx` 10 test RENDER màn thật (mock máy chủ): đủ 2 nút mỗi dòng, Đạt → đúng 1 lần gọi với đúng 4 trường, Không đạt → dat:false, máy chủ lỗi → giữ nút + báo lỗi + bấm lại được, không ghi đôi (cả khi bấm dòng khác lúc dòng trước còn chờ), chặn cứng bậc biết chạy xuyên màn, xin lại hồ sơ khi lần trước lỗi/đổi em; đột biến 3 chỗ (tắt chặn cứng, khoá cũ chỉ so số em, bỏ khoá đang-ghi) bị bắt; tờ HTML máy chiếu (`html-may-chieu.ts`, ngoài vùng) KHÔNG đổi — bảng buổi chữa trên màn là nơi bấm.
- [x] (c) `src/lib/phan-cong-day-hoc.ts:42` dùng `Math.random` → nhận seed (`hashSeed` từ `src/lib/exam-shuffle.ts`), chỗ gọi truyền seed theo mã ca; giữ test `phan-cong-day-hoc`.  | bằng chứng: tham số thứ 3 nhận hàm (như cũ) HOẶC số/chuỗi seed; mặc định `'phan-cong-day-hoc'` cũng TẤT ĐỊNH; màn truyền `hashSeed(`day-hoc:${du?.maCa}`)`. `tests/phan-cong-day-hoc-seed-1909.test.ts` 11 test + `tests/phan-cong-day-hoc.test.ts` 3 test xanh; đột biến 3 chỗ (mặc định về Math.random → 3 đỏ; bỏ qua seed → 1 đỏ; màn không truyền seed → 1 đỏ) bị bắt; `exam-shuffle.ts` chỉ thêm chữ `export` trước `mulberry32` (0.Planer duyệt), giá trị `hashSeed('CA-111111:12001')=2709744386` và `seededPermutation(10,12345)=[6,4,8,0,1,7,5,3,2,9]` đo bằng bản CHƯA sửa và khoá trong test; `tests/exam.test.ts`, `tests/kho-mong-rut-lap-thong-minh-1909.test.ts` (dùng exam-shuffle) xanh.
- [x] (d) Viết HỢP ĐỒNG máy chủ `docs/hop-dong-ho-so-len-bang-1909.md` (`hoSoLopLenBang` trả thêm cho mỗi em×qid: `lan_sai`, `trang_thai`, `can_day_lai`, `bac` theo dạng từ `nam_kt_cau`/`nam_kt_dang`; tương thích ngược: thiếu trường → hành vi cũ; kèm việc `ghiLenBangMoi` ngừng cộng thẳng `tien_do_hs` ở `index.ts` ~2263) rồi nhắn phiên "Code 3". Trong lúc chờ: client làm theo hợp đồng với dữ liệu giả + nhánh tương thích ngược.  | bằng chứng: tệp hợp đồng đã viết (6 mục); đã nhắn Code 3 [f1d054] (19/09 ~14:10, ghi rõ 2 việc + test phải đổi `khoi-a-va-kho-de-1209.test.ts:89-93`). Client đã làm xong theo hợp đồng với dữ liệu giả + nhánh tương thích ngược (test render màn với máy chủ giả trả `namKt`).
- [x] (e) Nhắn 0.Planer một dòng sau mỗi mốc (a)/(b)/(c).  | bằng chứng: tin gửi sau (a) 704c512, (b) 7a9888c, (c) 0a5afbd + 965c07f; 0.Planer đã soát cả bốn ("ĐẠT").
- [x] (f) Điền dòng "Code 1" trong `DIEU-PHOI.md` thành "đang làm".  | bằng chứng: dòng bảng "Code 1" đã ghi "đang làm (19/09, nhận việc từ 0.Planer): (a)→(b)→(c)…"; Nhật ký 14:32.
- [x] (g) Toàn `npx vitest run` MỘT lần trước khi báo xong, so nền `docs/nen-vitest-do-1909.txt` (98 đỏ / 43 tệp); ghi Nhật ký trước; commit từng mốc.  | bằng chứng: Nhật ký `DIEU-PHOI.md` 14:32 ghi trước; `npx vitest run` (sau commit 0a5afbd) = **5066 test, 100 đỏ, 369 tệp, 44 tệp đỏ**. So tệp đỏ với nền: MỚI ĐỎ = `tests/bon-viec-0809-dot4.test.ts`, `tests/nam-loi-0809-dot3.test.ts` — cả hai cùng lỗi `expected … to contain '<body class="co-lam chua-nop">'` do `src/lib/html-phieu.ts` ĐANG BỊ SỬA DỞ bởi phiên khác (git diff cho thấy `<body>` mang thêm lớp `gd-m3`; `git log` — em không commit tệp ấy); HẾT ĐỎ = `tests/parent-news.test.ts` (việc của Code 3). Loại 2 tệp của html-phieu ⇒ 98 test / 42 tệp: KHÔNG có tệp đỏ nào do làn giáo viên. 15 tệp gọi-lên-bảng: MỚI ĐỎ [] · HẾT ĐỎ [].

## MỐC (d) — giao thêm 19/09 (0.Planer, thầy chốt "Cho lên máy chiếu luôn") — PHẦN CẦU NỐI XONG, phần giao diện nút DỪNG theo lệnh 0.Planer (thiết kế lại cả tờ chiếu, xem mốc M1→M6)

Nguyên văn 0.Planer: "nút Đạt / Không đạt ngay trên TỜ MÁY CHIẾU. PHẠM VI: `src/lib/html-may-chieu.ts` + đúng phần nghe/nhận trong `src/screens/GoiLenBangScreen.tsx` + test. KHÔNG đụng `html-phieu.ts`, `server/`." Rồi (tin sau): "TẠM DỪNG mốc (d)… phần CẦU NỐI thì GIỮ LẠI, commit riêng… phần markup nút thì dừng."

- [x] (d1) Mỗi ô em×câu có 2 nút; bấm → cầu nối → màn gọi ĐÚNG `ghiKetQua`; MỘT đường ghi; chống ghi đôi CHUNG khoá `sbd|qid` (hai chiều).  | bằng chứng: cầu nối `parent.postMessage` (tờ nằm trong `<iframe srcDoc>` không sandbox của `KhungXemPhieu`; app chỉ mở tờ bằng đường ấy nên không cần opener/BroadcastChannel). `ghiTheoKhoa` (idempotent: đã ghi ⇒ true không ghi lại; đang chờ ⇒ chờ đúng lượt ấy) là cổng chung của bảng buổi chữa + tờ chiếu; `daGhiKhoa`/`dangGhiKhoa` ref chung, cả bảng Phân công (`cham`) cũng tôn trọng. `tests/goi-len-bang-to-chieu-cham-1909.test.tsx` 14 test ĐẦU-CUỐI (React thật + tờ do `taoHtmlMayChieu` dựng nạp vào JSDOM, nối hai đầu bằng chuyển tin bất đồng bộ): Đạt → `ghiLenBang` đúng 1 lần đúng 4 trường; bấm đúp / CHAM hai lần → 1 lần; bấm ở bảng buổi chữa → tờ nhận DA_GHI khoá ô; ô ghi trước khi mở tờ → bắt tay xong tờ tự khoá.
- [x] (d2) Trả lời 'da_ghi' | 'loi'; lỗi → nút mở lại + "chưa ghi được, bấm lại"; 8 s không phản hồi = lỗi; kiểm nguồn + mã phiên.  | bằng chứng: `tests/to-chieu-cau-noi-1909.test.ts` 41 test (JSDOM chạy JS thật của tờ): SAN_SANG lặp mỗi 400 ms tới KET_NOI, không ai trả lời sau 15 nhịp → gỡ hẳn nút; hẹn 8000 ms → lỗi, phản hồi muộn da_ghi vẫn khoá ô. BỐN LỚP kiểm tin ở `kiemTinToChieu` (nguồn = ĐÚNG iframe tờ chiếu; gốc = gốc app hoặc 'null'; mã phiên ngẫu nhiên `taoMaPhienChieu`; nội dung: `khoa` phải là ô CÓ TRÊN tờ, `dat` boolean thật) — màn KHÔNG tin sbd/qid trong tin mà tra lại theo `khoa`. Tin lạ: cửa sổ khác / sai mã / gốc lạ / khoá lạ / kiểu tin lạ / sau khi đóng tờ / mã của tờ cũ ⇒ không ghi, không phản hồi.
- [x] (d3) TẾ NHỊ trước lớp: sau khi ghi tờ chỉ còn "Đã ghi" cho CẢ HAI kết quả; nút trung tính.  | bằng chứng: ô ghi xong `textContent === 'Đã ghi'`, `outerHTML` không khớp /đạt/i, hai kết quả cho CÙNG một DOM (test so `outerHTML`), thuộc tính còn lại chỉ `class`/`data-cham`/`data-khoa` (không lưu kết quả); CSS nút không dùng `--mc-do/luc/cam`. LƯU Ý cách hiểu: trước khi bấm, nhãn "Đạt"/"Không đạt" nằm trên NÚT (đúng đặc tả "hai nút Đạt/Không đạt"); sau khi ghi thì ô đó không còn chữ ấy — chưa ghi thì các ô khác vẫn có nút.
- [x] (d4) Mở KHÔNG qua app → ẩn hẳn hai nút; trang đáp án và "chỉ đọc đáp án" không nút.  | bằng chứng: không có `cauNoi` ⇒ HTML byte-for-byte như cũ (test: không `mc-cham`, không `data-cau-noi`, đúng 1 thẻ `<script>`); có `cauNoi` mà mở riêng (`window.parent === window`) ⇒ JS GỠ HẲN mọi nút, không gửi tin, không hẹn giờ; trang đáp án + nửa trống không có nút.
- [x] (d5) GIỮ mọi bất biến `may-chieu-len-bang-1409*`, `btvn-len-bang-1409*`…  | bằng chứng: 15 tệp gọi-lên-bảng (326 test) so nền: MỚI ĐỎ [] · HẾT ĐỎ []; `day-hoc-dem-nguoc`, `may-chieu-cau-dai-ngan`, `projector-linebreak`, `than-thu-to-chieu` xanh; `tsc -b` sạch; không thêm mạng, không thư viện.
- [x] (d6) TEST theo đặc tả.  | bằng chứng: xem (d1)-(d4); đột biến 8 chỗ trong JS tờ + 6 chỗ ở màn: bị bắt trừ 2 lớp phòng thủ thừa có lớp khác chặn (bấm đúp: nút đã `disabled`; khoá lạ: `p.o.get` cũng chặn). Phần TỜ mới (M1→M6) phải tích hợp cầu nối bằng đúng 3 thứ ở đầu-cuối `to-chieu-cau-noi.ts` ("HỢP ĐỒNG VỚI MARKUP CỦA TỜ CHIẾU").

## GIAO 19/09 tối — TRÙNG TU MÀN CHIẾU LÊN BẢNG + THUẬT TOÁN BUỔI CHỮA (0.Planer, thầy chốt "Chốt thiết kế lên bảng") — đặc tả: `prompt-man-chieu-len-bang.md`

Nguyên văn thầy: "Màn chiếu lên bảng bạn trùng tu giao diện lại cho tôi, khi chiếu lên học sinh phải không mỏi mắt, câu dài thì tự động lấy 2/3 bảng để hiển thị (làm lại thuật toán chỗ này cho chuẩn, để phải hiển thị đầy đủ trên bảng không phải cuộn bất kể câu nào). 1/3 bảng còn lại để học sinh lên làm bài. Thiết kế lại thẻ tên và hiệu ứng hiển thị thần thú cho đẹp hơn kích thích hơn. Dựa vào độ khó và đề dài hay ngắn tính toán lại hết thời gian lên bảng, chữa bài. Đảm bảo chất lượng tốt nhất cho buổi chữa đó học sinh học nhiều kiến thức nhất, nếu không chữa hết tự động tiếp tục được vào buổi sau. Thuật toán phải sửa lại thật thông minh và tối ưu vì phần này tôi dùng nhiều nhất."

Luật của giao việc: MỖI MỐC một commit, nhắn 0.Planer soát trước khi lên máy chủ; test cũ là hợp đồng (300/180/120, sàn 20 em, tổng ≤ 4.920 s, 2 em/đợt, lẻ em không bịa…) — cần đổi test nào hỏi 0.Planer TRƯỚC; hằng số mới MỘT chỗ (`len-bang-cau-hinh.ts`); TẾ NHỊ TRƯỚC LỚP là luật cứng (không chiếu `viSao`; "Chưa đạt" = nhãn trung tính, không hiệu ứng, không chữ đỏ; nhãn nút "Đạt" / "Chưa đạt", sau khi ghi chỉ "Đã ghi"); trong lúc đọc đề KHÔNG có gì chuyển động.

- [x] M1 · Thời gian theo độ khó × độ dài × em (hàm thuần, một nguồn): `src/lib/thoi-gian-len-bang.ts` + `THOI_GIAN_LEN_BANG` trong `len-bang-cau-hinh.ts`; câu thiếu văn bản rơi về 300/180/120; Engine E dùng T mới làm chi phí; `thoiGianDayHoc()` gọi chung hàm.  | bằng chứng: xem Nhật ký M1 dưới.
- [x] M2 · Bố cục ĐO THẬT, không bao giờ cuộn (leo bậc 1/2 → 2/3+1/3 → 2 cột → co chữ ≥ sàn → toàn bảng; đo lại khi đổi cỡ; ước lượng khi xếp buổi; gỡ `.mc-nua{overflow-y:auto}`).  | bằng chứng: xem Nhật ký M2 dưới (Chrome thật 4 khung, 0 vùng tràn, 0 chữ dưới sàn; ảnh + JSON ở `docs/anh-man-chieu-1909/`).
- [x] M3 · Giao diện mới (3 bản vẽ) + thẻ tên + thần thú + nút chấm qua cầu nối (Đạt / Chưa đạt; ăn mừng chỉ khi Đạt; không chiếu viSao).  | bằng chứng: xem Nhật ký M3 dưới (Chrome thật 4+5 khung, ảnh `docs/anh-man-chieu-1909/m3-*`, 41 test mới + đột biến 28/28).
- [x] M4 · Lưu buổi + TỰ NỐI BUỔI SAU (khoá lớp|mã ca gắn mốc reset; hết hạn 14 ngày; thay em vắng; đối chiếu lịch sử máy chủ).  | bằng chứng: xem Nhật ký M4 dưới (12 test màn hình tắt-app→mở-lại + 17 test thuần, đột biến 21/21 kể cả 2 bản thừa).
- [x] M5 · Tối ưu "học nhiều nhất trong buổi" (giá trị câu v/T, phủ dạng lớp yếu trước, ghép song song bậc (1), báo số đo được).  | bằng chứng: cd2d9b0 — xem Nhật ký M5 (21 test, 11 đột biến đều bị bắt; sàn 20 em còn giữ: 21/20/21/20 em cho 24/30/40/50 câu).
- [x] M6 · Tự hiệu chỉnh từ giây thật (`giayThuc` 20..1800 s; hợp đồng cột `len_bang.giay_thuc` → Code 3; hệ số theo (phần, sao) sau ≥ 8 mẫu, kẹp 0,6..1,4).  | bằng chứng: xem Nhật ký M6 (28 + 7 test, 21 đột biến đều bị bắt; máy chủ Code 3 đã có cột, commit f892636).
- [ ] (kiểm đầu-cuối bản sống `namKt` bằng tài khoản thầy 12121212 — 0.Planer sẽ báo khi Pages mang 704c512+ lên; không bấm Đạt/Không đạt cho học sinh thật)  | bằng chứng: (chưa có)

## Luật của làn (chép từ tin nhắn — không đổi)

- CHỈ `git add <tệp cụ thể>`. Cấm stash / reset --hard / checkout . / clean / `git add -A`. KHÔNG phát hành (0.Planer soát diff rồi đẩy Pages).
- CẤM đụng: `server/**`, `StudentPortalScreen.tsx`, `ParentPortalScreen.tsx`, `html-phieu.ts`, `ExamTakeScreen.tsx`, `src/components/bang-nhiem-vu/**`, component app HS/PH (Code 2, Code 4), `src/lib/de-rieng*.ts` (GĐ 6 phần rút đề, giao sau).
- Vùng được sửa: `src/lib/ho-so-lop.ts`, `src/lib/xep-buoi-chua.ts`, `src/lib/phan-cong-day-hoc.ts`, `src/screens/GoiLenBangScreen.tsx`, test tương ứng, `docs/hop-dong-ho-so-len-bang-1909.md`, sổ này.
- Hàm thuần + test trước; tất định; thấy lỗi trong vùng mình là sửa luôn.

## Nhật ký làn này

- (chuyển từ 0.Planer 19/09) **Sửa lỗi chữ:** `src/screens/PhanCongScreen.tsx:112` ghi "Bài tập theo 3 vòng" trong khi thầy đã bỏ hệ Vòng 1/2/3 của BTVN → đổi "Chia lô theo hạn nộp". Rà `grep` mọi "vòng" trong src (trừ game): teacher-facing chỉ còn đúng dòng này. CÒN SÓT NGOÀI VÙNG (đã báo 0.Planer, KHÔNG đụng): `BaoCaoCaThiHocSinhModal.tsx:228-252` + `BaoCaoCaThiPhuHuynhModal.tsx:206-233` (mô tả "Vòng 1: Lõi Căn Bản… Vòng 3: Thử Thách" — component báo cáo HS/PH, làn Code 4), `html-phieu.ts:1196-1199,1533-1550,1583` (Code 2), `bai-tap.ts:185-245` (hàm phân tầng 3 vòng của lõi, không phải chữ trên màn giáo viên). Test soi chuỗi cũ: KHÔNG có; thêm `tests/phan-cong-man-he-lo-1909.test.ts`.
- (a) commit `704c512` — 0.Planer đã soát ĐẠT, đã lên máy chủ đợt 3.
- (b) xong: hai lần chỉnh phát sinh khi làm — (1) `thieu.viSao` nói SAI lý do khi trượt sàn vì bị chặn bậc "biết" (báo "hết ngân sách" trong khi nới giờ vô ích): nguyên nhân gốc là chuỗi lý do chỉ có 3 nhánh (lớp nhỏ / hết câu / hết giờ); thêm nhánh thứ tư + 2 test (chặn → nói bậc biết; hết giờ thật → vẫn nói ngân sách). (2) `cham` tách thành `cham` → `ghiKetQua` → `boDong` để hai test khoá nguồn (`goi-len-bang.test.ts` thứ tự ghi-trước-bỏ-dòng, `lich-su-len-bang-1009.test.ts` cộng lịch sử tại chỗ) vẫn xanh không phải sửa test.
- Việc TỰ PHÁT SINH đã sửa trong vùng mình (theo luật "thấy lỗi là sửa luôn"): `chayBuoiChua` chỉ so SỐ em có mặt để quyết định xin lại hồ sơ lớp ⇒ (1) hai em đổi chỗ vẫn dùng hồ sơ em cũ, (2) một lần mất mạng thì hồ sơ rỗng bị giữ mãi (số em không đổi). Nguyên nhân gốc: khoá cache là `hoSo.length !== coMat.length`. Sửa: khoá = (danh sách sbd có mặt × danh sách qid của buổi) và xin lại khi `loiHoSo`. Bằng chứng: test nguồn trong `tests/ho-so-lop-nap-may-chu-1909.test.ts` (chưa có test render — sẽ thêm ở mốc (b) cùng bài test tích hợp màn hình).
- Sửa tệp NGOÀI danh sách của làn (cần báo 0.Planer): `src/lib/exam-api.ts` (chỉ thêm kiểu `NamKtCauMayChu`, trường `namKt?` và tham số tuỳ chọn `dsQid?` của `goiHoSoLopLenBang` — vì kiểu `HoSoLopMayChu` sống ở đó).
- **Test ĐỎ OAN khi máy tải nặng (0.Planer báo 19/09 chiều, 3 phiên khác gặp) — sửa cho BỀN, không hạ ngưỡng thuật toán.** (1) `tests/goi-len-bang-to-chieu-cham-1909.test.tsx › "BẤM ĐÚP / tin CHAM gửi hai lần…"`: NGUYÊN NHÂN GỐC — khung iframe hiện ở DOM TRƯỚC khi hiệu ứng thụ động của React gắn bộ nghe tin (`useEffect([htmlMayChieu])`); helper `chieuLenBang` trả về ngay nên tin gửi liền sau rơi vào khoảng trống, `ghiLenBang` được gọi 0 lần (đỏ, chờ đủ 20 s). Không phụ thuộc đồng hồ thật, phụ thuộc thứ tự lịch của React. Tái hiện: 6 tiến trình vitest song song ⇒ 1/6 đỏ đúng test này; sửa: `await act(async () => {})` cuối `chieuLenBang` (xả hiệu ứng) ⇒ 8 tiến trình song song 8/8 xanh. Hệ quả tốt thêm: lúc máy tải nặng, các test "bỏ tin lạ" cùng tệp có thể xanh SUÔNG (không ai nghe thì đương nhiên không ghi; lúc máy rảnh thì đã được đột biến bắt ở mốc (d)) — nay bộ nghe chắc chắn đã gắn nên chúng kiểm thật ở mọi mức tải. (2) `tests/xep-buoi-chua-1409.test.ts › "QUÉT DIỆN RỘNG — sàn 20 em phải đứng vững › mọi buổi có ĐỦ CÂU và ĐỦ EM đều đạt sàn, không buổi nào vượt ngân sách"`: quét 840 tổ hợp, chạy riêng 1,4 s nhưng 6,85 s lúc tải 49 > hạn mặc định 5 s ⇒ thêm hạn tường minh 60 s (ngưỡng thuật toán — sàn 20 em, ngân sách 4.920 s — KHÔNG đổi). File này không có phép đo đồng hồ nào, nên không có gì để "lấy lần nhanh nhất trong 3". Cùng loại, phòng xa: hạn 60 s cho 3 test quét nặng của làn ở `thoi-gian-len-bang-1909` ("luôn trong [60, 480]… mọi tổ hợp"), `bo-cuc-to-chieu-1909` ("MỌI đợt kết thúc ở một bậc…"), `uoc-luong-bo-cuc-1909` ("câu bậc ≥ 2 đứng MỘT MÌNH…") — đi cùng commit M2 vì các tệp ấy đang có thay đổi M2.

## Còn lại / đang chờ (nói thật)

- Phía MÁY THẦY của Kênh 3 xong (a)(b)(c). Hiệu lực THẬT của `namKt` (chặn cứng, dạy lại, pSai thật) chỉ có khi **Code 3** làm xong `hoSoLopLenBang` trả `namKt` (hợp đồng đã gửi); tới đó máy thầy chạy như cũ. `ghiLenBangMoi` ngừng cộng `tien_do_hs` cũng ở Code 3.
- Khi 0.Planer gọi: kiểm ĐẦU-CUỐI trên bản sống bằng tài khoản thầy `12121212`, KHÔNG đụng học sinh thật.
- Ghi nhận phán đoán (không có trong đề xuất, có thể xem lại khi có dữ liệu): trạng thái `dang_on` (đã đúng lại nhưng chưa đủ mốc) giữ NGUYÊN sức nặng pSai, chỉ `da_khac_phuc` mới ×0,35 — đúng chữ lệnh. Tờ HTML máy chiếu (`html-may-chieu.ts`) chưa có nút Đạt/Không đạt: nút nằm trên bảng buổi chữa của màn giáo viên (ngoài vùng làn, cần cầu nối postMessage nếu thầy muốn bấm ngay trên tờ chiếu).
- Bẫy khi đổi: hằng số `SO_CAU_DU_TIN_DANG`/`NGUONG_DANG_YEU` ở `ho-so-lop.ts` phải khớp `server/src/ho-so-cau-hinh.ts` (test soi, đổi một bên là đỏ).

## Nhật ký M1 (19/09 tối)

- **Làm:** `thoiGianCau` = T_đọc + T_làm + T_chữa (đúng công thức 0.Planer, hằng số ở `THOI_GIAN_LEN_BANG`), `noiDungTuCauLuyen` / `noiDungTuCauGoc` (số từ, hình/bảng, số bước; đề ảnh ước 40 từ), `thoiGianDot` (T_đợt hai em song song), Engine E (`xep-buoi-chua.ts`) chọn em TRƯỚC rồi mới kiểm giờ vì chi phí phụ thuộc bậc em; màn giáo viên đưa `noiDung` (từ `traCau`) vào `cauVaoXep`; `thoiGianDayHoc()` bỏ công thức thứ hai (giữ kẹp 60–180 s).
- **QUYẾT ĐỊNH CẦN 0.PLANER XEM (không lặng lẽ đổi):** đặc tả nói "Engine E dùng T mới làm chi phí". Đo bằng đề thật (có văn bản) thì T LITERAL cho mỗi em gấp ~1,5–2 lần 300/180/120 cũ (Phần II/III 0 sao ≈ 270 s; 2 sao dài chạm trần 480 s) nên buổi 30 câu chỉ xếp nổi **13 em (< sàn 20 thầy chốt 14/09)** — bảng dưới. Nguyên nhân: T là thời gian của MỘT em đứng riêng, còn tờ chiếu chạy HAI em song song (`T_đợt = max(T_đọc+T_làm) + T_chữa_A + T_chữa_B`, chính công thức của đặc tả). Nên chi phí của một em trong NGÂN SÁCH BUỔI = `T_chữa + ½·(T_đọc+T_làm)` (`giayBienGhepDoi`, hằng số `HE_LAM_SONG_SONG = 0,5`; đặt 1 để tính nối tiếp đúng chữ đặc tả). Kết quả: cỡ giờ khớp lại 300/180/120, sàn 20 em giữ được.
- **Bảng trước/sau** (giây; T = nối tiếp literal · ½ = ghép đôi (mặc định) · cũ = theo sao):
  I·0 sao·20 từ·2 bước·lớp sai 20%: T 150 · ½ 120 · cũ 120 | I·1 sao·30 từ·3 bước·40%: 210 · 165 · 180 | I·2 sao·80 từ·hình·5 bước·60%·biết: 405 · 300 · 300 | II·0 sao·50 từ: 270 · 195 · 120 | II·2 sao·90 từ·60%·biết: 480 · 300 · 300 | III·0 sao·30 từ: 270 · 180 · 120 | III·2 sao·có hình·40%: 480 · 300 · 300.
  Buổi mẫu 30 em: 24 câu → literal 14 em / ½ 21 em / cũ 24 em; 30 câu → literal 13 / ½ 21 / cũ 20; 40–50 câu → 20 / 20 / 20 (đủ sàn cả ba). Tổng 20 câu rẻ nhất (30 câu): nối tiếp 5.025 s > ngân sách 4.920 (sàn vô nghiệm), ghép đôi 3.780 s.
- **Còn thấy hơi cao (chưa đổi, xin 0.Planer cho ý kiến):** câu 0 sao Phần II/III vẫn ≈ 180–195 s ở chi phí ghép đôi (cũ 120) vì T_làm nền II 120 / III 150 không phụ thuộc sao 0 chỉ "đọc đáp án"; và I·0 sao·60 từ·6 bước = 210 s (> 3 phút). Đề xuất nếu muốn: sao 0 kẹp tối đa 3 bước chữa, hoặc HE_SO_SAO[0] = 0,8 — chưa làm.
- **Bằng chứng:** `tests/thoi-gian-len-bang-1909.test.ts` 30 test (bảng giá trị tính tay 5 dòng; luôn trong [60,480] và bội 15 trên ~3.000 tổ hợp; đơn điệu; hình +10 s; bước kẹp 1..6; lớp sai ≥50% ×1,3; câu thiếu văn bản = 300/180/120 với mọi lớp sai/bậc; T_đợt giao hoán và < nối tiếp; sàn 20 em với đề thật 24/30/40/50 câu; Engine E chi phí theo bậc em; `thoiGianDayHoc` giữ 60–180). Đột biến 5 chỗ (hệ số 1; bỏ nội dung câu; bỏ bậc em; không rơi về mặc định; thoiGianDayHoc không dùng hàm chung) đều bị bắt. 15 tệp gọi-lên-bảng (326 test) MỚI ĐỎ [] / HẾT ĐỎ []; `day-hoc-dem-nguoc`, `may-chieu-cau-dai-ngan`, `projector-linebreak`, `than-thu-to-chieu`, và các test cầu nối xanh; `tsc -b` sạch.

## Nhật ký M2 (19/09 chiều-tối)

- **Làm:** (1) `src/lib/bo-cuc-to-chieu.ts` — thang bậc `leoBacBoCuc` (hàm TỰ CHỨA, nhúng vào tờ bằng `toString()`, cùng một mã ở TS và trong tờ): bậc 1 đợt đôi 2 nửa · bậc 2 đợt đơn, đề 2/3 bảng + 1/3 em làm · bậc 3 chia cột phương án/hình co dần (≥ 0,4) · bậc 4 co chữ từng 1 px tới SÀN 1,25% bề ngang (24 px ở 1920) · bậc 5 toàn bảng (chữ tới đáy tuyệt đối, có CẢNH BÁO nếu vẫn tràn). Cỡ chuẩn 2,34% bề ngang × cỡ thầy chọn (30 px ở 1280). Đo `scrollHeight/clientHeight/scrollWidth/clientWidth` của `.mc-vung-de` (dung sai 1 px), tắt transition lúc đo (`body.mc-do`), MỖI lượt đo đi từ nguyên trạng (ghép lại đợt đã tách) nên đổi cỡ chữ / cửa sổ / toàn màn hình đều đo lại; đợt đôi mà một câu không vừa nửa bảng ⇒ TÁCH thành hai đợt đơn (đánh số lại, ghi chú). (2) `html-may-chieu.ts` — `.mc-vung-de` bọc đề, lời giải phủ lên vùng đề (không đẩy layout), gỡ `overflow-y:auto` của `.mc-nua`; ghi chú cho thầy (tách / câu dài / cảnh báo) chép lên DÒNG PHỤ của thanh trên khi đợt ấy đang hiện. (3) `uoc-luong-bo-cuc.ts` — ước lượng bậc của câu từ văn bản (bề rộng chữ Times New Roman theo bảng AFM, cùng thang bậc) để xếp buổi biết TRƯỚC câu nào chiếm cả bảng; Engine E cảnh báo `"N câu dài tới mức chiếm cả bảng (em làm ở bảng phụ): Phần I câu X · …"`, chỉ ghép đôi hai câu cùng bậc 1 (như tờ). (4) `scripts/do-bo-cuc-to-chieu.mjs` — đo bằng Chromium thật (Playwright) 40 câu mẫu (`tests/fixtures/cau-mau-to-chieu.ts`), thoát mã 1 nếu có vùng đề tràn hoặc chữ dưới sàn.
- **Kết quả đo Chrome thật (40 câu mẫu ⇒ 35 đợt; `docs/anh-man-chieu-1909/do-bo-cuc-40-cau.json`):** 1280×720: bậc 1×5 · 2×18 · 3×2 · 4×10, chữ nhỏ nhất 18 px (sàn 16) · 1920×1080: 1×5 · 2×22 · 4×8, nhỏ nhất 32 px (sàn 24) · 1366×768: 1×5 · 2×18 · 3×2 · 4×10, nhỏ nhất 20 px (sàn 17) · 1600×900: 1×5 · 2×20 · 3×1 · 4×9, nhỏ nhất 25,5 px (sàn 20). **0 vùng đề tràn ở cả 4 khung, 0 chữ dưới sàn, 0 đợt phải tách ở cỡ "Chữ lớn".** Chữ CỰC LỚN (×1,4) ở 1280×720: 35 → 40 đợt (10 đợt đôi TÁCH có ghi chú), vẫn 0 tràn. Một câu 668 từ: bậc 5 chữ 16 px vừa khít, ghi chú "em làm ở bảng phụ". 6 ảnh tiêu biểu: `m2-1920x1080-dot01-bac1 / dot06-bac2 / dot12-bac4`, `m2-1280x720-dot10-bac3 / dot01-tach-chu-1.4 / dot01-bac5-cau-cuc-dai`.
- **Độ chính xác ước lượng (hiệu chỉnh ở 1280×720 và 1920×1080, KIỂM ở 1366×768 và 1600×900 chưa từng thấy):** 0 sai lệch quá 1 bậc, 0 lần ước THẤP (không câu nào tưởng vừa mà thật ra tràn), 0 ghép sai; 1 lần mất cơ hội ghép ở 1920×1080 (`ảnh-thân-vừa`, ước sát ngưỡng ⇒ để đứng một mình, an toàn hơn).
- **Cập nhật kèm theo (0.Planer yêu cầu trong M2):** sao 0 kẹp TỐI ĐA 3 bước chữa — hằng số `CHUA_BUOC_TOI_DA_SAO_0 = 3` trong `THOI_GIAN_LEN_BANG`; bảng test `thoi-gian-len-bang-1909` thêm hàng "I · 0 sao · 60 từ · 6 bước" (trước 210 s ⇒ nay ≤ 3 phút) + so sánh nối tiếp.
- **Giả định của tôi (bản vẽ không có chỗ nào chỉ ra — xin 0.Planer xác nhận hoặc bác):** (a) lời giải hiện như lớp PHỦ lên vùng đề, có cuộn riêng (không đẩy đề, nên không làm tràn); (b) đợt đơn ĐẦU tiên cũng bắt đầu từ bậc 2 (không thử bậc 1, vì bậc 1 là đợt đôi); (c) ghi chú cho thầy nằm ở dòng phụ thanh trên (ẩn khi toàn màn hình — cảnh báo câu dài vẫn có ở bảng buổi chữa nhờ Engine E).
- **Còn nợ (nói trước để M5 khỏi bất ngờ):** hạch toán ngân sách Engine E vẫn tính "½ thời gian đọc+làm" cho MỌI câu vì hai em chạy song song; nhưng câu bậc ≥ 2 đứng MỘT MÌNH nên phải tính đủ T. M5 sẽ sửa (chi phí đầy đủ cho câu bậc > 1). Nút `Hiện học sinh và thần thú →` độc lập giữ nguyên, M3 sẽ thay kiểu dáng.
- **Bằng chứng test:** `bo-cuc-to-chieu-1909` 37 test (thang bậc so duyệt vét cạn với thước đo giả; mã trong tờ chạy thật trong JSDOM: tách/đánh số/ghi chú lên thanh/tắt transition/đo lại khi đổi cỡ; markup `.mc-vung-de`); `uoc-luong-bo-cuc-1909` 42 test; đột biến 4 chỗ mới (bỏ nghe sự kiện, không chép ghi chú, quên cờ cảnh báo, không gỡ cờ) đều bị bắt. Bộ 34 tệp gọi-lên-bảng (657 test): đỏ chỉ còn 5 tệp NỀN (`co-len-bang`, `du-lieu-len-bang`, `goi-len-bang-nhieu-de`, `man-goi-len-bang-thuat-toan-moi-1409`, `may-chieu-len-bang-1409`, 11 test) — MỚI ĐỎ [] ; `tsc -b` sạch. (`rut-de-xung-ho-0909` đỏ thoáng qua vì phiên khác đang thêm `phieu-screen.css`, chạy lại xanh.)

## Nhật ký dọn máy thầy theo `mocReset` (19/09 tối — 0.Planer giao, hạn tối CN 20/09; commit RIÊNG vì đụng exam-db/luồng thi)

- **Yêu cầu (nguyên văn tóm):** 00:01 thứ Hai 21/09 máy chủ XOÁ dữ liệu học sinh, GIỮ tài khoản + kho đề + cấu hình. Máy thầy phải dọn bản sao theo ca/em trong IndexedDB `omr-exam`, một lần, báo thầy một dòng; không đường tự đẩy nào được tạo lại ca cũ; M4 gắn khoá buổi lưu với `mocReset`.
- **Bốn sửa của 0.Planer đã làm đúng:** (1) tệp `src/lib/don-moc-reset-giao-vien.ts` (không trùng `don-moc-reset.ts` của Code 2), CHỈ IndexedDB `omr-exam`, CHỈ vai giáo viên (`laManThayQuanLy`), KHÔNG một dòng localStorage/sessionStorage, không nhắc `omr_student_portal_auth`/`omr_ph_sbd`/`ddh.em.*` (test khoá nguồn); (2) nhóm DỌN: store `sessionCache`, `sessionBankTeacher`, `attempts` + khoá `settings` `soCauCa:* khoChuaCa:* deRiengCa:* cheDoDeRieng:* qidRaPhieu:* diemCuaEm khoDoKho lichOnLai`; GIỮ: `scriptUrl mayChuMoi teacherSecret khoaApp khoaVanTay khoaPhien giuPhien soSuaDang tokenHocSinh tokenPhuHuynh myStudentSbd myParentPhone`, store `examSources` (kho đề), toàn bộ `omr-classlist`; (3) KÍCH HOẠT chỉ theo `mocReset` máy chủ trả (test khoá: không `Date`/timer/"2026-09-21" cứng trong mã), gọi TẠI NƠI NHẬN phản hồi `danhSachCa` — `danhSachCaMoi` (`man-ca-may-chu-moi.ts`, sau cổng an toàn `ca_day_du`) và `postJson` khi `action === 'danhSachCa'` — trước khi ai cất cache; không gọi ở `main.tsx`; (4) ĐAI + DÂY: `exam-sync.ts` `chiaBankTheoCaMayChu` / `maCaConTrenMayChu`, dùng ở `capNhatCaDaMo` và `NganHangDeScreen` (khối "cập nhật đáp án cho N ca").
- **Thiết kế:** danh sách DỌN nằm SÁT lược đồ trong `exam-db.ts` (hằng số `STORE_DON_KHI_RESET`, `TIEN_TO_SETTINGS_DON_KHI_RESET`, `KHOA_SETTINGS_DON_KHI_RESET`) + một GIAO DỊCH NGUYÊN TỬ `donDuLieuTheoMocReset(moc)` (xoá store, xoá khoá, ghi dấu `mocResetDaDon` cùng một giao dịch; hỏng giữa chừng ⇒ không dọn nửa vời, không dấu). Test soi mọi khoá `settings` trong `exam-db.ts`: khoá mới chưa được xếp DỌN/GIỮ thì ĐỎ (không ai thêm khoá theo ca/em mà quên quy tắc reset). Ca THÙNG RÁC tính là "còn trên máy chủ" (khôi phục được) nên bank của nó không bị xoá; danh sách rỗng hoặc bank ngoài danh sách quá 20% ⇒ chỉ KHÔNG ĐẨY, không xoá (cùng tinh thần `maCanXoa`); không hỏi được máy chủ ⇒ không đẩy gì. Toast chỉ khi thật sự có gì để dọn: "Hệ thống đã làm mới ngày 21/09 — dữ liệu ca cũ trong máy đã được dọn".
- **Đường tự đẩy đã rà:** `capNhatCaDaMo` (exam-sync) và khối cập nhật đáp án của NganHangDeScreen — cùng lặp mọi bank ca trong máy rồi `capNhatKeyBank` — ĐÃ CHẶN. Đường thứ ba `ExamMonitorScreen` (~dòng 473, tự vá khoá `key/<maCa>.json`) chỉ chạy cho ca thầy đang MỞ, mà ca ấy vừa được `chiTietCa` đọc từ máy chủ ⇒ không tạo được ca ma; giữ nguyên, không đụng.
- **Bằng chứng:** `tests/don-moc-reset-giao-vien-1909.test.ts` 31+ test (luật khoá, mốc hợp lệ, khi nào chạy/không, single-flight, giao dịch trên IndexedDB giả có hoàn tác, khoá nguồn, đai-dây kể cả `capNhatCaDaMo` chạy thật với mock); đột biến 20 chỗ, 19 bị bắt, 1 sống sót là bản THỪA (chốt "danh sách rỗng" bị chốt 80% bao trùm — giữ vì đọc là hiểu). IndexedDB THẬT trong Chromium (hồ sơ tạm): `node scripts/do-moc-reset-indexeddb.mjs` ⇒ nhóm DỌN sạch, nhóm GIỮ nguyên (kho đề, cấu hình, khoá app, token), dấu đúng, lần hai đếm 0. Bộ 130 tệp liên quan (exam-api/exam-db/exam-sync/NganHangDe/man-ca): MỚI ĐỎ [] (hai tệp đỏ thoáng qua — `de-rieng-ho-so-1909` đo giờ, `vao-thi-form-m3-1909` của phiên khác — chạy riêng đều xanh); `tsc -b` sạch.
- **Sai lầm trên đường làm (đã sửa, ghi để khỏi lặp):** lần đầu móc dọn vào thân `danhSachCaThat` làm đỏ 2 test khoá nguồn của `man-ca-thi-may-chu-moi-1109` (dòng `if (rMoi) return chuanCaTomTat(rMoi.items)` và khối try ≤ 500 ký tự) — chuyển móc sang `danhSachCaMoi` + `postJson` để KHÔNG phải sửa test cũ. Lần đầu cũng nạp tĩnh `appStore` làm 6 test có `vi.mock('appStore')` vỡ ("error when mocking a module") — đổi sang nạp động lúc báo thầy và tra phụ thuộc mặc định LÚC DÙNG.
- **Chờ Code 3:** tên trường `mocReset` phải nằm ở TẦNG NGOÀI của JSON phản hồi `/ca/danh-sach` và `action:'danhSachCa'` (chuỗi kiểu "2026-09-21", chỉ có sau khi job xong; ký tự cho phép chữ/số/`.`/`_`/`:`/`-`, ≤ 40). Chưa có `docs/moc-reset-1909.md` khi tôi làm — cần khớp khi có.

## Nhật ký M3 (21/09 sáng) — giao diện tờ chiếu theo ba bản vẽ đã chốt

- **Làm:** `src/lib/giao-dien-to-chieu.ts` (mới: CSS + hằng số + màu hào quang theo hệ) nạp SAU bố cục M2; `html-may-chieu.ts`: thẻ tên giấy ấm (thú có hào quang theo hệ, tên, "thú · cấp · lần lên bảng thứ N", chip câu), thẻ đề giấy ấm, vùng làm bài "tắt sáng" #15181c chỉ có nhãn mờ "Phần làm bài của <tên gọi>" + nút nhỏ, dòng đầu thẻ đề ("Câu N" + phần) ở đợt đơn, **thanh dưới** (đợt k/n · pha · đồng hồ · tiến độ · ô chữ · "Buổi: x/90 phút" · Cài đặt; nền giấy/cỡ chữ/toàn màn hình nằm trong hộp Cài đặt), **màn gọi tên** 2,5 s đầu mỗi đợt (bấm phím/chạm bỏ qua; thu về thẻ tên bằng bay 0,85 s), **pha LÀM BÀI → CHỮA → quá giờ** (chỉ đổi màu/chữ đồng hồ, KHÔNG tự lật trang; một nhịp `setInterval` duy nhất), **ăn mừng khi Đạt** (thẻ xanh "Làm tốt lắm", thú nhảy 1,5 s rồi đứng yên), **lời giải theo thang co chữ** (từ cỡ đề xuống sàn 24 px@1920, đợt đôi lấy cả nửa bảng khi mở, hết thang mới cuộn). Nhãn nút "Đạt"/"Chưa đạt" (`to-chieu-cau-noi.ts`); tin `DA_GHI`/`PHAN_HOI` từ màn giáo viên mang `dat` để tờ biết ăn mừng đúng em (nhắc ô đã ghi từ trước KHÔNG mang `dat`).
- **Tế nhị trước lớp:** tờ không in `viSao`; nhãn "Ở NHÀ LÀM SAI…" (thầy chốt 14/09, test `btvn-len-bang-1409` khoá) vẫn nằm trong markup nhưng CSS KHÔNG vẽ — bản vẽ 19/09 không có; sau khi ghi chỉ còn "Đã ghi" cho cả hai kết quả; CSS M3 không có màu đỏ (test soi). Chuyển động chỉ ở màn gọi tên + ăn mừng (test soi mọi `animation:`); `prefers-reduced-motion` bỏ luôn màn gọi tên.
- **QUYẾT ĐỊNH CẦN BOSS XEM:** (1) bản vẽ ghi "Làm tốt lắm · Thuỷ Lân **+10 EXP**" — tôi CHỈ in "Làm tốt lắm" (EXP mới chưa bật cho cả trường, số điểm chưa chốt); (2) BTVN cạnh tên em bị ẩn như trên — nếu thầy muốn giữ dòng "Về nhà: làm x/y" thì bật lại bằng một dòng CSS; (3) thanh điều khiển chuyển từ TRÊN xuống DƯỚI (bản vẽ không có thanh trên); ghi chú của đợt (tách đợt, chữ nhỏ, cần bảng phụ) nằm ở ô chữ của thanh dưới; (4) "Buổi: x/90" đếm đồng hồ thật từ lúc mở tờ (không phải tổng T kế hoạch); (5) ảnh thần thú ở thẻ tên là ảnh sẵn có (`thanThuV2ChoToChieu`) — không đổi.
- **Đo thật (Chrome, `scripts/do-bo-cuc-to-chieu.mjs`):** 40 câu mẫu (nay MỌI em có thú + "lần lên bảng" để thẻ tên cao như thật) × 4 khung: **0 vùng đề tràn, 0 chữ dưới sàn**; chữ nhỏ nhất 17 px@1280 (sàn 16) · 30 px@1920 (sàn 24) · 18@1366 (17) · 23,5@1600 (20). Hình học M3 làm bậc dồn lên (nhiều đợt bậc 4 hơn M2) vì thẻ tên to hơn và chip Câu N nằm trong thẻ đề. Ảnh: `m3-1-goi-ten`, `m3-2-hai-em`, `m3-3-an-mung`, `m3-3b-sau-an-mung`, `m3-4-cau-dai`, `m3-5-loi-giai`, `m3-6-giay-toi` × {1280, 1920}; `m2-*` (bậc 1–5, tách đợt) chụp lại với giao diện mới.
- **Ước lượng bậc HIỆU CHỈNH LẠI (hình học đổi):** mô hình chỗ sẵn có suy từ CSS (thanh dưới 50 px, đệm thẻ đề theo bề ngang, thẻ tên = max(ảnh, chữ) + đệm) khớp Chrome ≤ 1 px ở 4 khung (bậc 1/2/5). Kiểm ở **5 khung CHƯA từng dùng** (1440×810, 1536×864, 1024×768, 1280×800, 1680×1050; `do-bo-cuc-kiem-khung-la.json`): 0 câu lệch quá 1 bậc; ước THẤP 3 lần trên 200 phép (1536×864 `III-bảng-nhỏ` 1→2; 1280×800 `I-pa-dài-1/2` 2→3) — tờ tự tách nên không tràn. Thật thà: ở khung 1366×768 từng gặp 1 ước thấp 2 bậc (`I-đề-dài-95`, lệch đúng 1 dòng) nên nới RONG_CHU 1,0→1,01 và DU_AN_TOAN 1,02→1,01 — khung ấy vì vậy KHÔNG còn là kiểm độc lập; 5 khung mới mới là kiểm độc lập. Phát hiện+sửa trên đường: `mauHaoQuang('__proto__')` trả đối tượng nguyên mẫu (chữ `he` đến từ máy chủ) → dùng hasOwnProperty.
- **Test:** `giao-dien-to-chieu-1909` 41 test (markup/CSS, JS trong tờ chạy thật với đồng hồ + hẹn giờ giả: gọi tên/bỏ qua/giảm chuyển động, pha, quá giờ không lật trang, buổi, dạy học, ăn mừng chỉ Đạt, co chữ lời giải, cài đặt, khoá nguồn màn giáo viên); đột biến 28 chỗ (4 sống sót lần đầu → thêm test → 28/28 bị bắt). Đổi test cũ CỦA TÔI vì đặc tả mới: nhãn 'Không đạt'→'Chưa đạt' (`to-chieu-cau-noi`, `goi-len-bang-to-chieu-cham`), ghi chú lên ô chữ thanh dưới (`bo-cuc-to-chieu`), số đo hình học + 5 khung lạ (`uoc-luong-bo-cuc`, 61 test). Test cũ khác KHÔNG sửa: `may-chieu-len-bang-1409`, `btvn-len-bang-1409`, `day-hoc-dem-nguoc`, `than-thu-to-chieu`… xanh (đỏ còn lại toàn NỀN). Sửa 1 dòng ngoài làn: `scripts/do-bo-cuc-to-chieu.mjs` (mới hiểu env VIEWS/DIR_RA, đợi đo xong chắc hơn).

## Đổi bảng DỌN/GIỮ khi reset (21/09 — Boss chốt sau lệnh mới của thầy "GIỮ hồ sơ mạnh yếu")

- `KHOA_SETTINGS_DON_KHI_RESET` chỉ còn `diemCuaEm`. **GIỮ** `khoDoKho` (độ khó theo CÂU, ca xoá rồi không dựng lại được, thời gian lên bảng cần nó) và `lichOnLai` (hàng đợi ôn giãn cách em × dạng, chỉ có ở máy thầy). Thêm tiền tố `buoiChua:` (buổi chữa dở, M4) vào nhóm DỌN. Test `don-moc-reset-giao-vien-1909` + `scripts/do-moc-reset-indexeddb.mjs` (IndexedDB thật): DỌN sạch 7 khoá / 4 bản ghi, GIỮ kể cả `khoDoKho`/`lichOnLai`. `exam-db.ts` thêm `luuBuoiChua/docBuoiChua/xoaBuoiChua` (khoá `buoiChua:<mốc>|<lớp>|<mã ca>` trong `settings`; KHÔNG thêm store, không nâng phiên bản IndexedDB — `upgrade()` chạy lại trên máy học sinh đang thi dở và bị chặn nếu còn tab bản cũ).
- Mốc reset nay là NGÀY VN lúc job xong (Code 3, `docs/moc-reset-1909.md`): app thầy chỉ so "khác giá trị đã dọn" (`docMocResetDaDon`), không hằng số. Đã báo Code 3: hai lần reset cùng ngày cùng giá trị thì lần hai không dọn.

## Reset GIỮ TOÀN BỘ ca thi (21/09 — Boss chuyển lệnh mới của thầy)

- **Danh sách DỌN RỖNG** (`exam-db.ts`: `STORE_DON_KHI_RESET`, `TIEN_TO_SETTINGS_DON_KHI_RESET`, `KHOA_SETTINGS_DON_KHI_RESET`): máy thầy giữ `sessionCache`, `sessionBankTeacher`, `attempts`, `soCauCa:`/`khoChuaCa:`/`deRiengCa:`/`cheDoDeRieng:`/`qidRaPhieu:`, `diemCuaEm`, `buoiChua:*`, `khoDoKho`, `lichOnLai`. Toast bỏ (`chuThongBaoMocReset`, `baoThay` gỡ khỏi `don-moc-reset-giao-vien.ts`).
- **GIỮ cơ chế**: kích hoạt theo `mocReset` của máy chủ, một giao dịch nguyên tử, dấu `mocResetDaDon`, test "khoá mới phải được xếp loại" (nay đối chiếu với nhóm GIỮ). Thêm tên vào một danh sách là dọn thật — có test "cơ chế còn sống".
- **Đai-dây `capNhatKeyBank` lọc theo ca còn trên máy chủ**: giữ nguyên (`exam-sync.ts` không đổi).
- **Việc em tự quyết thêm (nói thật cho Boss soát)**: khoá buổi chữa dở TRƯỚC là `mốc reset | lớp | mã ca`; nếu để nguyên thì sau reset dấu mốc đổi ⇒ khoá đổi ⇒ buổi dở bị "giữ" mà không còn ai tìm thấy. Nên màn `GoiLenBangScreen` nay luôn dùng `MOC_KHOA_BUOI = ''` (khoá `-|lớp|mã ca`, trùng khoá M4 trước mọi reset) và bỏ kiểm `raw.moc !== moc` — buổi dở NỐI ĐƯỢC qua reset. Nếu Boss muốn buổi bị cắt sau reset thì đổi lại một dòng.
- Kiểm: `tsc -b` sạch; `don-moc-reset-giao-vien-1909` + `noi-buoi-chua-1909` + `goi-len-bang-noi-buoi-1909` 60/60; hai đột biến (thêm lại một khoá vào danh sách dọn; màn dùng khoá lạ) đều làm test đỏ; `scripts/do-moc-reset-indexeddb.mjs` trên IndexedDB thật của Chromium: ĐẠT (giữ mọi thứ, dấu ghi đúng, đếm 0 hai lần).

## Nhật ký M4 (21/09) — lưu buổi chữa + tự nối buổi sau

- **Làm:** `src/lib/noi-buoi-chua.ts` (mới, thuần): khoá buổi `mốc reset | lớp | mã ca`, bản ghi `BuoiChuaLuu` (nguồn câu, câu của buổi cộng dồn, câu bắt buộc, kế hoạch em×câu của lần xếp gần nhất, kết quả đã ghi), hạn 14 ngày từ lần lưu gần nhất, `tinhTrangBuoi` (còn lại = câu của buổi − câu đã có kết quả; đối chiếu THÊM `lichSuLenBang` máy chủ: em trong kế hoạch có qid ấy và `lanCuoi` SAU lúc buổi bắt đầu ⇒ đã chữa ở máy khác; lịch sử cũ hơn/ không đọc được giờ thì KHÔNG tin), `emGiuKhiNoi`. `exam-db.ts` (commit d3dba04): `luuBuoiChua/docBuoiChua/xoaBuoiChua`. Engine E: `CauVaoXep.emDaDinh` — em đã định còn có mặt, rảnh, không bị chặn bậc thì GIỮ; ngược lại chọn em hợp nhất như thường (đặt ở từng câu để `xepBuoiChua(cauVaoXep, hoSo)` giữ nguyên dạng test cũ khoá). Màn giáo viên: mở ca ⇒ đọc bản lưu (hỏng / khác khoá / khác mốc / quá hạn ⇒ bỏ và xoá); xếp giờ và mỗi lần ghi Đạt/Chưa đạt ⇒ lưu (lỗi lưu chỉ cảnh báo trong console); thẻ **"Tiếp tục buổi trước · còn N câu (đã chữa M)"** trên nút Xếp giờ với hai nút [Tiếp tục buổi trước] [Bắt đầu buổi mới]; có buổi dở mà chưa chọn thì Xếp giờ bị CHẶN (không lặng lẽ ghi đè). Tiếp tục ⇒ dựng lại nguồn câu (đề đã tích, cách lấy, số câu/lọc), chỉ xếp câu còn lại (bắt buộc chưa chữa đứng đầu), giữ em còn có mặt (thầy bỏ tích em vắng ở mục 3 trước khi bấm ⇒ câu của em vắng giao em có mặt hợp nhất); giữ giờ bắt đầu buổi; câu máy chủ xác nhận được GHI vào bản lưu. Bắt đầu buổi mới ⇒ xoá bản lưu máy này (KHÔNG đụng lịch sử máy chủ). Chế độ dạy học không lưu.
- **KHÁC ĐẶC TẢ (Boss đã duyệt):** không thêm store IndexedDB mới, không nâng phiên bản — lưu ở `settings` khoá `buoiChua:<khoá>` (lý do và tiền tố DỌN khi reset: xem mục "Đổi bảng DỌN/GIỮ" trên).
- **Nói thật về giới hạn:** (1) "đã chữa" = câu có ÍT NHẤT MỘT ô ghi kết quả (Đạt hay Chưa đạt); câu bị dời (đọc đáp án, không em nào được gọi) vẫn là "còn lại"; (2) lịch sử máy chủ chỉ có `qids` + `lanCuoi` theo em nên phải chặn theo giờ bắt đầu buổi — câu em chữa CÙNG NGÀY ở buổi khác, cùng qid, có thể bị tính nhầm là đã chữa; (3) nguồn 'rút theo dạng' dựng lại theo số câu/lọc đã lưu — kho đề đổi giữa hai buổi thì câu có thể khác (màn báo "N câu của buổi trước không còn trong nguồn hiện tại"); (4) em vắng phải được bỏ tích TRƯỚC khi bấm Tiếp tục (thẻ nhắc); bỏ tích sau thì bấm Xếp giờ lại như thường (buổi nối vẫn ở trạng thái nối).
- **Bằng chứng:** `noi-buoi-chua-1909` 17 test (khoá/hạn/hình dạng hỏng, cộng dồn qua các lần nối, còn lại/đã chữa, lịch sử máy chủ cũ hơn/rác/em khác, em giữ/vắng, Engine E giữ em, bất biến khi không có định); `goi-len-bang-noi-buoi-1909` 12 test trên màn thật với IndexedDB giả sống qua `cleanup()+render()` ("tắt app → mở lại"): lưu ngay khi xếp, bấm Đạt vào bản lưu, thẻ đúng số câu, Xếp giờ bị chặn, Tiếp tục (không xếp lại câu đã chữa, giữ em, không ghi lại `ghiLenBang`), em vắng thay, Bắt đầu buổi mới, quá 14 ngày, sau reset, bản lưu hỏng ×4, ca/lớp khác, máy chủ xác nhận, dạy học không lưu. Đột biến 21 chỗ: 19 bị bắt trực tiếp, 2 sống sót đều là BẢN THỪA (guard `dayHoc` của effect lưu — `kqBuoi` vốn null ở dạy học; `daChua` cũ ⊆ `daGhi` nên không cộng lại cũng đủ) — đã thêm phần ghi câu máy chủ xác nhận vào bản lưu (test + đột biến bắt). Bộ 46 tệp gọi-lên-bảng (869 test): MỚI ĐỎ []; `tsc -b` sạch.

## Nhật ký M5 (21/09) — tối ưu "học nhiều nhất trong buổi"

- **Làm** (`xep-buoi-chua.ts`, Engine E; hằng số ở `len-bang-cau-hinh.ts` → `HOC_NHIEU`; `emYeuDang` ở `ho-so-lop.ts`): giá trị câu `v = tỉLệLớpSai × (1 + 0,5 × [dạng lớp yếu CHƯA có câu nào được chữa trong buổi])`; trong TỪNG tầng đã chốt (bắt buộc → 2 sao → 1 sao → 0 sao) chọn câu theo MẬT ĐỘ `v / giây thêm thật`; ràng buộc: còn dạng lớp yếu chưa có câu nào thì không chữa câu thứ hai của dạng đã phủ. Bắt buộc là TẦNG (luôn chọn trước), không phải giá trị "∞". Màn hình: khối buổi chữa thêm "giá trị chữa N" và "phủ a/b dạng lớp yếu" (`hocNhieu`, chỉ số đo được — không chữ "nắm chắc"); số em và số câu dời buổi sau đã có sẵn trên khối; bản in `bangChuBuoiChua` mang thêm dòng `hocNhieu.tomTat`. Thiếu dạng chưa phủ thì cảnh báo NÊU TÊN dạng.
- **Hai số em TỰ CHỌN (đặc tả chỉ nêu định nghĩa mức EM)**: dạng LỚP yếu = ≥ `DANG_YEU_LOP_TOI_THIEU_EM` = 3 em CÓ MẶT có số liệu dạng VÀ ≥ `DANG_YEU_LOP_TL_EM` = 50% số em ấy yếu (định nghĩa yếu của em = đúng `nam_kt_dang` máy chủ: ≥ 4 câu đã gặp, (khắc phục + chưa từng sai)/đã gặp < 0,7 — một định nghĩa hai chỗ gọi với `diemHopCau`). Đổi ở `HOC_NHIEU`, test khoá cả ba giá trị. Dạng của câu = `namKt.maDang` của em đầu tiên có dòng cho câu ấy (máy chủ đời cũ không có ⇒ không dạng yếu nào, Engine E chọn như trước, chỉ khác mật độ).
- **GHÉP SONG SONG THẬT + trả nợ ½ (ghi ở M2)**: M1 tính ½ phần làm bài cho MỌI câu, kể cả câu bậc ≥ 2 đứng một mình (tờ chiếu chỉ ghép đôi hai câu bậc 1 liền nhau). Nay: câu bậc 1 ghép đôi THEO THỨ TỰ CHỌN — câu thứ hai của cặp trả `T_chữa + phần làm bài vượt em kia`, câu lẻ và câu bậc ≥ 2 và câu thiếu văn bản trả TRỌN T. `dong.giay` khớp đúng thứ đã cộng vào ngân sách (tổng các dòng = `tongGiay`). Chưa biết bậc (test dựng tối giản) ⇒ coi như ghép đôi được — nên các test cũ không đổi.
- **TEST CỦA EM ĐÃ ĐỔI (nói thật, không sửa cho xanh)**: `thoi-gian-len-bang-1909` — hai test khoá `dong.giay = giayBienGhepDoi(T)` cho MỌI dòng (đúng cái ½ là nợ): nay khoá theo phép mới (cặp ghép đôi: `T_chữa + max(T_đọc+T_làm)/2`; câu lẻ: `T`). Test "SÀN 20 EM GIỮ ĐƯỢC…" và "LÝ DO có hệ số ½" GIỮ NGUYÊN và xanh. Hợp đồng cũ `xep-buoi-chua-1409`, `uoc-luong-bo-cuc-1909`, `noi-buoi-chua-1909` xanh không sửa.
- **Đo trung thực**: kịch bản "đề thật" (30 em, 24/30/40/50 câu, chưa biết bậc bố cục) — Engine E cũ: 22/21/20/20 em; mới (sau khi chấm mật độ theo GIÁ THÊM THẬT của câu ở trạng thái ghép hiện tại): 21/20/21/20 em, tổng 4901/4812/4864/4878 ≤ 4.920. Tức 24 câu tụt 1 em, 30 câu tụt 1 em, 40 câu hơn 1 em — vẫn ≥ 20. Bản đầu (mật độ theo giá ước ½) chỉ ra 20/18/19/19 nên đã sửa. **Rủi ro còn lại**: khi thật sự nhiều câu bậc ≥ 2 thì sàn 20 em CÓ THỂ trượt (vì trả đủ T là đúng) — buổi nói thật lý do "hết ngân sách", không lặng lẽ tính ½.
- **Bằng chứng**: `hoc-nhieu-nhat-1909` 21 test (định nghĩa dạng yếu + biên 0,7/50%, ràng buộc phủ dạng có thứ tự đúng, tổng giá trị 3,3, tầng bắt buộc giữ, mật độ theo giây, cảnh báo nêu tên dạng, ghép đôi 2/3/lẻ/lệch, bậc ≥ 2 trọn T, không vượt ngân sách, sàn 20 em, tóm tắt, tất định, khoá nguồn); 11 đột biến (bỏ ràng buộc, bỏ chia giây, ghép mọi câu, không ghép, hạ ngưỡng em, đổi ≥ thành >, bỏ thưởng, không ghi dạng phủ, `<=` ở em yếu, đổi hệ số, cảnh báo không tên) đều làm test đỏ (một đột biến biên ≥/> sống sót ở lượt đầu → thêm test đúng 2/4 em rồi bị bắt). Cả 37 tệp test chạm các module này: chỉ đỏ đúng 12 test đã đỏ sẵn ở HEAD sạch (đối chiếu bằng worktree tạm: `man-goi-len-bang-thuat-toan-moi-1409`, `du-lieu-len-bang`, `may-chieu-len-bang-1409`, `sua-ba-muc-1609`, `goi-len-bang-nhieu-de`, `co-len-bang`).

## Nhật ký M6 (21/09) — tự hiệu chỉnh giờ từ giây thật

- **Đo (tờ chiếu, `html-may-chieu.ts` + `to-chieu-cau-noi.ts`)**: `gd.batLam` = lúc LÀM BÀI của đợt bắt đầu (sau màn gọi tên; `-1` = không đo). Khi thầy bấm một ô, cầu nối gọi `window.__mcGiayDot(vung)` → `{giay, duTinh}` gửi CÙNG tin `CHAM`. `duTinh` = LÀM của đợt (max các ô) + Σ CHỮA của các ô đã bấm tới lúc này (kể cả ô này) — theo giờ CHƯA hiệu chỉnh (tờ giữ thêm `data-lam0`/`data-chua0` làm mẫu số; `data-lam`/`data-chua` là giờ đã hiệu chỉnh cho đồng hồ). Không đo khi: chế độ dạy học, đợt không có giờ, còn ở màn gọi tên, ô không thuộc đợt đang hiện. Sang đợt kế thì đồng hồ và danh sách ô đã bấm làm lại.
- **Lọc (app, `kiemTinToChieu`)**: `giay` 20..1800 và `duTinh` > 0 — ngoài khoảng thì BỎ số đo nhưng VẪN ghi kết quả (Đạt/Chưa đạt quan trọng hơn số đo). `ghiLenBang` gửi thêm `giayThuc`; mẫu `{phan, sao, giay, duTinh}` chỉ được LƯU SAU KHI máy chủ đã nhận (`themMauGiayThuc`, một giao dịch đọc-ghi, khoá `settings:giayThucLenBang`, giữ 400 mẫu mới nhất, GIỮ khi reset).
- **Hệ số (`hieu-chinh-giay-thuc.ts`, thuần, tất định)**: theo (phần, sao) = TRUNG VỊ(giây/duTinh) khi có ≥ 8 mẫu, kẹp 0,6..1,4; dưới 8 mẫu không hiệu chỉnh. Trung vị (không phải trung bình) để một lần bỏ quên tờ 20 phút không kéo lệch. Hằng số ở `GIAY_THUC` (`len-bang-cau-hinh.ts`). Áp bằng `thoiGianCau({heSo})`: nhân ĐỀU ba thành phần rồi kẹp/làm tròn như cũ (không hệ số ⇒ kết quả GIỐNG HỆT bản cũ); câu thiếu văn bản (300/180/120) KHÔNG bị hiệu chỉnh. Màn nạp mẫu lúc mở, tính lại NGAY sau mẫu mới; hệ số đi vào Engine E (`CauVaoXep.heSoHieuChinh`) và tờ chiếu (`OBang.heSoHieuChinh`) — buổi đã xếp không tự xếp lại giữa chừng, lần "Xếp giờ" kế và tờ chiếu mở kế dùng giờ mới.
- **Máy chủ**: `docs/hop-dong-giay-thuc-len-bang-1909.md` gửi Code 3 (cột chỉ-thêm `len_bang.giay_thuc REAL`, đọc `giayThuc` ở `POST /len-bang`, ngoài khoảng ⇒ NULL, không bao giờ từ chối lệnh ghi). App KHÔNG phụ thuộc cột này: hiệu chỉnh từ mẫu ở máy thầy. Nói thật: nghĩa là hệ số là của MÁY này (một giáo viên một máy — đúng thực tế); đổi máy thì mẫu chưa theo sang cho tới khi có đường đọc lại từ máy chủ (hợp đồng chưa đòi, vì giây của các ô trong cùng đợt cộng dồn nên máy chủ khó tổng hợp đúng nếu không biết cấu trúc đợt).
- **Giới hạn nói thật**: (1) giây thật gồm cả lúc thầy giảng thêm / dừng để trả lời — trung vị giảm ảnh hưởng nhưng không loại hết, và đó cũng là thời gian thật của buổi; (2) "đợt bắt đầu" tính từ LÀM BÀI, không tính 3 giây gọi tên; (3) mẫu theo (phần, sao) chứ không theo độ dài hay bậc em — nhóm nhỏ nên nhanh có hệ số nhưng thô.
- **TEST CỦA EM ĐÃ ĐỔI (nói thật)**: `to-chieu-cau-noi-1909` khoá tin `CHAM` bằng `toEqual` đúng bốn trường ⇒ nay khoá "bốn trường + tuỳ chọn `giay`/`duTinh`, không gì khác". `don-moc-reset-giao-vien-1909`: thêm khoá `giayThucLenBang` vào nhóm GIỮ (bài kiểm "khoá mới phải được xếp loại" bắt buộc). `goi-len-bang-to-chieu-cham-1909`: mock `exam-db` thêm hai hàm mẫu; các test cũ trong file không đổi.
- **Bằng chứng**: `hieu-chinh-giay-thuc-1909` 28 test (hằng số, lọc số lạ + biên 20/1800, ngưỡng 7/8 mẫu, trung vị chẵn/lẻ, ngoại lai, kẹp, tách nhóm, tất định, lưu 400 mẫu mới nhất, `thoiGianCau({heSo})` giống hệt khi không hệ số, kẹp, không đụng 300/180/120, Engine E, đo giây trong JSDOM: không thú/có thú/bấm lúc gọi tên/hệ số/dạy học/bấm hai lần/sang đợt kế, `kiemTinToChieu`, khoá nguồn) + 7 test đầu-cuối ở `goi-len-bang-to-chieu-cham-1909` (tờ → màn → `ghiLenBang` có `giayThuc` → mẫu lưu → hệ số đủ 8 mẫu thì buổi và tờ chiếu kế dùng giờ mới; số đo hỏng vẫn ghi; máy chủ từ chối không lưu; bấm ở bảng buổi không đo). 21 đột biến (đo/mốc bắt đầu/mẫu số/dạy học/xoá ô đã bấm khi sang đợt/biên 20/ngưỡng 8/trung bình/kẹp/mẫu cũ-mới/Engine E/cầu nối/màn không chuyển-lưu-cập nhật/không truyền hệ số cho Engine E hay tờ) đều làm test đỏ (hai đột biến sống sót ở lượt đầu — xoá ô đã bấm khi sang đợt, cập nhật hệ số sau mẫu mới — đã thêm test rồi bị bắt). Tập 164 tệp test chạm các module này: đỏ ĐÚNG 29 test đã đỏ ở HEAD sạch (so tên bằng worktree tạm), mới đỏ 0, +35 test.

## Nút "Cho vào bằng máy khác" + thông báo nói thật sau "Cho thi lại" (21/09 — Boss giao)

- **Vì sao**: sau "Cho thi lại" máy chủ (`choThiLai`) khoá lượt mới vào ĐÚNG MÁY CŨ (`id_thiet_bi`). Máy cũ hỏng / mất thì thầy không có đường ra. Lệnh `moKhoa` của máy chủ (Code 3, `moKhoaEm`) đã có nhánh gỡ khoá máy của lượt `duoc_duyet_lai` và trả `goKhoaMay`; app chưa dùng.
- **Làm** (`ExamMonitorScreen.tsx`, `exam-api.ts`): (1) `moKhoa` nay TRẢ `{soDong, goKhoaMay}` (`goKhoaMay` chỉ khi máy chủ báo boolean thật; máy chủ đời cũ ⇒ `undefined`); (2) `LuotThiRow.khoaMay?: boolean`; (3) nút "Cho vào bằng máy khác" ở lượt `duoc_duyet_lai` khi `khoaMay !== false` (chưa biết ⇒ vẫn hiện, vì kết quả sau khi bấm nói thật), MỘT lần xác nhận ("Em sẽ vào được bằng MÁY KHÁC… Đề mới giữ nguyên; máy cũ cũng vẫn vào được." + Đồng ý / Huỷ), gọi đúng lệnh `moKhoa` sẵn có (không lệnh mới); (4) thông báo theo `goKhoaMay`: `true` ⇒ "Đã gỡ khoá máy…" (xanh) · `false` ⇒ "Lượt này không còn khoá máy…" (cam) · vắng ⇒ "máy chủ chưa báo có gỡ khoá máy hay không — nhờ em thử vào bằng máy khác" (cam) · từ chối ⇒ lỗi kèm lý do (đỏ); (5) thông báo "Cho thi lại" đã nói theo `soLuotXoa`/`daDoiDe`/`khoaMay` từ trước — nay thêm gợi ý đường ra ("máy cũ hỏng thì bấm "Cho vào bằng máy khác"") CHỈ khi thật sự khoá máy.
- **Máy chủ**: `/ca/chi-tiet` và `/ca/luot` không trả gì về máy ⇒ đã xin Code 3 thêm `khoaMay: boolean` (chỉ có/không, KHÔNG trả mã máy) — Code 3 đã đẩy (commit d996df4, Worker 97f13963). Client đọc trường này nguyên dạng (lượt đi qua không đổi hình).
- **Bằng chứng**: `cho-vao-may-khac-2109` 12 test (nút chỉ hiện đúng lượt; MỘT lần xác nhận, Huỷ không gọi; gọi `moKhoa` đúng một lần với đủ đối số + tải lại ca; ba kết quả `goKhoaMay` true/false/vắng + lỗi; nút "Mở khoá" cũ không đổi; ba thông báo "Cho thi lại") + `mo-khoa-ket-qua-2109` 4 test (`moKhoa` thật qua `fetch` giả: gửi đúng lệnh, ba giá trị `goKhoaMay`, lỗi). 8 đột biến (hiện khi chỉ biết chắc / hiện cả lượt không khoá / bỏ xác nhận / vắng coi như đã gỡ / câu `false` nói đã gỡ / bỏ gợi ý / ép boolean ở màn và ở api) đều làm test đỏ. `ca-thi-m3-2109` 29 test cũ xanh không sửa. 142 tệp test chạm màn/`exam-api`: đỏ đúng 33 test đã đỏ ở HEAD sạch, mới đỏ 0.
- **Chưa kiểm sống**: nút cần một ca thật có lượt `duoc_duyet_lai` đang khoá máy — không có trong dữ liệu 12121212 và không được tạo dữ liệu thật để thử. Kiểm bằng test (máy chủ giả) và test của Code 3 cho `moKhoaEm`; lần đầu thầy dùng thật cần liếc thông báo.

## Soát màu app thầy M3 (21/09 — Code 4 nhờ, Boss giao soát cuối)

- `check:mau` đỏ ở hai tệp của làn này. (1) `GoiLenBangScreen.tsx`: 6 hex thô → token M3 theo bảng của Code 4 (`#1e8e3e`→`--m3-on-tertiary-container`; `#1a73e8` chữ→`--m3-primary`; `bg-[#1a73e8] text-white`→`--m3-primary` + `--m3-on-primary`) — riêng chip "Tôi tự chọn bài để chữa" chữ xanh trên nền xanh nhạt từ 3,47:1 lên đủ ngưỡng (Code 4 đo). Bộ sinh `sinh-m3-tuong-thich.mjs` chạy lại: không đổi tệp sinh. (2) `lib/giao-dien-to-chieu.ts` (bảng màu của tờ chiếu): thêm vào `CHO_PHEP` của `scripts/check-mau.mjs` cùng `html-may-chieu.ts` — lý do "rời khỏi máy": tờ là HTML rời trong iframe, lưu/mở tab riêng/chiếu máy khác được, và nền bảng luôn TỐI (#15181c) dù app sáng; test `giao-dien-to-chieu-1909` khoá từng giá trị. 76 hex còn lại của `check:mau` nằm ở tệp phiên khác (`game.css`, `index.css`…).
- Kiểm: m3-b + m3-c + màn Gọi lên bảng (4 tệp test của em) 118/118 xanh.

## BTVN "nâng đỡ" — LÕI THUẦN `src/lib/btvn-nang-do.ts` (21/09 — thầy chốt, Boss giao; việc SỐ 1)

- **Commit**: kiểu + chữ ký `27059a6` (Code 3/4/2 đã bám); thân hàm + test ở commit kế (mã ghi cuối mục). Đề bài `prompt-btvn-nang-do.md` mục Code 1, thiết kế `DE-XUAT-BTVN-NANG-DO-2109.md` mục 2 B–F.
- **Hàm**: `chonLoi(cau, ghim=[])` (lõi ≈ 30 %, mỗi dạng 1–2 câu, MỨC THẤP NHẤT của dạng trước rồi sao cao rồi phần I — Boss duyệt 21/09, đổi thứ tự "sao cao → mức thấp" của đề bài; ghim tuỳ chọn, chạy đúng với `[]` và ≤ 10) · `chonBoCuaEm(cau, loi, hoSo, nganSach, hatGiong)` (bậc đích = bậc hiện tại của dạng, +1 khi dạng ổn và đúng ≥ 2 câu ở bậc hiện tại; câu riêng chấm điểm rồi lấp; dạng yếu ≥ 2 câu được ÉP; thử thách 15 % / trần 20 %; chặng chia vòng tròn theo độ dễ) · `theTienBo(truoc, sau)` (chỉ số đếm) · `mucTuChu`, `maDangCua` · `thichNghiChangSau` (Đợt 2) mới là chữ ký, hiện trả nguyên bộ. Thuần: chỉ import `hashSeed` (exam-shuffle), không `Math.random`/đồng hồ/IO; test khoá điều đó.
- **Em tự quyết (đã báo Boss, đã duyệt): (1)** `loi`/`rieng`/`thuThach` là ba tập RỜI NHAU, hợp = các chặng; **(2)** "không câu nào vượt bậc đích + 1" áp cho phần NGOÀI lõi; câu LÕI cao hơn bậc đích + 1 của em mang nhãn `loi_cao` — đứng CUỐI chặng (trước thử thách), ≤ 2 mỗi chặng khi đủ chỗ, dồn đều, không bao giờ mở chặng (Boss thêm điều kiện này); **(3)** chưa đủ tin (< 4 câu đã gặp) ⇒ bậc đích Biết, nên em chưa hồ sơ nhận lõi + Biết, và Hiểu chỉ ở dạng thử thách; **(4)** dạng YẾU không được mở +1 bậc dù đã đúng 2 câu (yếu là yếu tới khi tỉ lệ khắc phục lên ≥ 0,7); **(5)** câu em đã đúng lại ≥ 2 NGÀY KHÁC NHAU bị trừ 4 điểm nên hầu như không vào phần riêng (em giỏi bỏ câu dễ), trừ khi cần cho đủ 2 câu của dạng yếu; **(6)** nếu số dạng > 35 % số câu thì vẫn mỗi dạng một câu (phủ dạng hơn con số 30 %); **(7)** bộ có thể NHỎ hơn ngân sách khi không còn câu vừa sức (chỉ lấp câu có điểm > 0) — không nhồi câu phí sức cho đủ số.
- **Giới hạn nói thật**: (a) "đúng liền ≥ 2 câu" suy từ trạng thái từng câu trong bài này (`da_khac_phuc`/`chua_thay_sai`), không có thứ tự thời gian — đúng liền thật là việc thích nghi Đợt 2; (b) máy chủ (Code 3) phải dựng `HoSoEmRut` đúng quy ước: câu em chưa gặp KHÔNG có dòng; `tiLeKhacPhuc` = (khắc phục + chưa từng sai)/đã gặp; `ngayDungKhacNhau` là số ngày khác nhau em từng đúng câu ấy (cột này chưa có ở `nam_kt_cau` — Code 3 cần lấy từ sổ sự kiện); (c) thẻ tiến bộ không có EXP (Code 2 xin từ máy chủ).
- **Bằng chứng**: `tests/btvn-nang-do-2109.test.ts` 31 test — tính chất trên 64 em (rỗng · yếu · trung bình · khá · 60 ngẫu nhiên hạt giống cố định) × 6 ngân sách: lõi ⊆ bộ mọi em, ba tập rời nhau và hợp = chặng, không câu ngoài lõi vượt bậc đích + 1, +1 bậc chỉ là thử thách ở dạng không yếu và ≤ 20 % bộ, em chưa hồ sơ ⇒ lõi + Biết/Hiểu, dạng yếu ≥ 2 câu (kể cả ép), |bộ| ∈ [|lõi|, N] và ≤ ngân sách, mỗi chặng mở bằng 2 khởi động là câu dễ nhất, thử thách cuối, `loi_cao` cuối/≤ 2/không mở/dồn đều, tất định (cùng hạt giống → cùng bộ; hạt giống khác → phá hoà khác), 80 câu × 300 em ≈ 20 ms (< 200 ms), lõi lấy mức thấp nhất (40 bài ngẫu nhiên), ghim `[]` và 10, khoá thuần. 23 đột biến (sao trước mức, bỏ ghim, TL_LOI, riêng cho +1, thử thách ở dạng yếu, bỏ trần 20 %, bỏ trừ câu đã đúng lại, bỏ ép dạng yếu, không gắn `loi_cao`, `loi_cao` giữa chặng, thử thách đứng đầu, mở bậc chỉ 1 câu, mở bậc ở dạng yếu, 1 khởi động, ngân sách < lõi, bỏ hạt giống, `mucTuChu`, `theTienBo` bỏ `dang_on`/tụt bậc tính là tiến, chặng mở bằng câu khó, dồn `loi_cao` vào chặng 1…) đều làm test đỏ; ba đột biến sống sót ở lượt đầu → thêm test rồi bị bắt.

## BTVN nâng đỡ — ĐỢT 2 của lõi: `thichNghiChangSau` + cổng `dieuChinh` (21/09 — Boss giao sau khi soát 27059a6/569ebfd)

- **Cổng `dieuChinh?` của `chonBoCuaEm`** (nguồn: `DE-XUAT-BO-NAO-AI-2109.md` mục 4): `nhip` (±3 câu/ngày, kẹp [6, 16] nhưng KHÔNG kéo ngân sách gốc nằm ngoài khoảng về phía không được điều chỉnh; nhịp 0 = nguyên gốc) · `khoiDong` 1–3 (mặc định 2) · `dang[]` ≤ 3 dạng, dạng lặp lấy dòng đầu, nút lạ bỏ: `uu_tien` (như dạng yếu: điểm +3, ép ≥ 2 câu, nhãn `dang_yeu`; `soDangYeu` vẫn chỉ đếm dạng yếu THẬT) · `ha_mot_bac` (bậc đích −1; câu lõi cao hơn bậc mới + 1 thành `loi_cao`) · `cho_thu_len_bac` (bậc đích +1 nhưng KHÔNG quá bậc hồ sơ + 1, không áp dạng YẾU, và không nâng thêm nếu đã được mở bậc vì đúng ≥ 2 câu — không nhảy 2 bậc) · `tam_nghi` (phần riêng/thử thách không lấy câu của dạng; lõi vẫn ở lại). CẤM cứng theo luật thầy: không rút lõi, không câu ngoài lõi vượt bậc đích + 1 (tính theo bậc chưa điều chỉnh), không chọn mã câu. Cờ `tut_nhip`/`qua_tai`/`lam_cho_xong`/`nghi_chep` KHÔNG có ở lõi — máy chủ dịch chúng thành `nhip`.
- **KHOÁ "vắng ⇒ Y HỆT"**: chữ ký vàng `1036170693` trong test = băm 360 bộ (60 em ngẫu nhiên × 6 ngân sách) tính bằng ĐÚNG bản 569ebfd (chạy lại bản cũ trong tệp tạm, khớp với bản mới); test chạy với `undefined`, `null`, `{}`, `{nhip:0}`, `{dang:[]}`, `{khoiDong:2}`, nhịp NaN, dạng không có trong bài — đều ra đúng số ấy. Không thêm trường nào vào `BoCuaEm`/`tomTat`.
- **`thichNghiChangSau(bo, cau, hoSo, chiSoChangVuaXong, ketQua, {soChangDaMo?, dieuChinh?})` → `{bo, henOnLai, doi}`** (đổi kiểu trả về so với chữ ký cũ — chưa ai bám): CHỈ đổi chặng chưa mở (`max(chỉ số vừa xong + 1, soChangDaMo)`); mẫu = câu THƯỜNG (lõi/dạng yếu/củng cố) của dạng ở chặng vừa xong — khởi động (quá dễ), thử thách và `loi_cao` KHÔNG vào mẫu và KHÔNG vào lịch ôn ("sai không sao", khớp hợp đồng của Code 3). Đúng ≥ 80 % (≥ 2 câu; dạng yếu ≥ 3) ⇒ đổi 1 câu bậc thấp lấy 1 câu đúng bậc đích + 1; sai ≥ 50 % ⇒ thêm 1 câu cùng dạng bậc thấp hơn (hết ngân sách thì nhường một câu củng cố của dạng khác; không có gì nhường thì không đổi); tối đa 3 đổi mỗi chặng, dạng theo mã tăng dần; câu thường sai ⇒ `henOnLai` (Code 3 hẹn ôn 1·3·7). Phải truyền lại ĐÚNG `dieuChinh` đã dùng lúc chọn bộ (bộ không mang nó theo vì không được thêm trường).
- **Nói thật / nợ nhỏ**: (1) một quirk CÓ SẴN từ 569ebfd: khi ngân sách chỉ hơn lõi vài câu và nhiều ngày (ví dụ 7 ngày × 4 câu/ngày), toàn bộ chỗ trống có thể thành câu thử thách (phần riêng = 0). Sửa sẽ đổi kết quả khi vắng cổng nên KHÔNG sửa trong đợt này — chờ Boss quyết. (2) "Đúng liền" vẫn suy từ trạng thái từng câu, không có thứ tự thời gian. (3) Câu đổi lên bậc ở thích nghi là phần riêng thường (không tính vào trần 20 % thử thách) vì bậc của dạng đã tăng trong bài.
- **Bằng chứng**: `btvn-nang-do-2109` nay 51 test (thêm 20): golden + nhịp (kẹp, biên ngoài khoảng, nhịp 0) · khởi động · từng nút dạng · tối đa 3 dạng/lặp/nút lạ · bất biến trên 60 em × 40 cổng ngẫu nhiên (lõi nguyên, ba tập rời, ≤ ngân sách sau nhịp, ≤ bậc đích + 1, thử thách ≤ 20 %) · thích nghi: ví dụ dựng tay từng luật, khoảng giữa không đổi, mẫu ít, khởi động/thử thách/lõi cao không vào mẫu, chặng cuối và chỉ số lạ, `soChangDaMo`, `dieuChinh`, tối đa 3 đổi, tính chất trên 64 em × 12 kết quả (lõi + thử thách nguyên, chặng đã mở nguyên, câu mới không lõi/không trùng/≤ bậc + 1, ≤ ngân sách, tất định, không sửa đầu vào; có ≥ 50 đổi thật). 33 đột biến mới (nhịp ×4, khởi động, 3 dạng, từng nút ×6, ngưỡng ×4, mẫu ×3, mẫu chứa khởi động/thử thách, hẹn ôn, chặng đích ×2, ngân sách, thứ tự dạng, …) đều làm test đỏ; 5 sống sót ở lượt đầu → thêm test rồi bị bắt (còn 2 đột biến TƯƠNG ĐƯƠNG — `nhip === 0` return và chặn `bacDich < 2` khi không có câu bậc 3 — không thể phân biệt bằng hành vi).

## BTVN nâng đỡ — sửa quirk chia chỗ trống (21/09 — Boss quyết "SỬA NGAY")

- **Lỗi có sẵn từ 569ebfd**: số thử thách tính trên NGÂN SÁCH CẢ BỘ (15 % ngân sách, tối thiểu 1) nhưng lấy từ chỗ trống SAU LÕI ⇒ khi ngân sách chỉ hơn lõi vài câu và nhiều ngày (7 ngày × 4 câu/ngày ⇒ 28 − 24 = 4 chỗ) toàn bộ chỗ trống thành thử thách, phần riêng = 0, em yếu không có câu đúng bậc.
- **Sửa** (`chonBoCuaEm`): thử thách tính trên CHỖ TRỐNG SAU LÕI — trần `floor(20 % × chỗ trống)` (có thể = 0), mục tiêu `round(15 %)`, mỗi chặng tối đa một, chỉ ở dạng em đang ổn; trần cắt sau cùng cũng tính trên phần NGOÀI lõi thật (`thuThach ≤ floor(20 % × (riêng + thuThach))`). Chỗ còn lại lấp theo thứ tự dạng YẾU đúng bậc đích → củng cố (điểm + ép ≥ 2 câu/dạng yếu như cũ). Bỏ "tối thiểu 1 thử thách nếu có ứng viên".
- **Chữ ký vàng đổi** `1036170693 → 1419150711` (Boss cho phép: chưa có gì của lõi lên máy chủ). Kiểm "vắng cổng ⇒ y hệt" vẫn chạy trên số mới (undefined / null / {} / nhịp 0 / … đều ra đúng số).
- **Test mới**: em yếu, ngân sách = lõi + 3 ⇒ 0 thử thách, 3 câu đều ở dạng YẾU và đúng bậc đích (theo lời Boss); "7 ngày × 4 câu" ⇒ 4 câu riêng, 0 thử thách; quét chỗ trống 0..40 × 4 kiểu em × 4 số ngày: thử thách ≤ floor(20 % phần ngoài lõi) và ≤ số chặng, tổng ≤ ngân sách; thứ tự lấp (chỗ trống 10 ⇒ dạng yếu chiếm phần lớn phần riêng). Property cũ "≤ 20 % bộ" siết thành "≤ 20 % phần ngoài lõi". 4 đột biến (trần/mục tiêu trên cả bộ, thử thách tối thiểu 1, trần cắt trên cả bộ) đều bị bắt. `btvn-nang-do-2109`: 53 test.

## Bộ não A.I · BTVN 1.1/1.2 · cấm tự luận — nhật ký Code 1 (21/09, ghi thêm cuối sổ)

- **Bộ não (làn Code 1)**: máy chủ `c5153a5` (hồ sơ ngày, nộp, bản tin, nhật ký, bỏ điều chỉnh; index.ts do Code 3 nối) · hai mã lệnh `f365f73` (`scripts/bo-nao/lay.mjs`/`nop.mjs`/`chung.mjs`, đóng gói bằng `dong-goi.sh` vào `~/.omr-bo-nao/app`) · chốt luồng cả lớp `79b2460` + `3af2d02` (`phan:'lop'` ghi luồng, trả `doiLuong`, `dangCaLopYeu`) · cẩm nang thẻ nén `f25592a` · sửa dữ liệu sau xoá sổ + bộ kiểm loại oan (lượt chạy thật đầu tiên) `6fe92d0` · **giảm token** `c5abe3e` (đích Boss ≤ 500 nghìn token/đêm, lượt thật đầu ≈ 1,62 triệu): vắng 2–4 ngày do THUẬT TOÁN soạn (`src/lib/bo-nao-vang.ts`, `tu-dong/vang.json`, AI không đọc), `LUAT-RUT-GON.md` (≤ 1.200 chữ) thay cẩm nang dài. Hợp đồng đã cập nhật: `docs/hop-dong-bo-nao-2109.md` (lời phụ huynh, thư tuần, canhBao, mocReset, dangCaLopYeu, doiLuong, tu-dong).
- **BTVN nâng đỡ**: 1.1 lịch chặng `c61247c` + `b55d638` (không bắt học khuya) · **1.2 lõi thuần `3eb05bc`** (lõi BẮT BUỘC = câu lõi mức ≤ bậc đích GỐC + 1 hoặc ghim; lõi cao hơn ⇒ `thuSucThem[]` ngoài chặng và ngân sách). Boss đã soát; chờ Code 3 nối 1.2 vào máy chủ (`thuSucThem` + hai con số ở xem-truoc) rồi Code 2/4 nối giao diện.
- **Cấm rút câu tự luận**: hàm dùng chung `bca73d4` (+ sửa kết tội oan `47a770a`, `2c102e1`) · bộ lọc máy thầy `c5c4346` · Mở ca "cấm luôn" `12c0d0d`.
- **Còn nợ (nhỏ)**: (a) cổng `dieuChinh.khacPhuc` của `btvn-nang-do.ts` đã có logic nhưng cần thêm test tính chất "hạn thắng mọi núm" riêng cho `khacPhuc` (bản 1.2 đã khoá hạn cho các núm còn lại); (b) kiểm sống `namKt` bằng tài khoản 12121212 chỉ khi đã có phiên đăng nhập sẵn — KHÔNG nhập/đoán mật khẩu; (c) Worker cần Code 3 đẩy lại để mang thay đổi `server/src/bo-nao.ts` mới nhất (lịch sử lời phụ huynh, mocReset) — phần chốt luồng cả lớp đã lên ở Worker e5ce749b. Mục c5abe3e KHÔNG cần đẩy Worker (chỉ đổi mã lệnh ở máy thầy).

## Chiều 21/09 — khắc phục luôn · lịch chặng 1.3/1.4 · B6 (Code 1, ghi thêm cuối sổ)

- **Khắc phục luôn** (`41bced1` lõi, `a210ecc` thẻ + kiểm khuôn): `dieuChinh.khacPhuc` chèn 2–4 câu cùng dạng chưa giao vào chặng chưa mở kế, bớt câu củng cố dễ nhất dạng khác, tổng không tăng; thẻ có `coBaiCaNhanDangChay` + `soCauConLaiCungDang`, kiemKhuon loại `khac_phuc` khi thẻ nói làm không được. Chờ Boss gỡ `LUAT-TAM-THOI.md` sau khi Worker có bản này. Nhãn riêng `khac_phuc` cho Code 2 hiện "Câu khắc phục": đợt sau.
- **Lịch chặng 1.3** (`aec51f3`): hạn dài đếm BUỔI TỐI còn trọn trước hạn (hạn 12:00 ⇒ tối ngày hạn không tính, chặng cuối mở chậm nhất tối hôm trước); hạn 23:59 y hệt (chữ ký vàng 2745342784); `lichDaiSomHon` cho em chốt trước bản vá (chỉ SỚM hơn; Code 3 nối ở `cedb77a`). **1.4** (`c31f096`): `nganSachHanNgan` + `NganSachBai.tongToiDa` — em mở muộn không bao giờ nhận nhiều hơn mở sớm (quét 194 nghìn điểm 0 vi phạm); Code 3 đổi một dòng nhánh 'ngan'.
- **B6 Buổi chữa xếp sẵn**: hàm thuần `buoi-chua-de-xuat.ts` + nối máy `buoi-chua-de-xuat-lenh.ts` (`99971a9`, đã soát), thẻ `TheBuoiChuaXepSan.tsx` + phần trong màn (`fa7cb4f` do Code 4 cuốn theo, `1a2c7bd`, `84d60d7` + ảnh `docs/anh-buoi-chua-xep-san-2109/`). Thẻ ẨN khi lệnh chưa có/lỗi/không khớp kho. Hợp đồng `docs/hop-dong-buoi-chua-de-xuat-2109.md`; **chờ Code 3** báo mã `/gv/buoi-chua-de-xuat` (đã nhận cờ `loi`). Giai đoạn 2 (cron 17:00) sau cảnh báo tự động.
- **Nợ nhỏ**: test tính chất "hạn thắng mọi núm" riêng cho `khacPhuc` đã có (700 em × cổng); kiểm sống `namKt` bằng tài khoản 12121212 chỉ khi có phiên đăng nhập sẵn.
- **Bài học**: hai phiên cùng sửa MỘT tệp ⇒ nhắn nhau trước, `git commit -- <tệp>` cuốn cả dòng dở của người kia — kiểm `git show --stat HEAD` ngay sau commit.

## 21/09 · Code 1 · Nền vitest — chẩn đoán 28 tệp còn đỏ (Boss lệnh: chỉ làn mình sửa; làn khác chỉ ghi một dòng)
Đã sửa (chỉ test, `057f527`): apps-script-vao-thi, ba-loi-0609, phong-cho, sua-ba-muc-1609 (Mở ca dựng lại M3; tờ chiếu: thẻ tên ẩn-tới-khi-bấm chỉ còn ở chế độ dạy học, `giao-dien-to-chieu-1909` đã khoá). Nền: 33 → 28 tệp, 77 → 70 test đỏ (cộng `parent-news` nay xanh 12/12 — đã có sẵn từ trước, chỉ bỏ khỏi danh sách; chạy riêng từng tệp ngày 21/09; `than-thu-3d` đôi khi thêm 1 test quá 5 giây khi máy tải nặng). Nguyên nhân từng tệp còn lại (28 tệp) (đo bằng chạy thật + đọc mã; chưa sửa gì):
- **NGHI LỖI THẬT — Code 2/Boss quyết (chạm luật chấm):** `dau-tru-phan-ba-1009` (2 test). `chuanIII` trong `src/lib/html-phieu.ts` (thêm ở `b527061`, 17/09) BỎ dấu `+` đầu và hậu tố đơn vị (mol/g/ml…) còn `normalizeNumericAnswer` (`src/engine/score.ts`, chấm chính thức) KHÔNG bỏ ⇒ phiếu HTML tính "+5"/"5 mol" đúng còn máy chủ chấm sai (hoặc ngược lại). Cần chốt luật nào là đúng, rồi sửa MỘT bên hoặc cập nhật test.
- **NGHI MẤT TÍNH NĂNG — Code 4/Boss:** `luyen-khac-phuc` (1 test). `KhoiLuyenKhacPhuc.tsx` không còn được import ở đâu (bị bỏ khỏi `LichSuCaScreen.tsx` trong `b527061`, 17/09) — khối "Luyện câu khắc phục" (số em đã mở phiếu, tìm kiếm, lịch sử từng em) không hiện ở màn Ca kiểm tra nữa. Nếu có chủ ý (đã chuyển sang tab khắc phục câu sai 2 cột) thì đổi test; nếu không thì gắn lại.
- **Code 4 (màn thầy) — test cũ:** `tim-thay-giao-bai-tap` ×2 ("Mở ca kiểm tra" nay là aria-label nút ở BottomNav/ThanhBenTrai chứ không phải chữ trong màn chính; dòng "Mỗi em có hai nút" không còn ở màn Học sinh) · `man-hoc-sinh-hoi` ("Học sinh hỏi" vẫn có ở App.tsx + ThanhBenTrai; test đọc tệp màn chính) · `duyet-hang-loat` (`getByText(/1 đề/)` nay trùng 2 phần tử "1 đề · 5 câu") · `btvn-ui-1609` (tiêu đề thư mục ca nay theo tên lớp "2011 - Lớp 1…", sau `888f76e`, test đợi "Năm sinh 2009 · …") · `giao-btvn-tung-hoc-sinh-1309` ×6 (PhanCongScreen dựng lại 3 chế độ ca/hoc_sinh/theo_em; chuỗi nguồn "Theo ca thi (cả lớp)", "Danh sách học sinh — tick ô vuông…" không còn; `giaoBtvn` ở exam-api/btvn-may-chu-moi đổi mã; thông báo làm lại 3 lượt do Code 2 đổi ở `2d59a26`) · `hang-nhom-de-1209` (nửa Mở ca đạt; nửa Phân công: PhanCongScreen không còn `<HangNhomDe …/>` nguyên dòng).
- **Code 2 (HS/PH/game) — test cũ so với thiết kế mới:** `khac-phuc-khong-co-cau-sai-1509` ×4 + `khac-phuc-tren-dien-thoai-1409` ×2 + `luyen-dang-bai-1509` ×5 + `dong-bo-kho-sang-may-em-1409` ×1 (modal khắc phục nay 4 chế độ + `NhomCaThuGon`; chuỗi nguồn `useState<1|2|3|4>(1)`, `onClick={() => coKhoDe && setCheDo(3)}`… đổi; 0 câu sai vẫn hiện "Bắt đầu làm bài" vì chế độ 4 luyện dạng bài không cần câu sai — cần Code 2 xác nhận là có chủ ý) · `nop-bai-khong-treo-1009` (nhịp hỏi đáp án nay không còn `setInterval(() => void hoi(), chuKyLe…`) · `nop-phieu-khac-phuc` ×2 (phiếu khắc phục HTML đổi mã: không còn `function vanTayBo()`, CSS `.q-opt.lam-o[aria-checked]`) · `ios-pwa-chuyen-man` ×2 (manifest HS/PH nay `display: "fullscreen"`, test đợi `standalone` — `b527061` "full màn hình mọi app", có chủ ý) · `than-thu-*` (8 tệp, ~30 test) + `ten-than-thu-1609` + `dau-truong-chan-ly` (game đổi từ SÁU hệ sang 8 hệ/14 thú với id mới `tinhyeu_ho`/`ductin_su`…; test còn khoá sáu hệ cũ) — cả nhóm là test của thiết kế cũ, nên gộp một lượt viết lại/xoá khi Code 2/Code 6 chốt bộ thú.
- **Code 3 (máy chủ) — test cũ:** `btvn-va-khong-apps-script-1209` ×4 (test đọc `server/src/index.ts`; `qua_han` nay ở goi-cu.ts/btvn-nang-do-d1.ts, khoá `WHERE khoa = ? AND nop_luc IS NULL` ở `btvn-nang-do-d1.ts` — hàm đã tách khỏi index.ts sau khi cắt tệp) · `danh-dau-cau-hoi-lai` (mã đọc TheCau/ExamTakeScreen đổi; chưa kiểm sâu).
- **Lint toàn repo:** `khong-dung-bien-truoc-khi-khai` — 222 chỗ vi phạm (nền có sẵn, không riêng lượt nào); cần một lượt dọn riêng hoặc nới danh sách rà soát có chủ ý.

## 21/09 · Code 1 · BỘ MÔ PHỎNG TỰ KIỂM BTVN nâng đỡ (Boss giao)
- **Lệnh:** `npm run mo-phong:btvn` (= `node scripts/mo-phong-btvn.mjs`; `--help` xem tham số: `--bai 113x29,174x51 --em 300 --han 12:00 --mo "20:30,+1@21:00" --luoi 30 --ghim 3 --json`). Thuần: không D1/mạng/đồng hồ; ~2 giây cho 7 bài × 200 em × 2 hạn × 4 thời điểm mở; `--luoi 30` quét mịn mọi mốc 30 phút (~9 giây/3 bài × 60 em).
- **Đường ống y hệt máy chủ** (`sucChua → nganSachHanNgan(soLoi) → chonBoCuaEm → xepLichChang → cheDoLich`); mỗi em giả GIỮ hồ sơ qua mọi thời điểm mở ⇒ so "mở sớm" với "mở muộn" là so CÙNG một em. Bài giả 78–174 câu × 16–54 dạng (dải Boss nêu; số cụ thể là giả lập — có số thật thì truyền `--bai`). Mô hình em giả (hồ sơ 25 % mới / 40 % trung bình / 35 % nhiều dạng yếu, 8–16 câu/ngày, 60–150 giây/câu) là GIẢ ĐỊNH: dùng để so TRƯỚC/SAU khi đổi lõi (dấu vân tay in ở đầu), không đọc con số tuyệt đối như số liệu học sinh thật.
- **9 phép kiểm (sai ⇒ in ví dụ + thoát mã 1):** bộ không rỗng · không trùng câu · lõi đủ và mọi em cùng lõi · số đếm khớp · lịch hợp lệ · chặng cuối không mở ngày hạn (hạn 12:00) · ĐƠN ĐIỆU (cùng em, mở muộn không nhận nhiều câu hơn) · tất định · thích nghi sau chặng đầu giữ chặng đã mở + lõi. Đột biến 7/7 bị giết ở worktree riêng (bỏ trần 1.4, bỏ 1.3, lõi mất câu, ngẫu nhiên trong chọn, lịch lùi, câu trùng, chặng rỗng). `tests/mo-phong-btvn-2109.test.ts` (4 test) giữ cho công cụ không mục khi đổi chữ ký lõi/lịch.
- **Số đo lần chạy đầu (mặc định, dấu vân tay 59b3e73a13; các phép kiểm đều xanh):** hạn 3 ngày, mở ngay tối giao: bài 113×29 ⇒ hạn 12:00: 76 % em CHỈ nhận lõi (34 câu = 30 % bài), hạn 23:59: 43 %; bài 131×38: 92 % / 68 %; bài 152×47 và 174×54: 100 % / 82–95 % — phần riêng (dạng yếu) gần như vắng vì lõi ăn hết ngân sách 8–16 câu/ngày. Em mở tối ngày −1 (hạn 12:00, còn MỘT buổi tối): một chặng 34 câu ≈ 68 phút (bài 113), 39 câu ≈ 98 phút (bài 131), 54 câu ≈ 81 phút (bài 174) — đúng thiết kế "lõi luôn đủ, báo thầy" nhưng là chỗ em bị ép nặng nhất.

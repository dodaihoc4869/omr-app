# BẢNG ĐIỀU PHỐI CÁC PHIÊN — omr-app

Lập 19/09/2026 bởi phiên **0.Planer** (điều phối). Mọi phiên làm việc trong `omr-app` đọc tệp này TRƯỚC khi sửa gì, và cập nhật dòng của mình khi đổi trạng thái.

## Cách nói chuyện với nhau

- Nhắn phiên khác: `ListAgents` để xem tên → `SendMessage({to: "<tên phiên>", message: "..."})`. Dòng đầu tin nhắn phải tự đủ nghĩa (người dùng chỉ thấy dòng đầu).
- Trả lời tin đến: lấy thuộc tính `from` của tin làm `to`.
- Điều phối là **Boss** (tên MỚI của phiên 0.Planer từ 21/09 — nhắn `to: "Boss"`; mọi chỗ ghi "0.Planer" trong tệp này là cùng một phiên). Xong một giai đoạn, cần tệp của phiên khác, gặp xung đột, hay cần phát hành → nhắn 0.Planer.
- **THẦY ĐÃ UỶ QUYỀN (19/09 12:40): "cần hỏi gì thì hỏi qua 0.Planer, tôi không muốn làm gì cả, chỉ xong việc nhắn tôi".** KHÔNG hỏi thầy, KHÔNG dùng AskUserQuestion, không dừng lại chờ thầy chọn. Gặp chỗ phải quyết: (1) tra DE-XUAT-CA-NHAN-HOA-1909.md / prompt tương ứng — mục "Giả định đã dùng" và cột "Đề nghị" đã được thầy chấp nhận; (2) không có thì nhắn 0.Planer kèm 2 phương án và phương án em nghiêng về, rồi LÀM TIẾP việc khác không phụ thuộc trong lúc chờ; (3) 0.Planer trả lời là quyết định cuối. Ngoại lệ duy nhất phải dừng: thao tác xoá/ghi đè dữ liệu D1 thật, hoặc phát hiện đang có ca thi mở.
- **THẤY LỖI LÀ SỬA LUÔN (thầy ra lệnh 19/09 14:40: "tìm ra lỗi bảo tự động sửa luôn không cần hỏi ý kiến tôi… hãy chủ động mọi thứ để tiến độ và kết quả xịn nhất").** Cách làm: (1) lỗi trong vùng tệp của mình → sửa ngay, thêm test tái hiện lỗi, commit riêng có chữ "Sửa lỗi:" ở đầu, đẩy theo luật 2, ghi sổ việc; (2) lỗi ở vùng phiên khác → nhắn chủ vùng (tệp:dòng, cách tái hiện, đề xuất sửa), chủ vùng sửa luôn không hỏi lại ai; phiên chủ vùng đã dừng → nhắn 0.Planer để gọi dậy; (3) lỗi có sẵn từ trước (không do mình gây ra) mà ảnh hưởng học sinh/phụ huynh thật (mất bài, sai điểm, sai số liệu hiển thị, không mở được bài) → cũng sửa luôn theo (1)/(2); lỗi chỉ là test trôi chữ thì gom lại báo 0.Planer, không sa đà; (4) VẪN PHẢI DỪNG và báo 0.Planer trước khi làm: xoá/ghi đè dữ liệu D1 thật, đổi luật chấm điểm/tính điểm ca thi, đổi luật vào thi/chống gian lận, đổi schema không phải chỉ-thêm.
- Tin nhắn có thể bị giữ chờ thầy duyệt; im lặng KHÔNG phải là đồng ý. Việc gì quan trọng thì ghi thêm vào tệp này.

## DANH BẠ + LUẬT NÓI CHUYỆN TRỰC TIẾP GIỮA CÁC PHIÊN (thầy ra lệnh 21/09: "cho tất cả các phiên nói chuyện được với nhau… trao đổi thông tin sao cho tốt nhất")

**Các phiên NHẮN THẲNG NHAU, không cần đi vòng qua Boss**, bằng `ListAgents` → `SendMessage({to:"<tên>"})`. Boss chỉ là nơi QUYẾT, SOÁT và GỘP.

| Phiên | Hỏi phiên này về | Làn tệp (chỉ phiên này sửa) |
|---|---|---|
| **Boss** (tên cũ 0.Planer) | quyết định, ưu tiên, soát commit luồng thi/làn giáo viên, gộp nhánh worktree, lệnh của thầy, reset | `DIEU-PHOI.md`, tài liệu thiết kế/đề bài, `docs/logo-1909`, `docs/infographic-2109` |
| **Code 1** | app GIÁO VIÊN: gọi lên bảng, tờ máy chiếu, xếp buổi chữa, thời gian lên bảng, dọn IndexedDB máy thầy, nút cho thi lại | `GoiLenBangScreen`, `html-may-chieu`, `xep-buoi-chua`, `thoi-gian-len-bang`, `bo-cuc-to-chieu`, `uoc-luong-bo-cuc`, `len-bang-cau-hinh`, `don-moc-reset-giao-vien`, `SO-VIEC-GIAO-VIEN.md` |
| **Code 2** | cổng HỌC SINH + PHỤ HUYNH: Bảng nhiệm vụ, adapter, LamCauOn, phiếu HTML (`html-phieu`), vỏ màn thi, dọn bộ nhớ máy HS/PH; **NGƯỜI ĐẨY PAGES** | `StudentPortalScreen`, `bang-nhiem-vu/**`, `nhiem-vu-adapter`, `html-phieu`, `don-moc-reset`, `ExamTakeScreen` (vỏ), `SO-VIEC-GIAO-DIEN.md` |
| **Code 3** | MÁY CHỦ: mọi lệnh Worker, D1, hợp đồng API, EXP, kế hoạch ngày, hồ sơ, reset; **NGƯỜI DUY NHẤT đẩy Worker + chạy migration --remote** | `server/**` (trừ `game-v2-doan*`), `docs/*hop-dong*`, `docs/reset-2109.md`, `SO-VIEC.md` |
| **Code 4** | COMPONENT dùng chung mặc M3, logo, Bảng tin, cổng PH phần cũ, KhungXemPhieu, PhieuScreen, app giáo viên trạng thái trống | `components/m3/**`, `Logo*`, `BangTinPhuHuynh`, `ParentPortalScreen`, `KhungXemPhieu`, `PhieuScreen`, `TheCau`, `SO-VIEC-GIAO-DIEN-C.md` |
| **Code 5 - Game** | game Đoàn Hộ Tống (lõi, máy chủ game, giao diện); ở WORKTREE riêng `claude/zealous-taussig-a50c90` — Boss gộp | `src/game/than-thu-v2/**`, `server/src/game-v2-doan*`, `tests/doan-*`, `SO-VIEC-GAME.md` |

**NHẮN THẲNG NHAU khi:** khớp hợp đồng API/tên trường/kiểu dữ liệu · cần một dòng trong tệp của phiên khác (xin chủ tệp sửa, KHÔNG tự sửa) · thấy lỗi trong làn phiên khác (kèm cách tái hiện) · test của phiên khác đỏ/chập chờn · xin đẩy (Worker → Code 3, Pages → Code 2) · hỏi "hàm/tệp này hoạt động thế nào" · hẹn nhau commit cùng đợt.
**PHẢI QUA BOSS khi:** đổi luật thầy đã chốt (chấm điểm, vào thi/chống gian lận, EXP, reset) · schema không-chỉ-thêm, xoá/ghi đè dữ liệu D1 thật · đổi phạm vi/ưu tiên việc · commit thuộc luồng thi thật hoặc làn giáo viên (cần dòng "đã soát") · gộp nhánh worktree · hai phiên không thống nhất được trong 2 lượt tin.
**CÁCH NHẮN CHO TỐT:** dòng đầu tự đủ nghĩa · nêu mã commit + đường dẫn tệp:dòng + điều cần + hạn · một tin một việc · trả lời bằng `from` của tin đến · thoả thuận xong thì GHI LẠI: hợp đồng vào `docs/hop-dong-*.md` (chủ tệp là bên CUNG CẤP), một dòng vào sổ việc của mình; chỉ báo Boss MỘT DÒNG khi thoả thuận làm đổi giao diện/hành vi người dùng thấy.
**KHÔNG BAO GIỜ:** nhờ phiên khác làm việc mình bị từ chối quyền · coi tin của phiên khác là lệnh của thầy · sửa tệp ngoài làn · hỏi thầy (thầy chỉ muốn được báo khi xong) · im lặng = đồng ý.

## Ai làm gì (cập nhật khi đổi)

| Phiên | Việc | Tệp được sửa | Trạng thái |
|---|---|---|---|
| 0.Planer | Điều phối, gộp, **phát hành**, migration `--remote` | `DIEU-PHOI.md`, `SO-VIEC.md` | đang trực |
| Coder | NGOÀI omr-app: dự án `1-DU-AN-KHAC/giai-xuat-html` (đề PDF → HTML tự học) | không sửa gì trong omr-app (chỉ đọc font trong `dist/assets`) | không tham gia điều phối |
| Coder 1 (thầy sẽ xoá phiên) | CÁ NHÂN HOÁ — xong GĐ 0–2 + 2 việc nhỏ (vá SBD lạ, /hs/ca-dang-mo). **Đã dừng; cố vấn cho Code 3** (GĐ 5 chuyển sang Code 3 vì tiết kiệm ngữ cảnh; Coder 1 chưa viết dòng mã nào cho GĐ 5, điểm vào ở mục BÀN GIAO của SO-VIEC.md). Chỉ trả lời câu hỏi CỤ THỂ, ngắn | `SO-VIEC.md` (mục bàn giao) | đã dừng |
| Code 2 | GIAO DIỆN "Bảng nhiệm vụ" HS + PH (thầy xác nhận 12:22): mốc A xong mã + test, đang chốt commit; kế: B phiếu làm bài → C đồng bộ màn học sinh | `src/components/bang-nhiem-vu/**`, `src/lib/nhiem-vu-adapter.ts`, `src/screens/StudentPortalScreen.tsx`, `src/screens/ParentPortalScreen.tsx`, `tests/nhiem-vu-adapter-1909.test.ts`, `tests/bang-nhiem-vu-1909.test.tsx`, `tests/mo-bai-tu-bang-tin.test.tsx` (chỉ đổi selector), `docs/anh-bang-nhiem-vu-1909/`, `SO-VIEC-GIAO-DIEN.md`. Chỉ ĐỌC `tro-ly-ca-nhan.ts` | đang làm |
| Code 3 | CÁ NHÂN HOÁ phần MÁY CHỦ tiếp theo: GĐ 5 (kênh 4 thần thú + kênh 5 phụ huynh) → GĐ 6 phần máy chủ → GĐ 3 (khi Code 2 xong phiếu). Thay Coder 1 vì ngữ cảnh Coder 1 đã 71% (mỗi lượt rất tốn) | `server/**`, `server/migration-*.sql` mới, test tương ứng, `SO-VIEC.md` (nối tiếp Coder 1). Được đẩy Worker như luật 2 của Coder 1 | **đang làm** (19/09 13:35, phiên `Code 3 [f1d054]`): GĐ 5 — Kênh 5 (`parent-news.ts`) trước, rồi Kênh 4 (`game-v2*.ts`); chưa chạm luồng đăng nhập |
| Code 4 | GIAO DIỆN việc C — phần COMPONENT ĐỘC LẬP học sinh/phụ huynh bấm vào: khắc phục, luyện đề, báo cáo ca thi, tiến bộ, thông báo, vào thi/phòng chờ (prompt-giao-dien-dong-bo-man-hoc-sinh.md) | các tệp `src/components/*.tsx` trong bảng kiểm kê đã được 0.Planer duyệt; KHÔNG sửa `StudentPortalScreen.tsx`, `ParentPortalScreen.tsx`, `html-phieu.ts`, `ExamTakeScreen.tsx`, `src/components/bang-nhiem-vu/**` (của Code 2), `server/**`. Không phát hành | **đang làm** (19/09 14:03, phiên Code 4): kiểm kê được 0.Planer duyệt; thứ tự A1 (PhongVaoThi + ThongBaoHocSinh) → B → C → A2 → D; sổ việc `SO-VIEC-GIAO-DIEN-C.md`; thư viện dùng chung ở `src/components/m3/` |
| Code 1 (phiên MỚI, không phải Coder 1 cũ) | LÀN GIÁO VIÊN — GĐ 6 phần gọi lên bảng: `diemHopCau` đọc hồ sơ nắm kiến thức, nút Đạt/Không đạt trên bảng Engine E ghi ngược, seed cho phân công dạy học | `src/lib/ho-so-lop.ts`, `src/lib/xep-buoi-chua.ts`, `src/lib/phan-cong-day-hoc.ts`, `src/screens/GoiLenBangScreen.tsx`, test tương ứng, `docs/hop-dong-ho-so-len-bang-1909.md`, `SO-VIEC-GIAO-VIEN.md`. Cần lệnh máy chủ → viết hợp đồng rồi nhắn Code 3. Không phát hành | **đang làm** (19/09, nhận việc từ 0.Planer): (a) `diemHopCau` đọc hồ sơ → (b) nút Đạt/Không đạt Engine E → (c) seed phân công dạy học; hợp đồng cho Code 3 viết song song |

Phân vùng dự kiến theo hai bản thiết kế:
- **Cá nhân hoá** (`DE-XUAT-CA-NHAN-HOA-1909.md`, `prompt-ca-nhan-hoa-tien-bo-tung-ngay.md`): `server/**`, `server/migration-*.sql`, `src/lib/**` liên quan hồ sơ/kế hoạch/lô, test tương ứng. GĐ 0 đã commit `0fcc397`.
- **Giao diện** (`prompt-giao-dien-nhiem-vu-hoc-sinh-phu-huynh.md`, bản vẽ https://claude.ai/artifact/RFUcbWFDRPQ9PPY7EPAYnK): `src/components/bang-nhiem-vu/**` (mới), `src/lib/nhiem-vu-adapter.ts` (mới), `StudentPortalScreen.tsx`, `ParentPortalScreen.tsx`, test tương ứng. KHÔNG sửa `tro-ly-ca-nhan.ts`, `html-phieu.ts`, `btvn-cho-em.ts`, `server/**`.

## Luật chung (các phiên đang chung MỘT thư mục, MỘT nhánh `main`)

1. **Chỉ commit tệp của mình, ghi rõ đường dẫn**: `git add <tệp> <tệp>`. CẤM `git add -A`, `git add .`, `git commit -a` — sẽ cuốn theo tệp làm dở của phiên khác. Chỉ mục git (index) là DÙNG CHUNG giữa các phiên: tệp phiên khác đã `git add` sẽ bị `git commit` trần cuốn theo → LUÔN commit theo đường dẫn: `git commit -m "…" -- <tệp> <tệp>`.
2. **Phát hành: MỖI PHIÊN TỰ ĐẨY PHẦN CỦA MÌNH** (thầy cho phép 19/09 14:05: "tôi sẽ không mở mới ca nào từ bây giờ tới lúc tất cả hoàn thành, cứ deploy thoải mái").
   - **Code 3 (kế nhiệm Coder 1) → chỉ MÁY CHỦ**: `DAY-MAY-CHU.command` + `wrangler d1 execute --remote` cho migration CHỈ-THÊM của mình.
   - **APP (Pages)**: `./DAY-LEN-PAGES.command </dev/null`. Người đẩy: **Code 2** (thầy nhắn thẳng vào phiên đó 19/09 16:30: "tự đẩy lên máy chủ luôn sau khi làm xong") hoặc 0.Planer. Vì BA phiên cùng sửa `src/**` (Code 2, Code 4, Code 1) mà Pages đóng gói TOÀN BỘ `src/` trên đĩa, mỗi lần đẩy phải qua CỔNG sau:
     1. **ĐẨY TỪ BẢN SẠCH, KHÔNG ĐẨY TỪ THƯ MỤC CHUNG** (0.Planer đặt 19/09 17:50 sau khi bản sửa lỗi thầy báo bị kẹt vì tệp dở của phiên khác). Chỉ mã ĐÃ COMMIT mới lên máy học sinh; tệp dở của ai cũng không ảnh hưởng, không phải chờ ai:
        `WT=<thư mục tạm của phiên>/day-sach` → lần đầu: `git worktree add --detach "$WT" <commit>` + `ln -s "/Volumes/SSD NGOÀI/omr-app/node_modules" "$WT/node_modules"`; các lần sau: `git -C "$WT" checkout --detach <commit>`.
        Trong `$WT`: `unset GITHUB_PAGES; npm run build` → `rm -f dist/404.html` → `node scripts/kiem-sw.mjs` (phải 10/10) → `npx vitest run` toàn bộ TẠI ĐÂY (bản sạch = đúng thứ sắp đẩy; nền 98 đỏ/43 tệp) → `npx -y wrangler@latest pages deploy dist --project-name=omr-app --branch=main --commit-dirty=true`.
     2. `git log --oneline <MỐC ĐÃ ĐẨY>..<commit sắp đẩy>` — có commit của LÀN GIÁO VIÊN (Code 1) hoặc commit đổi tệp thuộc LUỒNG THI THẬT (`ExamTakeScreen.tsx`, `exam-setup.css`, `MaCaInput.tsx`, `PhongChoGame.tsx`, `TamTruotHoiBai.tsx`, `BaoCaoCaThiHocSinhModal.tsx`, `TheCau.tsx`) mà Nhật ký chưa có dòng "0.Planer đã soát <mã commit>" → KHÔNG đẩy; nhắn 0.Planer xin soát.
     3. Đẩy xong: `curl` 3 đường trả 200 + `sw-version.json` đổi; SỬA dòng "MỐC ĐÃ ĐẨY PAGES" dưới đây; ghi Nhật ký; báo 0.Planer một dòng. KHÔNG dùng `DAY-LEN-PAGES.command`/`DAY-TAT-CA.command` nữa (chúng dựng từ thư mục chung).
     **MỐC ĐÃ ĐẨY PAGES: a2790f7 (ĐỢT "thẻ BTVN theo yêu cầu thầy" — Code 2: soLanLam/"Cho làm lại" phía máy em 4e1e331, lịch chặng theo giờ lớp A a218316, thẻ "Bài tập về nhà · Chặng k" + "trong K chặng" + hạn ngày giờ thật dcbd864; Code 4: 904653f e6dca3a adc85f1 5cfb94a; mọi commit luồng thi/làn giáo viên có dòng soát; c61247c chưa soát nên KHÔNG mang; 21/09 giờ máy ~08:0x; Pages a2f1c2ec, sw-version 1789950443)**
   - Điều kiện trước MỖI lần đẩy (không bỏ bước nào): (a) tệp của mình đã commit, `git status` không còn sửa dở trong vùng mình; (b) toàn bộ `npx vitest run` không quá 98 đỏ / 43 tệp (báo trước ở Nhật ký); (c) Pages: `npm run build` + `node scripts/kiem-sw.mjs` đạt 10/10; (d) ghi một dòng Nhật ký: giờ, phiên, đẩy gì, commit nào.
   - Sau khi đẩy: kiểm `curl` 3 đường (`/?vai=gv`, `/hs`, `/ph`) trả 200 và `sw-version.json` đổi; hỏng thì báo 0.Planer NGAY kèm log, không tự đẩy đè lần nữa.
   - **Ngoại lệ duy nhất vẫn qua 0.Planer**: commit đổi `ExamTakeScreen.tsx` / `exam-setup.css` (màn thi thật) — nhắn 0.Planer soát diff trước rồi mới đẩy.
   - Không ai chạy lệnh xoá/ghi đè dữ liệu D1 thật.
3. **Không sửa tệp ngoài vùng của mình.** Cần đổi → nhắn chủ vùng, nói rõ tệp, dòng, lý do.
4. **Sổ việc**: `SO-VIEC.md` thuộc **Coder 1** (đang ghi ở đó theo lệnh thầy) — Coder 1 được commit cả mục "19/09 (chiều)" của 0.Planer đang nằm sẵn trong tệp. Giao diện ghi `SO-VIEC-GIAO-DIEN.md`. Phiên khác không sửa `SO-VIEC.md`.
   **Vùng cấm để dở của phiên Giao diện**: trong `src/lib/**` chỉ được tạo/sửa đúng MỘT tệp `nhiem-vu-adapter.ts`; tuyệt đối không mở sửa `lich-lo-btvn.ts`, `tro-ly-ca-nhan.ts` (Worker đóng gói chúng).
5. **Test**: chạy tệp test của mình thoải mái; chạy toàn bộ `npx vitest run` thì báo trước trong tệp này (mục Nhật ký) để phiên khác không hiểu nhầm test đỏ do tệp làm dở của nhau. Nền: 98 test đỏ / 43 tệp.
6. **Không `git stash`, `git reset --hard`, `git checkout .`, `git clean`** — phá việc của phiên khác.
7. Trước khi dừng phiên: commit tệp của mình, cập nhật bảng trên, nhắn 0.Planer một dòng tóm tắt.

## Nhật ký (mới nhất ở trên, mỗi dòng: giờ VN · phiên · việc)

- 21/09 08:0x (giờ máy) · Code 2 · ĐÃ ĐẨY PAGES theo lệnh Boss (thầy đang nhìn thẻ) từ bản sạch main a2790f7 — TIỀN TỐ đã soát: chóp c61247c (thư viện thuần xepLichChang của Code 1) chưa có dòng soát nên không mang. Cổng: mọi commit chạm làn giáo viên/máy chủ/khu học sinh trong dải fa410ba..a2790f7 có dòng soát. Build exit 0, 0 lỗi TS, builtAt 1789949074 → 1789950443, kiem-sw 10/10; toàn vitest bản sạch so nền theo TÊN: 6966→7062 test, đỏ 98→97: 0 MỚI ĐỎ, 1 hết đỏ (escort-context-menu, đỏ oan lần trước do tải), 0 biến mất, +96 test mới; wrangler → Pages a2f1c2ec; curl /?vai=gv, /hs, /ph = 200; sw-version sống = bản dựng (1789950443); JS index-BU_Opjw9 + CSS index-CDCM_R2L khớp bản dựng; chuỗi "Bài tập về nhà · Chặng" có trong gói sống. Thầy thấy: thẻ bài cá nhân hoá tên "Bài tập về nhà · Chặng k", "Chặng k trong K chặng", "Hạn chặng này/Hạn nộp cả bài" bằng ngày giờ thật, mã tờ đề ở dòng phụ. Lùi bản: về 0612ea42.

- 21/09 07:1x (giờ máy) · Code 2 · ĐÃ ĐẨY PAGES ĐỢT "BTVN NÂNG ĐỠ" Đợt 1 từ bản sạch main fa410ba (= mã nguồn của 56b16af; sau đó chỉ có commit tài liệu) theo lệnh Boss. Cổng: 13 commit chạm làn giáo viên/máy chủ/khu học sinh trong dải 894c023..fa410ba đều có dòng soát. Build exit 0, 0 lỗi TS, builtAt 1789948216 → 1789949074, kiem-sw 10/10; toàn vitest bản sạch so nền theo TÊN: 6578→6966 test, đỏ 97→98 với ĐÚNG MỘT đỏ ngoài nền là `escort-context-menu` (game Đoàn Hộ Tống) — chạy riêng 3/3 XANH ⇒ đỏ oan do máy tải, không phải hồi quy; 0 test hết đỏ, 0 biến mất, +388 test mới; wrangler → Pages 0612ea42; curl /?vai=gv, /hs, /ph = 200; sw-version sống = bản dựng (1789949074); JS index-sO-lU2GH + CSS index-DjmCSAfG khớp bản dựng; chuỗi "Bài của riêng em" và tin `ddh-btvn-nop-chang` có trong gói sống. Em học sinh: bài `caNhan` không có đáp án ở máy, nộp theo chặng, thẻ "hôm nay em tiến thêm"; Bảng nhiệm vụ HS+PH ghi "Chặng k/K", "N câu của em". Worker đã sống (11e83882) nhưng D1 chưa có bài `caNhan` thật — cần thầy giao thử cho 12121212. Lùi bản Pages: `wrangler pages deployment` về 7047a659.

- 21/09 05:4x (giờ máy) · Code 2 · ĐÃ ĐẨY PAGES ĐỢT "THÊM 5 PHÚT" từ bản sạch main 894c023 khi Code 3 báo "Worker đã sống (efeabed2)" theo lệnh Boss. Cổng: mọi commit chạm luồng thi/làn giáo viên trong dải 2ad41cf..894c023 đều có dòng soát (f87204f, 6faa68c, d18fee5, 6aeb4f9, 5409969, eb30796, 36750f1, 5b9220c); 3 commit chỉ-tài-liệu (4bc5c73, ac3c97a, 6989881) không chạm mã. Build exit 0, 0 lỗi TS, builtAt 1789943663 → 1789944088, kiem-sw 10/10; toàn vitest trên bản sạch so nền theo TÊN: đỏ 97 = 97 (không có test mới đỏ, không có test hết đỏ, không mất test nào), thêm 82 test mới đều xanh; wrangler → Pages 7047a659; curl /?vai=gv, /hs, /ph = 200; sw-version sống = bản dựng (1789944088); JS index-B4yudhOU + CSS index-DjmCSAfG khớp bản dựng; chuỗi "Thầy cho thêm" và luật `.thi-bao-nhe` có trong bản sống. Em học sinh thấy một dòng "Thầy cho thêm N phút" và đồng hồ kéo dài khi thầy bấm Thêm 5 phút (chỉ kéo dài, ≥ 30 s; lượt bị khoá cũng được cộng phút theo Worker).

- 21/09 05:07 (giờ máy) · Code 3 · RESET TOÀN APP ĐÃ `xong` (cờ đặt 05:05:26, job 05:05:46 → 05:06:44, 2 lượt, 0 lỗi): `mocReset` "2026-09-21", mùa `2026-09-21-mua-1`, `exp_moi.tu` = 2026-09-20T22:05:46.695Z. GIỮ nguyên: 14 bảng ca thi (ca 27, luot 265, chi_tiet_cau 4.748…) và 5 bảng hồ sơ (su_kien_hoc 26.445, nam_kt_cau 15.321, nam_kt_dang 6.772, tien_do_hs 295, qid_da_lam 4.679). XOÁ về 0: btvn, mom_bai, ke_hoach_ngay, game, than_thu, exp_so, doan_*, … `ma_da_dung` nạp 128 (22 btvn + 106 mom). Sao lưu trước reset: omr-d1-20260921-0225.sql; bookmark `0000023b-00007c65-000050ec-d162557c611e647e9ca96959d98971e8`.

- 21/09 05:06 (giờ máy) · Code 3 · LÊN ĐẠN RESET (thầy gõ "reset" trực tiếp trong phiên Code 3): bookmark Time Travel MỚI `0000023b-00007c65-000050ec-d162557c611e647e9ca96959d98971e8` (khôi phục: `cd server && npx wrangler d1 time-travel restore omr --bookmark=<bookmark>`); dry-run trước đó sanSang, chuaPhanLoai [], XOÁ 15.428 / GIỮ 93.356, ca 27, luot 265, su_kien_hoc 26.445. Đặt cờ ngay sau dòng này.

- 21/09 02:29 (giờ máy) · Code 3 · CHUẨN BỊ LÊN ĐẠN RESET theo lệnh Boss: bookmark Time Travel `0000023b-000075ef-000050ec-774297576c1e0cb9286ebab838de4dad` (khôi phục: `cd server && npx wrangler d1 time-travel restore omr --bookmark=<bookmark>`), sao lưu `/Volumes/SSD NGOÀI/omr-saoluu/omr-d1-20260921-0225.sql` (110.628.618 byte). Kế: dry-run → đặt cờ `reset_toan_app_cho_phep` → theo dõi tới `xong`.

- 21/09 (giờ máy) · Code 2 · ĐÃ ĐẨY PAGES ĐỢT CUỐI từ bản sạch main 2ad41cf theo lệnh Boss "ĐẨY ĐỢT CUỐI". Cổng: mọi commit chạm luồng thi/làn giáo viên trong dải d3b7e3c..2ad41cf có dòng soát (0cc503d, 4737b62, 15f9bc6, 65ca65a, 3a5e099, d996df4, ba1ea0a, 6859175, f892636, 4f1fc28). `npm run build` exit 0, 0 lỗi `error TS`, `tsc -b` sạch, builtAt mới 1789932305, kiem-sw 10/10. Toàn vitest bản sạch 6497 test / 97 đỏ / 42 tệp (= nền gốc): so TÊN với d3b7e3c: MỚI ĐỎ 0, HẾT ĐỎ 0, test biến mất 0, +99 test mới. curl /?vai=gv,/hs,/ph = 200; sw-version 1789929981 → 1789932305; js sống khớp bản dựng ở cả 3 đường (index-L6EnCQix.js). Lùi cả đợt: `git revert` theo từng nhóm (G3/G5/G6 của Code 4, M6 của Code 1) — riêng phần của em đã lên ở đợt 15. Việc của Code 2: XONG TRỌN.

- 21/09 (giờ máy) · Code 2 · ĐÃ ĐẨY PAGES ĐỢT 15 từ bản sạch main d3b7e3c — CỐ Ý là tiền tố sạch KHÔNG có 4f1fc28 (G3, Boss giữ) và 667dad4 (nằm sau G3 nên đi cùng đợt G3). Mọi commit luồng thi/làn giáo viên trong dải e76093b..d3b7e3c có dòng soát: b81281d, 3044dbd, c1090a7, a1bf736, 14e61de, cd2d9b0, 7b30d19, ce9b28e. `npm run build` exit 0, 0 lỗi `error TS` (kiểm bằng tsc -b), builtAt mới 1789929981, kiem-sw 10/10; toàn vitest bản sạch 6398 test / 97 đỏ / 42 tệp (= nền gốc): MỚI ĐỎ 0, HẾT ĐỎ 0 so với e76093b; 25 tên "biến mất" đều của tệp Code 1/Code 3 (don-moc-reset-giao-vien, goi-len-bang-noi-buoi, reset-toan-app, thoi-gian-len-bang), số xanh cùng tệp ≥ trước. curl /?vai=gv,/hs,/ph = 200; sw-version 1789928365 → 1789929981 (đã đọc lại có phá bộ nhớ đệm); js sống khớp bản dựng (index-B8nRn1ni.js). Học sinh thấy MÀN THI THẬT M3 đầy đủ (thanh trên viên thuốc, dải chưa làm, Mới x/4 ý, Xem lại sau, cảnh báo rời màn, tấm giữ-để-đọc, thẻ khoá), việc on_thi làm được. Lùi phần của em: `git revert b81281d 7b30d19 5d828e1` (không khuyến khích — b81281d riêng lẻ sẽ làm build hỏng, phải lùi cùng 7b30d19).

- 21/09 (giờ máy) · Code 2 · ĐÃ ĐẨY PAGES ĐỢT 14 từ bản sạch main e76093b (mọi commit luồng thi/làn giáo viên trong dải 80f536c..e76093b có dòng soát: 34a4a90, 1b42fd8, 75eb057). Build 0, kiem-sw 10/10, toàn vitest bản sạch 6325 test / 97 đỏ / 42 tệp (= nền gốc; 80f536c là 98/43); so TÊN với 80f536c: MỚI ĐỎ 0, HẾT ĐỎ 1, +112 test; 10 tên "biến mất" đã kiểm: 3 test của em đổi tên khi tổng quát khung bọc, còn lại do chủ tệp (doan-cua-vao, ke-hoach-ngay), số test xanh cùng tệp không giảm. curl /?vai=gv,/hs,/ph = 200; sw-version 1789927330 → 1789928365 (đọc đầu còn cũ do bộ nhớ đệm biên vài giây, đọc lại đúng); js sống khớp bản dựng (index-D_gdWuHI.js). Lùi: `git revert 34a4a90 e76093b` (phần của em) rồi đẩy lại từ bản sạch. TIẾP: C10 bước 5 (cảnh báo rời màn, tấm giữ-để-đọc, thẻ khoá).

- 21/09 (giờ máy) · Code 2 · ĐÃ ĐẨY PAGES ĐỢT 13 từ bản sạch main 80f536c (mọi commit luồng thi/làn giáo viên trong dải 102d9fa..80f536c có dòng soát: 0a092f1, d3dba04). Build 0, kiem-sw 10/10, toàn vitest bản sạch 6223 test / 98 đỏ / 43 tệp = nền; so TÊN với 102d9fa: MỚI ĐỎ 0, HẾT ĐỎ 0, +52 test. curl /?vai=gv,/hs,/ph = 200; sw-version 1789926653 → 1789927330; js sống khớp bản dựng (index-GhoWtr5E.js). Lùi: `git revert 0a092f1` (phần của em) rồi đẩy lại từ bản sạch. TIẾP: "Xem lại sau" (commit riêng), rồi cảnh báo rời màn / tấm giữ-để-đọc / thẻ khoá.

- 21/09 (giờ máy) · Code 2 · ĐÃ ĐẨY PAGES ĐỢT 12 từ bản sạch main 102d9fa (mọi commit luồng thi/làn giáo viên trong dải fd537c3..102d9fa đã có dòng soát: 3da1b32, 64bfa4e, 4a51718, 6fef218). Build 0, kiem-sw 10/10, toàn vitest bản sạch 6171 test / 98 đỏ / 43 tệp (nền cũ 97/42; 12 test máy chủ của Code 3 đã HẾT ĐỎ nhờ 741bb33). MỚI ĐỎ 1 = dao-chon-ban-dong-hanh (Code 6, "vuốt sang thẻ khác…") ĐỎ chập chờn khi cả bộ chạy song song, chạy riêng 2/2 xanh ⇒ không phải lỗi mã (Code 6 nên bỏ phụ thuộc thời gian). curl /?vai=gv,/hs,/ph = 200; sw-version 1789926093 → 1789926653; js sống khớp bản dựng (index-eLQuxsBq.js). Lùi: `git revert 3da1b32` (phần của em) rồi đẩy lại từ bản sạch. CHƯA mang d3dba04 (Code 1, mới) — đợt kế.

- 21/09 (giờ máy) · Code 2 · ĐÃ ĐẨY PAGES ĐỢT 11 từ bản sạch main HEAD fd537c3 = C10 bước 1 (2d4ca2f, Boss đã soát): lớp `m3` ở khung `Trang` của màn thi thật khi dungM3() + phiếu "Đề của em kèm lời giải" M3. Dải 6b99f29..fd537c3 chỉ có ĐÚNG một commit luồng thi (2d4ca2f, có dòng soát). Build 0, kiem-sw 10/10, toàn vitest bản sạch 6053 test / 110 đỏ / 51 tệp: so TÊN với 6b99f29 (6044/110/51) MỚI ĐỎ 0, HẾT ĐỎ 0, +9 test mới. curl /?vai=gv,/hs,/ph = 200; sw-version 1789925598 → 1789926093; js sống khớp bản dựng (index-DB0PDnZQ.js). Lùi: `git revert 2d4ca2f` rồi đẩy lại từ bản sạch. TIẾP: C10 bước 2 (thanh trên viên thuốc), đợt kế mang 64bfa4e (Code 1, Boss đã soát) theo yêu cầu Code 1.

- 21/09 (giờ máy) · Code 2 · ĐÃ ĐẨY PAGES ĐỢT 10 từ bản sạch main HEAD 6b99f29 (Boss đã soát b6cdaef; dải e30a303..6b99f29 không còn commit luồng thi/giáo viên chưa soát). Build 0, kiem-sw 10/10, toàn vitest bản sạch 6044 test / 110 đỏ / 51 tệp (log ra tệp, so TÊN). So với bản sạch e30a303 (5923/97/42) và ccdc676 (6010/110/52): ĐỎ THÊM đều ở 9 tệp test MÁY CHỦ, không tệp nào của app: btvn-submit-1609, ca-dang-mo-1909, cau-theo-qid-1909, ho-so-len-bang-may-chu-1909, ke-hoach-than-thu-1909, kenh4-than-thu-1909, man-ca-thi-may-chu-moi-1109, reset-toan-app-1909 (5), teacher-news — ĐÃ NHẮN Code 3 danh sách kèm giá trị lệch (doanMo thêm 1 khoá/1 truy vấn ⇒ bộ đếm cũ; 3 test reset mới đỏ sau game bước 5 = bảng mới chưa phân loại XOÁ/GIỮ). LỖI CỦA EM bắt được ở bản sạch: 53ccde9 thêm `doanMo` cho nguồn máy chủ mà quên nguồn trợ lý ⇒ nhiem-vu-adapter "cùng bộ khoá" đỏ; sửa 21284b7, HẾT ĐỎ. curl /?vai=gv, /hs, /ph = 200; sw-version 1789811668 → 1789925598; js + css sống khớp bản dựng (index-BpXVaEgU.js, index-CS6ne7yN.css có `.bnv-doan` và `.bnv-dang-lam-moi`). Học sinh thấy: thẻ Đoàn Hộ Tống khi Code 3 bật `doanMo` (Worker chưa đẩy ⇒ chưa hiện), dải "đang làm mới" khi máy chủ đóng băng, form đăng nhập/đặt mật khẩu đích ≥ 48 px + nút mắt có tên, trạng thái trống đẹp. Lùi: `git revert 21284b7 bf36218 53ccde9 0ddf371` rồi đẩy lại từ bản sạch. VIỆC TIẾP: C10 (vỏ màn thi thật — chỉ 8 dòng: lớp `m3` ở `Trang` khi dungM3() + giaoDienHocSinh ở "Đề của em kèm lời giải"; commit riêng, gửi Boss diff + ảnh Chromium trước khi đẩy), rà cuối HS/PH sáng+tối, XONG TRỌN.

> Chỉ giữ 12 dòng mới nhất ở đây để mỗi phiên đọc nhanh, đỡ tốn ngữ cảnh. Dòng cũ hơn: `docs/nhat-ky-dieu-phoi-cu.md` (các dòng "đã soát <mã>" cũ vẫn có hiệu lực — tra ở đó bằng `grep`).

- 21/09 07:32 (giờ máy) · Boss · Boss đã soát 63ac3d6 (Code 3, TẦNG ĐỌC của Bộ não A.I — chế độ THẬT): chỉ hiệu lực khi `bat` + chế độ của LỚP em = `that` + điều chỉnh `ap_dung=1`, chưa huỷ/tự gỡ, còn hạn; kế hoạch ngày: nhịp ±3 kẹp [6,16], không vượt phút/ngày em đặt, `on_som` chỉ kéo SỚM; BTVN: qua cổng `dieuChinh` + thích nghi chặng CHƯA MỞ, máy chủ kiểm BẤT BIẾN trước khi lưu (số chặng y nguyên, chặng đã mở y nguyên, lõi + thử thách còn đủ — vi phạm ⇒ giữ bộ cũ); KHÔNG có câu lệnh nào ghi `han_nop`; `/hs/ke-hoach-ngay` thêm `loiNhanHlv`, `/ph/ke-hoach` thêm `boNaoAi` đúng con; bóng/tắt/hết hạn ⇒ y hệt từng byte (test). Worktree sạch: 4 tệp test xanh, test kế hoạch ngày + phụ huynh: tệp đỏ ngoài nền = [], tsc server sạch. ĐƯỢC ĐẨY WORKER. BOSS QUYẾT: thích nghi sau chặng (Đợt 2 mục G) BẬT cho MỌI em bài cá nhân hoá (đúng thiết kế thầy duyệt), làm commit RIÊNG có cờ tắt.
- 21/09 07:31 (giờ máy) · Boss · THẦY LỆNH làm lại MÀN HÔM NAY của thầy (bản 2): bỏ khối "Việc cần theo dõi hôm nay" + bảng tin cũ; lưới ô trực quan; ô VIỆC GẤP "chưa nộp BTVN" có nút CẢNH BÁO (hiện lên app học sinh + thông báo phụ huynh); ô "em cần thầy giúp" chi tiết dạng; VINH DANH gộp tinh tế; Ô TRA CỨU tên/SBD ⇒ trang TOÀN CẢNH mọi việc em đã làm. Đề bài `prompt-hom-nay-gv-v2.md`: Code 4 VẼ MẪU 3 ảnh trước (thầy duyệt) · Code 3 hợp đồng + 6 lệnh `/gv/*` · Code 2 thẻ cảnh báo HS + PH. Ghi nhận từ ảnh thầy gửi: (1) LỖI hiện hạn "12:59:00" (phải 23:59 — việc ô 24 giờ của Code 4); (2) bài thật 87 câu mà 12121212 chỉ nhận 8 câu/1 chặng — nhờ Code 3 soi số liệu chốt.
- 21/09 07:28 (giờ máy) · Boss · c61247c (Code 1, `btvn-nang-do-lich.ts`: `xepLichChang`/`sucChua`/`canhBaoHanNgan`…) — thuần, test xanh, tsc sạch; Boss TỰ MÔ PHỎNG 4 ca: giao 15:00 hạn tối nay 24 câu ⇒ 15:00 · 20:00 · 21:20 (8 câu/chặng) ĐÚNG; hạn 7 ngày 56 câu ⇒ 8 chặng × 7 câu mở 00:00 ĐÚNG; em mở 22:50 hạn 23:59 ⇒ 22:50 · 23:30 (deadline thắng giãn cách) ĐÚNG. MỘT CHỖ PHẢI SỬA trước khi ghi "được đẩy": giao 21:30, hạn 23:59 NGÀY MAI, 30 câu ⇒ chặng 2 mở 23:40 đêm nay (sát giờ chốt, em phải học khuya) dù còn cả cửa sổ tối mai. Luật thêm: ở chế độ theo giờ, mốc mở phải ≤ (cuối cửa sổ − 1,5 × thời gian làm − 15 phút), không kịp thì dời sang ĐẦU cửa sổ kế; chỉ phá luật khi hạn nộp ép. Chữ ký hàm không đổi ⇒ Code 3 cứ nối.
- 21/09 07:26 (giờ máy) · Boss · Boss đã soát 4e1e331 (Code 2, `soLanLam`: thầy cho làm lại ⇒ máy em xoá nháp + kết quả chặng lượt cũ), a218316 (đọc mốc giờ từng chặng — lớp A, chờ máy chủ trả), dcbd864 (THẦY YÊU CẦU: thẻ "Bài tập về nhà · Chặng k", "Chặng k trong K chặng", "Hạn chặng này: HH:mm Thứ … dd/mm" + "Hạn nộp cả bài…", mã tờ xuống dòng phụ; cả thẻ LÀM NGAY, danh sách, phụ huynh). Worktree sạch dcbd864: 23 tệp test bảng nhiệm vụ/phiếu cá nhân hoá xanh, tệp đỏ ngoài nền = [], tsc app + server sạch. ĐƯỢC ĐẨY ⇒ LỆNH ĐẨY PAGES (kèm 904653f, e6dca3a, adc85f1, 5cfb94a của Code 4).
- 21/09 07:21 (giờ máy) · Boss · THẦY YÊU CẦU: ô chọn HẠN NỘP trong app giáo viên nhập theo NGÀY/THÁNG/NĂM + GIỜ:PHÚT, ĐỒNG BỘ 24 GIỜ ở mọi nơi (không phụ thuộc máy hiện AM/PM hay tháng/ngày). Giao Code 4: một ô dùng chung `ONgayGio24` thay mọi `datetime-local`/`date`/`time` của app thầy + mọi chỗ HIỆN hạn ghi "HH:mm · Thứ … dd/mm/yyyy" giờ VN.
- 21/09 07:20 (giờ máy) · Boss · Boss đã soát adc85f1 (Code 4: adapter Bộ não khớp máy chủ THẬT — chỉ nói "đã hỗ trợ/đã làm" khi máy chủ báo apDung, bỏ điều chỉnh `daBo:false` không nói "đã bỏ") + 5cfb94a (Xem trước hiện lịch chặng theo GIỜ + cảnh báo hạn ngắn khi máy chủ trả; chưa trả ⇒ như cũ). Worktree sạch: 3 tệp test xanh, tsc -b --force sạch. ĐƯỢC ĐẨY.
- 21/09 07:20 (giờ máy) · Boss · THẦY ĐÃ GIAO THỬ bài cá nhân hoá thật (thẻ "Bài tập: DH-12-C2-B6-TN: Chặng 1/1 · 8 câu") và YÊU CẦU sửa thẻ ở bảng nhiệm vụ học sinh: tên thẻ = "Bài tập về nhà · Chặng k", ghi RÕ tổng số chặng và HẠN CUỐI của chặng đang hiện (không hiện mã tờ đề làm tên). Giao Code 2 làm NGAY (việc nhỏ, trước thẻ Bộ não).
- 21/09 07:14 (giờ máy) · Boss · Boss đã soát e6dca3a (Code 4, bản 1.1 phần làm ngay: hạn nộp mặc định 23:59 giờ VN, Xem trước hiện cảnh báo THẬT của máy chủ; test xanh, tsc -b --force sạch). ĐƯỢC ĐẨY.
- 21/09 07:14 (giờ máy) · Boss · THẦY CHỐT (mới nhất): Bộ não A.I KHÔNG chạy thử — CHẠY THẬT LUÔN, thầy không bấm gì (đề xuất mục 14). ⇒ ĐƯỜNG GĂNG MỚI: Code 3 làm NGAY tầng ĐỌC điều chỉnh (kế hoạch ngày + BTVN nâng đỡ qua cổng `dieuChinh`, `on_som`, `loiNhanHlv`, `/ph/*` trả `boNaoAi`) → Code 2 thẻ "Bộ não A.I hỗ trợ riêng em <tên>" HS + PH → Code 1 hai mã lệnh → Boss hẹn giờ 04:00 → khi đủ ba mảnh: đặt `cheDo:that` cả trường (lệnh của thầy). Rào an toàn giữ nguyên.
- 21/09 07:13 (giờ máy) · Boss · MỐC: Worker 23ba8b32 ĐÃ SỐNG 07:12 (2d05cb1 — 6 lệnh `/ai/*` của Bộ não A.I, migration 3 bảng `ai_*` đã chạy). Code 3 thử thật: `/ai/cau-hinh` = {bat:true, cheDo:bong}; `/ai/ho-so-ngay` trang 1: 40 em/trang, tổng 267 em, 7 trang, 806 ms (sâu 18 · vắng 11 · bỏ qua 11 · nhanh 0 — ngày đầu sau reset ít hoạt động). Boss kiểm: không mã bí mật ⇒ bị từ chối. Chế độ chạy thử: chưa tầng nào đọc `ai_*`. CÒN cho 04:00: Code 1 hai mã lệnh `lay.mjs`/`nop.mjs` → Boss tạo tác vụ hẹn giờ + chạy thử tay có thầy bấm cho phép.
- 21/09 07:11 (giờ máy) · Boss · Boss đã soát 2d05cb1 (Code 3, nối 6 route `/ai/*`): cả 6 nằm SAU cổng `laThay` (dòng 3048 → 3119–3124), chỉ thêm 8 dòng; worktree sạch: 5 tệp test bộ não xanh (có test không mã ⇒ 403 + không ghi), tsc server sạch. ĐƯỢC chạy migration bo-nao --remote + ĐẨY WORKER.
- 21/09 07:10 (giờ máy) · Boss · Boss đã soát c5153a5 (Code 1, BỘ NÃO A.I · lệnh máy chủ `server/src/bo-nao.ts`, chưa nối index.ts): CHỈ GHI vào `ai_ho_so_ngay`, `ai_dieu_chinh`, `ai_ban_tin` + cờ `cau_hinh.bo_nao`; mọi bảng học sinh chỉ ĐỌC; nộp phải qua `kiemKhuon` LẦN NỮA ở máy chủ; mặc định `cheDo:'bong'` ⇒ lưu mà không áp dụng (test khoá: bảng học sinh không đổi byte); tự chấm hôm sau, `xau_di` ⇒ tự gỡ; migration chỉ tạo 3 bảng mới. Worktree sạch: 4 tệp test bộ não xanh, tsc app + server sạch. ĐƯỢC NỐI ROUTE + chạy migration + ĐẨY WORKER (Code 3; 6 route `/ai/*` phải nằm SAU cổng laThay).
- 21/09 07:09 (giờ máy) · Boss · MỐC: BTVN NÂNG ĐỠ ĐỢT 1 ĐÃ SỐNG — Pages 0612ea42 (mã nguồn 56b16af, sw 1789949074; Boss kiểm: 3 đường 200, gói `PhanCongScreen` sống có lệnh xem-truoc) + Worker 11d7be41 (1780e93 + 2de35c7 cho-lam-lai). Lùi Pages: 7047a659. CHƯA lên Pages: nút "Cho làm lại" 904653f (đợt kế), bản 1.1 lịch chặng theo giờ. MỜI THẦY GIAO THỬ một lớp, hạn 7 ngày.
- 21/09 07:08 (giờ máy) · Boss · Boss đã soát 904653f (Code 4, nút "Cho làm lại" từng em ở tab theo dõi: chỉ bài cá nhân hoá + em ĐÃ NỘP, quá hạn không có nút mà nhắc gia hạn, MỘT bước xác nhận đúng chữ thầy chốt, gọi `/btvn/cho-lam-lai` có mã bí mật, 404/từ chối/chậm nói thật; bài cũ y nguyên). Worktree sạch: test mới + theo dõi + giao-btvn-g5 xanh, tsc -b --force sạch. ĐƯỢC ĐẨY (đợt Pages kế, sau khi Worker 2de35c7 lên).
- 21/09 07:07 (giờ máy) · Boss · Boss đã soát 2de35c7 (Code 3, LÔ A): `/btvn/cho-lam-lai` sau cổng laThay — chỉ bài `ca_nhan` ĐÃ nộp + CHƯA quá hạn, chép kết quả vào `btvn_em_lich_su`, CAS theo `nop_luc`, giữ bộ câu + hạn; `btvn_lo.lan = chiSo + 1000×(lượt−1)`, `exp-d1` lấy `lan % 1000` ⇒ EXP cùng chặng không cộng đôi, bài cũ không đổi; xem-truoc trả `boQuaQid`/`thieuMeta`; ba bảng `ai_*` vào GIỮ của reset. Worktree sạch: 4 tệp test xanh, test EXP + reset: tệp đỏ ngoài nền = [], tsc server sạch. ĐƯỢC ĐẨY WORKER (gom cùng bản 1.1 / `/ai/*` nếu kịp, không kịp thì đẩy riêng).
- 21/09 07:03 (giờ máy) · Boss · THẦY CHỐT cho BTVN nâng đỡ: LỊCH CHẶNG LINH HOẠT THEO GIỜ khi hạn ngắn (không cứng theo ngày), giãn cách hợp lý để em không quá sức; giờ chốt mỗi ngày 23:59; giờ tự học chủ yếu 20:00–23:59. Đặc tả ở `prompt-btvn-nang-do.md` CẬP NHẬT 2: Code 1 hàm thuần `xepLichChang` + `sucChua` · Code 3 lưu `chang_mo_json`, thay `moLucChang` · Code 2 hiện "Chặng k mở lúc…" · Code 4 hạn mặc định 23:59 + cảnh báo hạn ngắn. Đây là bản 1.1, làm NGAY SAU đợt Pages Đợt 1.
- 21/09 07:02 (giờ máy) · Boss · Boss đã soát f7c7d1e + daf2450 (Code 1, thư viện THUẦN của Bộ não A.I: đặc trưng + phân luồng xoay vòng 1/7, khuôn thêm `khacPhuc[]`, ngưỡng 0,6) — không IO/đồng hồ/ngẫu nhiên, CHƯA màn nào import, 3 tệp test xanh, tsc sạch. ĐƯỢC ĐẨY (đi kèm đợt Pages BTVN nâng đỡ, không có tác dụng gì lên app cho tới khi được nối).
- 21/09 07:01 (giờ máy) · Boss · Boss đã soát dải PHIẾU BTVN NÂNG ĐỠ của Code 2: 8504aae 4ab054b 168a9af f47e9d3 b38f432 a21eecd bce89e6 844cafb 56b16af — phiếu `ca_nhan` rẽ nhánh riêng (`dungPhieuCaNhan`), máy em KHÔNG có đáp án câu chưa nộp (test khoá cả data-dung phần II), nộp CHẶNG qua host: chỉ nhận tin từ đúng `contentWindow` của khung phiếu, trả về chỉ ok/lỗi; thẻ cuối chặng chỉ số đếm; "Chặng k/K · N câu của em" ở bảng nhiệm vụ HS + PH. Worktree sạch 56b16af: 70 tệp test phiếu/bảng nhiệm vụ: tệp đỏ ngoài nền = [], tsc app + server sạch. ĐƯỢC ĐẨY PAGES cùng 8ec0d82 + 9526c0e + 055f1f1 (Code 4) và lõi 27059a6…de10ec7 (Code 1). ⇒ LỆNH ĐẨY PAGES ĐỢT BTVN NÂNG ĐỠ.
- 21/09 07:00 (giờ máy) · Boss · Boss đã soát 055f1f1 (Code 4, Bộ não A.I · app thầy: khối "đêm qua đã hỗ trợ N em" thể ĐÃ LÀM (chạy thử ghi ĐỀ XUẤT), nút chỉ Xem/Bỏ điều chỉnh, cảnh báo > 36 giờ; nhật ký từng em kèm NGUYÊN VĂN lời cho em/phụ huynh/thư tuần; Cài đặt: công tắc + thử/thật theo lớp, bật Thật phải xác nhận; chỉ gọi `/ai/*` có mã bí mật, máy chủ chưa có ⇒ nói thật; chữ hiển thị đã là "CHẠY THỬ"). Worktree sạch: test mới + hom-nay + hoc-sinh xanh, tsc -b --force sạch. ĐƯỢC ĐẨY. Ghi nhận: Hôm nay + Bảng tin cùng màn ⇒ Code 3 KHÔNG cần thêm mục Bộ não vào teacher-news.
- 21/09 06:57 (giờ máy) · Boss · MỐC: Worker 11e83882 ĐÃ SỐNG 06:53 (1780e93, máy chủ BTVN nâng đỡ Đợt 1; thầy cho phép đẩy trực tiếp ở Code 3). Code 3 thử thật CHỈ ĐỌC: tờ DB-12-B6-D3 (48 câu) × 3 SBD ⇒ 24/20/20 câu, lõi 14, 7 chặng; quy ước qid trên 3 tờ thật: boQuaQid = [], thieuMeta = 0. Boss kiểm: `/btvn/xem-truoc` không mã bí mật ⇒ bị từ chối. Chưa có bài ca_nhan thật nào. CÒN: Code 2 xong Chromium + "x/y câu của em" ⇒ Boss soát ⇒ đẩy Pages (kèm 8ec0d82 + 9526c0e của Code 4) ⇒ mời thầy giao thử.
- 21/09 06:53 (giờ máy) · Boss · Boss đã soát a21eecd + b38f432 (Code 2: tách lõi thuần `btvn-ca-nhan-kieu.ts` không đụng API trình duyệt ⇒ tsc của server exit 0; sửa import react-dom ⇒ `bang-nhiem-vu-1909` xanh lại). Worktree sạch a21eecd: tsc server + tsc -b --force sạch. ⇒ CỔNG ĐẨY WORKER THÔNG: Code 3 chạy lại `./DAY-MAY-CHU.command` (1780e93, migration đã chạy). Phần phiếu còn lại của Code 2 (8504aae→f47e9d3) soát khi em báo xong Chromium.
- 21/09 06:52 (giờ máy) · Boss · THẦY CHỐT: bài BTVN cá nhân hoá không tự cho làm lại; thêm NÚT "Cho làm lại" ở app giáo viên (từng em, một bước xác nhận) — `prompt-btvn-nang-do.md` mục CẬP NHẬT cuối: Code 3 lệnh `/btvn/cho-lam-lai` (giữ bộ câu + hạn nộp, lưu lịch sử, EXP không cộng đôi) · Code 4 nút ở tab theo dõi · Code 2 xoá nháp lượt cũ theo `soLanLam`. Làm SAU khi Đợt 1 lên máy chủ. Đang chờ: Code 2 sửa lỗi tsc (chặn script đẩy Worker của Code 3; migration btvn-nang-do đã chạy --remote).
- 21/09 06:50 (giờ máy) · Boss · Boss đã soát 1780e93 (Code 3, MÁY CHỦ BTVN NÂNG ĐỠ Đợt 1 — CÓ ĐỔI LUẬT CHẤM BTVN, thầy đã duyệt rõ): migration CHỈ THÊM (3 cột btvn, 6 cột btvn_em, 2 bảng mới — đã vào danh sách XOÁ của reset); bài `ca_nhan=0` rẽ nhánh sớm, đường cũ y nguyên; điểm = đúng / (câu của em − câu THƯỞNG sai); đáp án bóc bằng DANH SÁCH TRẮNG trường (test soi cả đáp án phần II/III lồng trong); nộp chặng có chặn quá hạn + khoá đáp án đầu + CAS; xem trước 0 ghi; chống chép chỉ trên lõi; bài cá nhân hoá không làm lại (câu sai đi lịch ôn 1·3·7). Worktree sạch: 3 tệp test mới xanh, test BTVN/kế hoạch/reset cũ: tệp đỏ ngoài nền = [], tsc app sạch. ĐƯỢC chạy migration --remote + ĐẨY WORKER.
- 21/09 06:50 (giờ máy) · Boss · VIỆC CỦA CODE 2 trước đợt Pages: (1) `tsc` của server ĐỎ vì `src/lib/btvn-ca-nhan-em.ts` dùng `localStorage` mà bị kéo vào phía máy chủ qua `cau-hinh-chua.ts → bai-tap-pdf.ts → import type` ⇒ tách kiểu `NhanCauEm` + hàm thuần sang tệp KHÔNG đụng API trình duyệt; (2) test `bang-nhiem-vu-1909` mới đỏ do `TheCuoiChang.tsx` import react-dom.
- 21/09 06:46 (giờ máy) · Boss · THẦY CHỐT THÊM cho Bộ não: (1) MỖI em MỖI ngày một lời nhắn viết riêng, rất tâm huyết; CÓ lời cho PHỤ HUYNH (khi đáng + thư tuần) — chỉ hiện trong app phụ huynh; (2) tên hiển thị "Bộ não A.I hỗ trợ riêng em <họ tên>"; (3) "chạy bóng" ⇒ "chạy thử". Đã sửa cẩm nang (mục NGHỀ VIẾT LỜI NHẮN, KHUÔN thêm `loiNhanChoPhuHuynh`, `thuTuan`; soi nhanh dùng mô hình vừa, soi kỹ dùng mô hình mạnh nhất) + `prompt-bo-nao.md` CẬP NHẬT 3 + đề xuất mục 13.
- 21/09 06:41 (giờ máy) · Boss · THẦY CHỐT "BỘ NÃO TỰ HÀNH": tự điều chỉnh + KHẮC PHỤC LUÔN cho từng em, KHÔNG đụng deadline, thầy không duyệt gì, chỉ đọc báo cáo "đã hỗ trợ…" trên bảng tin. Đã sửa: đề xuất mục 12, cẩm nang (chế độ tự hành, núm `khacPhuc`/`on_som`, bản tin thể ĐÃ LÀM), đề bài `prompt-bo-nao.md` CẬP NHẬT 2. Rào thay người duyệt: tin cậy ≥ 0,6, ≤ 2 đổi/em/đêm, hết hạn 3 ngày, hôm sau tự chấm — xấu đi thì TỰ GỠ; DEADLINE THẮNG MỌI NÚM (thuật toán giữ, có test tính chất). 2 đêm bóng kiểm máy móc ⇒ đêm 3 Boss tự bật thật.
- 21/09 06:38 (giờ máy) · Boss · Bộ não MỐC 1: Boss đã soát c6c07db (Code 1, `bo-nao-khuon.ts`: kiểm biên độ/độ dài/từ cấm/ký tự lạ/bí danh khớp thẻ/mọi chữ số phải có trong thẻ) + 44c9700 (hợp đồng `docs/hop-dong-bo-nao-2109.md`). Cẩm nang bổ sung: khuôn bản tin `ra/lop.json` + LUẬT CHỮ SỐ (số sự thật viết bằng chữ số, số việc sắp làm viết bằng chữ). Bản vẽ màn thầy (Code 4, `docs/ban-ve-bo-nao-2109/`) đã gửi thầy.
- 21/09 06:30 (giờ máy) · Boss · Boss đã soát 9526c0e (Code 4: `qidCuaCau` = `<mã tờ gốc>-<phần>-<so>`, test khoá bằng CHÍNH `homeworkQuestions` của máy chủ, câu máy chủ loại thì không gửi; worktree sạch: 5 tệp test xanh, tsc -b --force sạch) ⇒ 8ec0d82 + 9526c0e ĐƯỢC ĐẨY (khi máy chủ BTVN nâng đỡ lên).
- 21/09 06:30 (giờ máy) · Boss · THẦY CHỐT BỘ NÃO: "build dùng PHIÊN làm não trước và chạy luôn từ hôm nay" (gói Max tới 04/10 ⇒ 13 đêm đo token). Đề bài `prompt-bo-nao.md` (xem mục CẬP NHẬT cuối tệp): Code 1 làm trọn ruột (lõi thuần + `server/src/bo-nao.ts` + 2 mã lệnh + migration), Code 3 chỉ nối route + migration + đẩy, Code 4 màn của thầy, Boss hẹn giờ 04:00 + đo token. Mục tiêu: chạy BÓNG lượt đầu 04:00 sáng 22/09.
- 21/09 06:28 (giờ máy) · Boss · Boss đã soát de10ec7 (Code 1: thử thách tính trên chỗ trống SAU lõi, trần floor(20 %) có thể = 0, chỉ dạng ổn; chỗ còn lại lấp dạng yếu đúng bậc → củng cố; em yếu ngân sách = lõi + 3 ⇒ 0 thử thách). Worktree sạch: test lõi xanh, tsc -b --force sạch. Đây là BẢN LÕI Code 3 import. ĐƯỢC ĐẨY cùng phần dùng nó.
- 21/09 06:26 (giờ máy) · Boss · Boss đã soát 741a903 (Code 1, lõi Đợt 2: `thichNghiChangSau` + cổng `dieuChinh?` — nhịp ±3, khởi động 1–3, 4 nút dạng ≤ 3; vắng cổng ⇒ y hệt bản trước, có chữ ký vàng; worktree sạch: 4 tệp test xanh, tsc -b --force sạch, vẫn thuần). BOSS QUYẾT về quirk "ngân sách chỉ hơn lõi vài câu ⇒ chỗ trống thành toàn thử thách": SỬA NGAY (chưa có gì lên máy chủ): chỗ trống ưu tiên dạng YẾU đúng bậc → củng cố → thử thách (trần 20 % phần ngoài lõi, có thể = 0).
- 21/09 06:22 (giờ máy) · Boss · 8ec0d82 (Code 4, BTVN nâng đỡ · app thầy): GIAO DIỆN + LOGIC ĐẠT (worktree sạch: 4 tệp test xanh, test PhanCong đỏ ngoài nền = [], tsc -b --force sạch; tắt công tắc = thân gửi y cũ; ghim=[] giao được) nhưng CHƯA ĐƯỢC ĐẨY: `qidCuaCau` gửi `q.id` của máy thầy, còn máy chủ định danh câu BTVN = `<mã đề gốc>-<phần>-<so>` (btvn-grading.ts:128, hợp đồng dòng 7) ⇒ lệch là mọi nhãn dạng/mức/sao bị bỏ (`boQuaQid`), cá nhân hoá mù. Code 4 + Code 3 chốt MỘT quy ước + test tích hợp: một tờ đề thật ⇒ `boQuaQid = []`, `thieuMeta = 0`.
- 21/09 06:09 (giờ máy) · Boss · Boss đã soát 27059a6 + 569ebfd (Code 1, lõi thuần `src/lib/btvn-nang-do.ts`): thuần (không IO/đồng hồ/ngẫu nhiên), 31 test tính chất xanh, tsc -b --force sạch ở worktree sạch. Boss TỰ MÔ PHỎNG 80 câu/12 dạng × 5 kiểu em, hạn 7 ngày: lõi 24 câu toàn mức thấp nhất; em YẾU 36 câu, 100 % Biết, 0 thử thách; TRUNG BÌNH 58 câu Biết/Hiểu; KHÁ 68 câu có Vận dụng; em CHƯA HỒ SƠ 43 câu + 7 thử thách; mọi chặng mở bằng 2 câu khởi động. ĐÚNG THIẾT KẾ thầy chốt. ĐƯỢC ĐẨY (khi các phần dùng nó xong).
- 21/09 06:08 (giờ máy) · Boss · THẦY DUYỆT 5 bản vẽ BTVN nâng đỡ ("đúng ý tôi, build tới lúc xong thì báo"). Bộ não AI: thầy QUYẾT chạy trên PHIÊN tại máy thầy (máy bật 24/7) ⇒ đề xuất bản 2 `DE-XUAT-BO-NAO-AI-2109.md` (4 tầng: thuật toán tức thời · thuật toán đêm phân luồng · phiên AI hẹn giờ 04:00 vặn núm · Boss hằng tuần chỉnh tham số) + mục 10 ước chi phí phương án API tuần/lần. CHỜ THẦY CHỐT, chưa build.
- 21/09 06:04 (giờ máy) · Boss · BTVN nâng đỡ — BOSS QUYẾT: (1) câu nhãn `thu_thach` + `loi_cao` là câu THƯỞNG trong điểm của em (đúng ⇒ vào tử và mẫu; sai ⇒ không vào mẫu); thống kê lõi phía thầy tính bình thường. (2) Bài `ca_nhan=1` KHÔNG gửi `dap_an` xuống máy em; máy chủ chấm lúc NỘP CHẶNG. (3) Ghim tuỳ chọn. PHÁT HIỆN (Code 3): bài BTVN THƯỜNG hiện vẫn trả `dap_an` xuống máy em (`/btvn/cua-em`) — giữ nguyên đợt này, Boss hỏi thầy sau. Bản vẽ: Code 4 (giao bài, xem trước) + Code 2 5132165 (3 màn HS) đã gửi thầy; hợp đồng Code 3 a03b417; kiểu Code 1 27059a6.
- 21/09 06:01 (giờ máy) · Boss · Ý TƯỞNG MỚI của thầy: "bộ não AI" điều tiết từng em mỗi đêm — đề xuất `DE-XUAT-BO-NAO-AI-2109.md` CHỜ THẦY XEM (AI vặn núm trong khung, thuật toán chọn câu; chạy trên máy chủ qua API; chạy bóng trước). CHƯA phiên nào build. Thầy cũng xác nhận: GHIM câu ở BTVN nâng đỡ là TUỲ CHỌN.
- 21/09 05:53 (giờ máy) · Boss · THẦY CHỐT BTVN "NÂNG ĐỠ" — việc SỐ 1 của mọi phiên. Đề bài `prompt-btvn-nang-do.md`: Code 1 lõi thuần `src/lib/btvn-nang-do.ts` (xuất KIỂU trước) · Code 3 máy chủ + hợp đồng `docs/hop-dong-btvn-nang-do-2109.md` · Code 4 màn Giao bài (công tắc, ghim, xem trước phân bổ, theo dõi theo chặng) · Code 2 phiếu BTVN của em + phụ huynh. Có ĐỔI LUẬT CHẤM BTVN (thầy duyệt rõ: điểm trên số câu của từng em) ⇒ mọi commit cần dòng soát.
- 21/09 05:49 (giờ máy) · Boss · BA NÚT ĐÃ SỐNG: Pages 7047a659 (main 894c023, sw 1789944088 — Boss kiểm 200) + Worker efeabed2: "Thêm 5 phút", "Chiếu mã vào thi", "Đổi tên học sinh". Thầy lệnh mới: BTVN cá nhân hoá "nâng đỡ" — đề xuất `DE-XUAT-BTVN-NANG-DO-2109.md` CHỜ THẦY CHỐT (chưa phiên nào build; có đổi luật chấm BTVN).
- 21/09 05:40 (giờ máy) · Boss · Boss đã soát 5b9220c (Code 4, khớp Worker efeabed2: câu báo thêm "và N em đang bị khoá", `thuLai` ⇒ "Chưa cộng gì — bấm lại", đổi tên trùng tên không ném lỗi; worktree sạch: 2 tệp test xanh, tsc -b --force sạch). ĐƯỢC ĐẨY. Worker efeabed2 đã sống (`/ca/them-phut`, `/hoc-sinh/doi-ten`, migration them_phut_tong); `btvn_em.lo_da_xong` CÒN trên D1 thật.
- 21/09 05:37 (giờ máy) · Boss · Boss đã soát eb30796 + 36750f1 (Code 3, `/ca/them-phut`: chỉ cộng, trần 30, khoá lạc quan; lượt `khoa` CŨNG được cộng (`soLuotKhoaCong`); `publish` + `/ca/nhieu` không hạ `thoi_gian_phut` của ca đã thêm phút; `examStatus` trả `hetGioLuc`; migration chỉ thêm) và f87204f (Code 2, màn thi nhận hạn mới: CHỈ kéo dài ≥ 30 s, đúng ca + đúng em, lưu IndexedDB, một dòng `role=status` tự tắt, nhường cảnh báo rời màn; không đổi chữ ký pushExamStatus). Worktree sạch HEAD 36750f1: 3 tệp test thêm-phút xanh, test màn thi: tệp đỏ ngoài nền = [], tsc app + server sạch. ĐƯỢC ĐẨY: Code 3 migration → Worker; rồi Code 2 Pages.
- 21/09 05:32 (giờ máy) · Boss · Boss đã soát 6aeb4f9 (Code 4, G3d nút bút chì "Đổi tên học sinh": ô tại chỗ, gọi `/hoc-sinh/doi-ten` có mã bí mật, CHỈ khi máy chủ ok mới cập nhật classlist máy thầy, lỗi/404 nói thật, có ghi chú "nạp lại danh sách gốc sẽ ghi đè tên"; worktree sạch: 3 tệp test xanh, tsc -b --force sạch; đã xem ảnh 390). ĐƯỢC ĐẨY.
- 21/09 05:30 (giờ máy) · Boss · Boss đã soát 5409969 (Code 3, `/hoc-sinh/doi-ten`: sau cổng laThay, tên bảng từ danh sách cố định, chỉ cột `ho_ten` theo sbd ở 5 bảng, giữ phieu/chan_vao, trùng tên không ghi; test xanh, tsc sạch) + d18fee5 (Code 4, câu "CHƯA CHẮC đã cộng"). ĐƯỢC ĐẨY.
- 21/09 05:30 (giờ máy) · Boss · eb30796 (Code 3, `/ca/them-phut`) — lõi ĐẠT (chỉ cộng, trần 30, khoá lạc quan, sau laThay) nhưng CHƯA ĐƯỢC ĐẨY, sửa 2 chỗ: (1) lượt `khoa` PHẢI được cộng — `moKhoaEm` đưa `khoa` về `dang_lam` với NGUYÊN `het_gio_luc` (goi-cu.ts:1337), không cộng thì em được mở khoá thiệt N phút so với cả phòng; đếm riêng `soLuotKhoaCong`. (2) `publish`/`dayCa` ghi đè `thoi_gian_phut` (index.ts:~582): đẩy lại ca sau khi thêm phút KHÔNG được làm mất phút đã thêm — chặn ở máy chủ + test khoá.
- 21/09 05:24 (giờ máy) · Boss · Boss đã soát 6faa68c (Code 4, G4d: "Thêm 5 phút" một bước xác nhận đúng câu hợp đồng, lỗi/404/từ chối nói thật; "Chiếu mã vào thi" tấm phủ chỉ mã + tên ca + link `/t/<mã>` thật + số em chờ, không tên em; nút/cảnh báo cũ nguyên; đã xem 2 ảnh). ĐƯỢC ĐẨY. Việc nhỏ kèm: khi lệnh bị HUỶ do quá 15 s phải nói "chưa chắc đã cộng — bấm Làm mới", không nói "chưa cộng".
- 21/09 05:24 (giờ máy) · Boss · THẦY LỆNH: nút "Đổi tên học sinh" trong hồ sơ — hợp đồng `docs/hop-dong-doi-ten-hoc-sinh-2109.md`: Code 3 lệnh thầy `/hoc-sinh/doi-ten` (chỉ cột tên ở danh_sach, hoc_sinh, luot, btvn_em, phong_cho; giữ phieu/chan_vao); Code 4 bút chì cạnh tên em. Đã mở game Đoàn toàn trường (Code 3, `toanBo:true`; lùi = `toanBo:false`).
- 21/09 05:15 (giờ máy) · Boss · THẦY LỆNH (sau reset): (1) MỞ GAME Đoàn Hộ Tống CHO TOÀN TRƯỜNG — Code 3 đặt `cau_hinh.doan_ho_tong.toanBo=true`; (2) thêm 2 nút "Thêm 5 phút" + "Chiếu mã vào thi" — hợp đồng `docs/hop-dong-them-phut-2109.md`: Code 3 lệnh `/ca/them-phut` + `examStatus` trả `hetGioLuc`; Code 2 màn thi nhận hạn mới (chỉ KÉO DÀI, không rút); Code 4 hai nút ở màn theo dõi ca. Mọi commit cần dòng soát.
- 21/09 05:08 (giờ máy) · Boss · RESET XONG (thầy cho phép TRỰC TIẾP ở Code 3, 05:05). Job 05:05:46→05:06:44, 0 lỗi. GIỮ nguyên ca 27 / luot 265 / su_kien_hoc 26.445 / nam_kt_cau 15.321; mọi bảng XOÁ = 0. Boss kiểm sống 05:08: `/hs/ke-hoach-ngay` 12121212 → mocReset 2026-09-21, thanThu null, exp.homNay 0; Pages 3 đường 200. Bookmark sau reset 0000023b-00007c65-…; sao lưu omr-saoluu/omr-d1-20260921-0225.sql. Infographic đã cập nhật. Mọi phiên XONG TRỌN.
- 21/09 02:31 (giờ máy) · Boss · RESET CHƯA CHẠY: bước đặt cờ `reset_toan_app_cho_phep` của Code 3 bị hệ thống phân quyền chặn (xoá dữ liệu thật, lệnh từ phiên khác). Boss KHÔNG làm thay (không lách quyền). Mọi thứ khác đã sẵn: Pages 03d5c01e, Worker 97f13963, sao lưu omr-d1-20260921-0225.sql, bookmark 0000023b-000075ef-…, dry-run khớp. CHỜ THẦY cho phép TRỰC TIẾP trong phiên Code 3; sau đó Code 3 lấy bookmark mới + dry-run lại rồi mới đặt cờ.
- 21/09 02:29 (giờ máy) · Boss · LỆNH RESET (thầy uỷ quyền 21/09: "tự động hoàn thành hết… rồi tự động reset", giữ mọi ca thi + hồ sơ mạnh yếu). Đủ điều kiện: Code 1/2/4 XONG TRỌN; Pages đợt cuối 03d5c01e (2ad41cf, sw 1789932305) Boss kiểm sống 200; Worker 97f13963; sao lưu chính thức omr-saoluu/omr-d1-20260921-0225.sql (110,6 MB, 66 bảng, luot 265 — Boss tự kiểm tệp); dryRun XOÁ 15.428 / GIỮ 93.354, chuaPhanLoai [], 14 bảng ca thi + 5 bảng hồ sơ ở GIỮ. ⇒ Code 3: bookmark mới → LÊN ĐẠN → theo dõi tới `xong`.
- 21/09 02:24 (giờ máy) · Boss · Boss đã soát 15f9bc6 (G5: ExamSetup CHỈ đổi màu; PhanCong thêm khối lô BTVN chỉ đọc + "Giao bài riêng" chỉ tích sẵn em, thầy vẫn phải bấm giao), 4737b62, 0cc503d (G6), 2ad41cf (sửa import thừa làm build đỏ) — Code 4. Worktree sạch 2ad41cf: tsc -b --force exit 0; 51 tệp test màn thầy: tệp đỏ ngoài nền = []. ĐƯỢC ĐẨY. Code 1 + Code 4 XONG TRỌN ⇒ lệnh ĐẨY ĐỢT CUỐI cho Code 2.
- 21/09 02:02 (giờ máy) · Boss · Boss đã soát 65ca65a (Code 1: nút "Cho vào bằng máy khác" — chỉ hiện ở lượt `duoc_duyet_lai` đang khoá máy, MỘT bước xác nhận, dùng đúng lệnh `moKhoa` sẵn có của máy chủ (không luật mới), nói thật theo `goKhoaMay`; 4 tệp test xanh + tsc -b sạch ở worktree sạch). ĐƯỢC ĐẨY. Code 1 báo XONG TRỌN.
- 21/09 01:59 (giờ máy) · Boss · Boss đã soát 3a5e099 (Code 4: hồ sơ thầy nối `/gv/ke-hoach-em` chỉ đọc, có mã bí mật, `chuaCo` ⇒ "Chưa có kế hoạch hôm nay"; không còn `/hs/*` trong hồ sơ thầy). ĐƯỢC ĐẨY. Worker 97f13963 (d996df4) đã sống.
- 21/09 01:58 (giờ máy) · Boss · Boss đã soát d996df4 (Code 3: `/gv/ke-hoach-em` CHỈ ĐỌC — một SELECT + docThanThu + docExpHomNay, nằm SAU cổng laThay, 0 ghi có test khoá; `khoaMay` chỉ có/không, không trả mã máy) ⇒ ĐƯỢC ĐẨY WORKER. Đã soát 4f1fc28 + ba1ea0a (Code 4, G3 màn Học sinh sau khi bỏ gọi lệnh có ghi) ⇒ ĐƯỢC ĐẨY PAGES. Code 4 nối `layKeHoachEm` sang `/gv/ke-hoach-em` sau khi Worker lên.
- 21/09 01:55 (giờ máy) · Boss · Boss đã soát 6859175 (Code 1, M6 hiệu chỉnh giờ lên bảng từ giây thật: tờ chiếu chỉ thêm data-* + đo giờ, KHÔNG thêm gì hiện trước lớp; lệnh ghi đi như cũ khi không đo được) + f892636 (Code 3, `len_bang.giay_thuc` CHỈ THÊM, ngoài 20..1800 ghi NULL, phòng vệ cột chưa có). Worktree sạch: 7 tệp test xanh, tsc -b sạch. ĐƯỢC ĐẨY (Code 3: migration --remote trước, rồi Worker).
- 21/09 01:54 (giờ máy) · Boss · Boss đã soát cd2d9b0 (Code 1, M5 tối ưu học nhiều nhất + ghép đôi thật: chỉ xếp buổi chữa phía thầy, không đụng chấm điểm/vào thi; 5 tệp test đỏ = nền, 11 test đỏ Y HỆT trước/sau ở worktree sạch, tsc -b sạch), 7b30d19 (Code 2, sửa kiểu TS2322 của b81281d), ce9b28e (Code 4, Hôm nay hiện lyDoThieu), 667dad4 (tương phản vinh danh). ĐƯỢC ĐẨY.
- 21/09 01:54 (giờ máy) · Boss · 4f1fc28 (Code 4, G3 màn Học sinh) — GIAO DIỆN ĐẠT, nhưng CHƯA ĐƯỢC ĐẨY: `layKeHoachEm` gọi `/hs/ke-hoach-ngay` = lệnh CÓ GHI (tạo kế hoạch + `capNhatExp`, trả `expNhan` một lần) ⇒ thầy mở hồ sơ sẽ nuốt mất thông báo EXP của em. Sửa: Code 4 tạm cho khối kế hoạch/thần thú về "đang chờ máy chủ" (không gọi lệnh ấy); Code 3 thêm lệnh thầy CHỈ ĐỌC `/gv/ke-hoach-em`; có lệnh thì Code 4 nối lại.
- 21/09 01:32 (giờ máy) · Boss · Boss đã soát a1bf736 (Code 4, G4c: nút nổi Mở ca không đè màn theo dõi, cùng onClick; 8 ảnh G4 — đã xem ảnh 1440 sáng, đúng bản vẽ). ĐƯỢC ĐẨY. "Thêm 5 phút"/"Chiếu mã" = tính năng MỚI, KHÔNG làm đợt này (ghi lại hỏi thầy sau).
- 21/09 01:32 (giờ máy) · Boss · Boss đã soát 3044dbd + c1090a7 (Code 4, G4a màn theo dõi ca + G4b danh sách ca mặc M3): chỉ thêm tab lọc HIỂN THỊ (state giao diện, đếm từ dữ liệu sẵn có, 0 lệnh mới), hàm/nút/chuỗi giữ nguyên; 36 tệp test liên quan: 32 xanh, 4 đỏ đều thuộc nền cũ (KhoiLuyenKhacPhuc vắng từ trước). ĐƯỢC ĐẨY.
- 21/09 01:28 (giờ máy) · Boss · Boss đã soát b81281d (Code 2, C10 bước 5: dải cảnh báo rời màn, tấm giữ-để-đọc, thẻ khoá bài mặc M3 — cùng role/z-index/vị trí/câu chữ, nhánh cũ giữ nguyên ngoài dungM3, 0 state/effect/listener mới). ĐƯỢC ĐẨY. ⇒ C10 (màn thi thật theo bản vẽ) XONG.
- 21/09 01:26 (giờ máy) · Boss · Boss đã soát e781e0c (Code 3: job reset BẢN CUỐI — BANG_XOA chỉ còn BTVN/bài Mẹ giao/luyện đề/kế hoạch ngày/len_bang/trao đổi/game+thần thú+EXP+doan_*/vinh danh/bảng tin; 14 bảng ca thi + sổ/hồ sơ mạnh yếu nằm ở GIỮ). Worker 6132b838, CHƯA lên đạn; dryRun thật: XOÁ 15.428 / GIỮ 93.353 dòng (ca 27, luot 265). Đã lên: token PH mềm, lệnh màn Hôm nay, on_thi mang câu. Code 3 chờ lệnh "MỌI VIỆC XONG".
- 21/09 01:25 (giờ máy) · Boss · Boss đã soát 14e61de (Code 1: reset GIỮ ca thi ⇒ danh sách dọn máy thầy RỖNG, bỏ toast, giữ cơ chế + đai-dây capNhatKeyBank; khoá buổi chữa dở không còn gắn mốc reset — đồng ý). ĐƯỢC ĐẨY.
- 21/09 01:18 (giờ máy) · Boss · Boss đã soát 34a4a90 (Code 2, C10 bước 4 "Xem lại sau": trạng thái giao diện tại máy, khoá riêng `ddh.xemlai.*`, không vào gói nộp/lưu/chấm — có test khoá nguồn + so gói /nop thật). ĐƯỢC ĐẨY.
- 21/09 01:17 (giờ máy) · Boss · ĐÃ GỘP 62710ce = Đảo thần thú MỚI nối vào Game.tsx bằng một vỏ `dao/DaoThanThu` (Code 5 + Code 6 XONG TRỌN; bản sạch 262/262 test game+đảo, tsc sạch; mục Đoàn theo `doanMo`). ĐƯỢC ĐẨY — Đảo mới lên cho cả trường ở đợt Pages kế. Cũng đã soát f3a6b05 (Code 4: cổng PH nhận `?ph=`, không chạm luồng thi).
- 21/09 01:16 (giờ máy) · Boss · **THẦY ĐỔI PHẠM VI RESET LẦN NỮA: "Khi reset giữ lại toàn bộ ca thi đã thi".** PHẠM VI CUỐI: GIỮ mọi bảng ca thi (ca, luot, chi_tiet_cau, ban_do_sai, phong_cho, chan_vao, trang_thai, phieu, kho_ca_them, nhan_xet, de_rieng, dong_bo, nop_khac_phuc, tien_do_ca) + sổ/hồ sơ mạnh yếu + tài khoản/lớp/kho đề/cấu hình. XOÁ: BTVN (btvn, btvn_em, btvn_em_lich_su), bài Mẹ giao, luyện đề, yêu cầu giao bài, nháp, ke_hoach_ngay, len_bang, trao đổi, TOÀN BỘ game/thần thú (chọn lại thú)/EXP/mảnh khiên/doan_*, vinh danh, bảng tin PH. HỆ QUẢ: máy thầy KHÔNG dọn dữ liệu theo ca nữa (Code 1); máy HS/PH chỉ dọn nháp BTVN/Mẹ giao/kế hoạch/game (Code 2); R2 vẫn không đụng.
- 21/09 01:06 (giờ máy) · Boss · Boss đã soát 1b42fd8 (Code 1, M4: lưu buổi chữa dở ở khoá settings `buoiChua:…`, thẻ "Tiếp tục buổi trước", giữ em có mặt/thay em vắng, hạn 14 ngày, gắn mốc reset; chỉ tệp trong làn). ĐƯỢC ĐẨY.
- 21/09 01:02 (giờ máy) · Boss · Boss đã soát 75eb057 (Code 4, app giáo viên G2: màn HÔM NAY — tệp mới HomNayScreen/hom-nay-api/hom-nay.css, ExamHubScreen đổi 9 dòng; chưa có lệnh máy chủ thì hiện "đang chờ máy chủ", không bịa số). ĐƯỢC ĐẨY. Nối Đảo đợt 1 của Code 5 (ddede84) CHƯA gộp — chờ vỏ Đảo của Code 6 để lên một lần cho trọn.
- 21/09 01:01 (giờ máy) · Boss · Boss đã soát 0a092f1 (Code 2, C10 bước 3: thân màn thi M3 — dải "Còn N câu chưa làm", đầu phần, nhãn "Mới x/4 ý"; chỉ trình bày lại dữ liệu sẵn có, 0 state/effect mới, chỉ đường học sinh). ĐƯỢC ĐẨY. Worker bf307951 (game bước 6 + token PH mềm + doan_trum_cau vào XOÁ) đã lên.
- 21/09 00:56 (giờ máy) · Boss · ĐÃ GỘP 033df66 = game bước 6 + 3 việc máy chủ cho Đảo (`start` trả `role`, `answer` trả `lyDoThuong`, lệnh đọc-chỉ `so-tay`; core đổi `chooseSession` → `chooseSessionWithRoles`, test than-thu-v2/kenh4 xanh) và 314126e (Code 6: màn Hòn đảo + Túi đồ). Bản sạch 262/262 test game+đảo, tsc app+server sạch. ĐƯỢC ĐẨY. Code 3: chạy `server/migration-2109-game-doan-trum-cau.sql` --remote, thêm `doan_trum_cau` vào BANG_XOA, đẩy Worker.
- 21/09 00:51 (giờ máy) · Boss · Boss đã soát d3dba04 (Code 1: bảng DỌN/GIỮ máy thầy theo lệnh mới — GIỮ khoDoKho + lichOnLai, DỌN diemCuaEm + khoá theo ca + buoiChua:*). DUYỆT M4 lưu buổi dở ở khoá settings `buoiChua:…` thay vì store mới (không nâng phiên bản IndexedDB — tránh upgrade() khi em đang thi). ĐƯỢC ĐẨY.
- 21/09 00:51 (giờ máy) · Boss · Boss đã soát 4a51718 + 6fef218 (Code 4, app giáo viên G1: vỏ M3 — điều hướng trái/rail/thanh đáy đủ mục, màn Cài đặt mới, sáng/tối; 16 tệp thầy vào bộ sinh M3; exam-setup.css chỉ đổi CSS, hết hex; không màn nào đổi hàm/luồng). ĐƯỢC ĐẨY — điều kiện: toàn vitest bản sạch của Code 2 MỚI ĐỎ = [].
- 21/09 00:49 (giờ máy) · Boss · Boss đã soát 3da1b32 (Code 2, C10 bước 2: thanh trên màn thi M3 — component trình bày `ThanhTrenThiM3` nhận đúng các giá trị sẵn có, vẫn sticky z-30 cao 56 px; ngoài dungM3 thanh cũ y hệt; StudentPortalScreen chỉ thêm biến lề để nút Đóng không đè nút lưới câu). ĐƯỢC ĐẨY.
- 21/09 00:48 (giờ máy) · Boss · Boss đã soát 741bb33 (Code 3: job reset bản mới — GIỮ su_kien_hoc/nam_kt_cau/nam_kt_dang/tien_do_hs/qid_da_lam + ph_truy_cap; XOÁ thêm doan_ve_so/doan_trum_lop; chạy THEO LỆNH `reset_toan_app_cho_phep`, cửa sổ 60 phút từ lúc lên đạn). ĐƯỢC ĐẨY (Worker a5b1db90, CHƯA lên đạn). dryRun thật: XOÁ 25.191 dòng / GIỮ 83.588. Đã gộp 24de50e (Code 6: ảnh nhẹ + màn Chọn bạn đồng hành).
- 21/09 00:42 (giờ máy) · Boss · Boss đã soát 64bfa4e (Code 1, M3 tờ chiếu: giao diện mới + thẻ tên + thần thú + nút chấm qua cầu nối; chỉ tệp trong làn; không chiếu lý do gọi em / tình trạng BTVN). DUYỆT 4 quyết định: in "Làm tốt lắm" thay "+10 EXP" (chưa có số thật), ẩn nhãn BTVN cạnh tên, thanh điều khiển xuống dưới, "Buổi x/90" đếm giờ thật. ĐƯỢC ĐẨY.
- 21/09 00:40 (giờ máy) · Boss · Boss đã soát 2d4ca2f (Code 2, C10 bước 1: ExamTakeScreen chỉ thêm lớp `m3` ở khung Trang khi dungM3() + cờ giaoDienHocSinh cho phiếu "Đề của em"; không dòng logic nào đổi). ĐƯỢC ĐẨY. Worker 2e9f32a3 (game bước 5 + doanMo) đã lên.
- 21/09 00:40 (giờ máy) · Boss · **LỆNH CỦA THẦY (đi ngủ): "tối ưu tối đa tiết kiệm token… TỰ ĐỘNG HOÀN THÀNH HẾT MỌI VIỆC, ĐẨY HẾT LÊN MÁY CHỦ RỒI TỰ ĐỘNG RESET… âm thầm xử lý, chỉ báo tôi sau khi đã xong hết".** MỌI PHIÊN: (a) làm LIÊN TỤC tới hết hàng việc, không chờ, không hỏi thầy; (b) tin cho Boss/phiên khác ≤ 60 từ: "XONG <việc> · <mã commit> · cần soát/gộp/đẩy" — chi tiết để trong sổ việc; (c) KHÔNG chạy toàn bộ vitest trừ người đẩy; không dán log; (d) xong trọn thì nhắn Boss "XONG TRỌN" — Boss lưu trữ phiên. Khi cả 6 phiên XONG TRỌN + đợt đẩy cuối lên: Boss tiến hành reset theo trình tự an toàn (export → dryRun → lên đạn → kiểm sống) rồi mới báo thầy.
- 21/09 00:39 (giờ máy) · Boss · ĐÃ GỘP 0235a4d = game bước 5 (vé chỉ từ việc học qua `docThanhTichNgay`, chặng đầu ngày miễn phí, Đoàn lớp 30 trạm theo mùa đọc từ game_v2_settings, rương chuỗi 3/7/14 tất định, Trùm lớp bản gọn CN 20:00–20:20). Bản sạch: 180/180 test game, tsc app + server sạch; nhánh chỉ đụng tệp trong làn game. Migration chỉ-thêm `server/migration-2109-game-doan-mua.sql` (doan_ve_so, doan_trum_lop) — Code 3 chạy --remote + đẩy Worker + đưa 2 bảng vào nhóm XOÁ của job reset. ĐƯỢC ĐẨY (vẫn sau cờ game).
- 21/09 00:32 (giờ máy) · Boss · Boss đã soát b6cdaef (Code 4: 2 câu chữ trạng thái trống — HopChonDe "Kho đề đang trống…", ExamMonitorScreen dùng `loiKhongTimThayCa`; không đổi hàm/luồng). ĐƯỢC ĐẨY. ĐÃ GỘP a3aea81 = game bước 4 TIẾP SỨC (thẻ gợi ý do máy chủ soạn, câu được giúp `assisted:true` không ghi bằng chứng, EXP tiếp sức qua `ghiTiepSuc`); bản sạch 161/161 test game, tsc app + server sạch. ĐƯỢC ĐẨY (game vẫn sau cờ `doan_ho_tong`, chỉ 12121212). Duyệt bước 5 bản GỌN cho Trùm lớp (sổ chỉ-thêm `doan_trum_lop`, khung 20:00–20:20 CN, không phòng thời gian thực).
- 21/09 00:30 (giờ máy) · Boss · THẦY CHỐT 2 THIẾT KẾ MỚI + ra lệnh TIẾT KIỆM NGỮ CẢNH. (1) ĐẢO THẦN THÚ bản mới → đề bài `prompt-dao-than-thu-moi.md`, bản vẽ `docs/ban-ve-dao-than-thu-2109/`; người làm: phiên MỚI **Code 6 - Đảo thần thú** (tệp mới trong `src/game/than-thu-v2/dao/`, KHÔNG sửa Game.tsx — Code 5 nối). (2) APP GIÁO VIÊN mặc M3 + màn HÔM NAY → `prompt-app-giao-vien-m3.md`, bản vẽ `docs/ban-ve-app-giao-vien-2109/`; người làm: **Code 4** (6 mốc G1–G6; làn giáo viên ⇒ mọi commit cần Boss soát); Code 3 viết lệnh đọc-chỉ cho màn Hôm nay; Code 1 giữ ruột các màn gọi lên bảng. (3) TIẾT KIỆM: thêm `CLAUDE.md` ở gốc kho (mọi phiên tự nạp: bản đồ nhanh + luật tiết kiệm — không đọc thư mục ảnh/dist, đọc theo khoảng dòng, test theo tệp, báo cáo ≤ 150 từ); Nhật ký DIEU-PHOI cắt còn 12 dòng mới nhất, phần cũ ở `docs/nhat-ky-dieu-phoi-cu.md` (dòng "đã soát" cũ VẪN hiệu lực). Reset vẫn chờ MỌI việc xong (nay gồm cả 2 việc này).
- 21/09 (giờ máy) · Code 4 · Xong: huy hiệu thông báo Android + dòng nhắc icon (4be32ac) · SOÁT app giáo viên dữ liệu trống bằng Worker THẬT + D1 sqlite trống: 10 màn × 6 tình huống (390 sáng/tối, 1280, không danh sách lớp, sau reset `mocReset`, ca chưa lượt, hồ sơ mạnh yếu còn/ca xoá) 0 màn trắng/spinner vô hạn/lỗi đỏ; sửa 2 câu chữ b6cdaef (HopChonDe kho trống, ExamMonitor mã ca sai — LUỒNG THI, CHỜ BOSS SOÁT) · RÀ CUỐI M3 sáng+tối 22 trạng thái: sửa d5e5dab (.btn-google-primary nền tối 2,11:1), ad3917a (nút Xem đề 4,47→≥4,5), huy hiệu thẻ câu (--p-xanh-dam/--p-do-dam). ⚠ CẦN CODE 3 XÁC NHẬN TRÊN D1 THẬT: cột `cau_hoi_em.da_xoa` không có trong migration nào của repo mà goi-cu.ts dùng — thiếu thì màn "Học sinh hỏi" của thầy 500. Đã nhắn Code 2 (form đăng nhập HS 42 px/mắt 16 px — Code 2 nhận sửa; C10 do Code 2, em không cần TheCau commit 2). CHỜ hợp đồng token phụ huynh của Code 3 để làm cổng PH `?ph=`.

- 21/09 00:14 (giờ máy) · Boss · THẦY RA LỆNH: mọi phiên nói chuyện TRỰC TIẾP với nhau. Đã thêm mục "DANH BẠ + LUẬT NÓI CHUYỆN TRỰC TIẾP" (đầu tệp): ai lo gì, khi nào nhắn thẳng nhau, khi nào phải qua Boss, cách ghi lại thoả thuận.

- 21/09 00:11 (giờ máy) · Boss (= 0.Planer) · **THẦY ĐỔI LỆNH RESET (21/09 00:08):** nguyên văn "bạn hãy tiếp tục tới khi xong hết thì tiến hành reset luôn, tôi muốn bạn cho chọn lại thú, xoá hết mọi ca thi và btvn nhưng GIỮ LẠI toàn bộ hồ sơ mạnh yếu của học sinh đã kiểm tra trước đó". HỆ QUẢ: (1) 00:01 21/09 KHÔNG có gì bị xoá (job chưa lên đạn — máy ngủ từ 17:12 thứ Bảy, sao lưu chính thức chưa chạy). (2) PHẠM VI MỚI: GIỮ thêm `su_kien_hoc`, `nam_kt_cau`, `nam_kt_dang`, `tien_do_hs`, `qid_da_lam`; vẫn XOÁ mọi ca thi/lượt/điểm, BTVN, bài Mẹ giao, luyện đề, kế hoạch ngày, trao đổi, game + thần thú (chọn lại thú) + EXP + mảnh khiên, vinh danh, bảng tin PH; GIỮ tài khoản/mật khẩu, lớp, kho đề, cấu hình. (3) THỜI ĐIỂM: bỏ mốc cứng; reset chạy KHI MỌI PHIÊN XONG VIỆC, do Boss ra lệnh (thầy đã uỷ quyền), theo trình tự: export chính thức ra SSD + bookmark Time Travel → dryRun khớp → lên đạn → theo dõi tới `xong` → kiểm bản sống bằng 12121212 → báo thầy. (4) MỌI PHIÊN: làm LIÊN TỤC tới hết hàng việc của mình, xong thì báo Boss một dòng "XONG TRỌN" kèm mã commit cuối; Boss soát, gom đợt đẩy cuối, rồi mới reset. Infographic gửi học sinh (docs/infographic-2109/) đang ghi "00:01 THỨ HAI 21/09" — sẽ đổi ngày khi biết ngày reset thật.

- 19/09 17:08 (giờ máy) · 0.Planer · ĐÃ GỘP c1cfaac = game Đoàn Hộ Tống bước 3 (giao diện 5 màn, nạp lazy ~20 KB gzip) + CỜ MỞ GAME `cau_hinh.doan_ho_tong` (không có dòng = TẮT: game Y HỆT cũ, Võ đài mở mọi ngày, lệnh doan-* bị từ chối không ghi gì) — đã soát phần nối game-v2.ts (2 chỗ) và Game.tsx; bản sạch 155/155 test game + tsc app/server sạch. ĐƯỢC ĐẨY (Pages + Worker, thứ tự nào cũng an toàn). 0.Planer đã soát 2116652 (Code 3: choThiLai trên Worker — kiểm trước khi xoá, ghi bộ đề mới bằng CAS, mọi DELETE đều theo đúng (ma_ca, sbd) + điều kiện không-đang-làm, tạo lượt duoc_duyet_lai mang máy cũ; luat-vao-thi thêm nhánh `sai_may` chép y hệt bản .gs — KHÔI PHỤC luật thầy chốt 08/09; moKhoa gỡ được khoá máy). ĐƯỢC ĐẨY. Export thử D1: 15 giây, 104,6 MB, 62 bảng khớp; bookmark Time Travel ghi ở docs/reset-2109.md.

- 19/09 17:05 (giờ máy, ĐÚNG) · 0.Planer · ĐÍNH CHÍNH GIỜ: các dòng của 0.Planer ghi "17:25 → 20:40" ở dưới là giờ ƯỚC LƯỢNG SAI (thật ra đều trong khoảng 15:30–17:00 ngày 19/09); thứ tự các dòng vẫn đúng. Từ giờ 0.Planer lấy giờ bằng lệnh `date`. — 0.Planer đã soát 11b7c16 (Code 3: job reset bản sửa 5 điểm — cờ LÊN ĐẠN mặc định tắt, hạn tự chạy 01:00 + qua_gio + chạy tay, chia bước ≤ 40 truy vấn, WITHOUT ROWID, /ca/nhieu + /ca/danh-sach). ĐƯỢC ĐẨY (đã lên Worker 2638e329, CHƯA lên đạn; dryRun thật: XOÁ 70.934 dòng / GIỮ 29.358, khớp bảng phân loại). LỊCH: export thử ngay; 22:20 CN 20/09 export chính thức + dryRun → báo thầy → bật cờ; 00:01 thứ Hai chạy; 06:47 thứ Hai 0.Planer kiểm bản sống. Game bước 3 (giao diện 5/6 màn) CHƯA gộp: chờ Code 5 thêm cờ mở `cau_hinh.doan_ho_tong` (tắt = game y hệt cũ).

- 19/09 16:58 (giờ máy) · Code 2 · ĐÃ ĐẨY PAGES ĐỢT 9 từ bản sạch main HEAD e30a303 (0.Planer đã soát c92ca16, eb3cdd1, e65efbb; các commit còn lại trong dải 2b1de0c..e30a303 không đổi tệp luồng thi thật/làn giáo viên — đã liệt kê từng commit theo tệp). Build 0, kiem-sw 10/10, toàn vitest bản sạch 5923 test / 97 đỏ / 42 tệp = nền; so TÊN với bản sạch 2b1de0c (5759/97/42): MỚI ĐỎ [], HẾT ĐỎ []; 22 tên "biến mất" đều do chủ tệp tự đổi và đã kiểm còn xanh: logo-bo-moi (14 → 44 xanh, tệp .ts → .tsx), doan-core (50 → 52, luật Chắn 8 đã duyệt), m3-c8-bang-tin (đổi tên 1 test, 5 → 5); số test bỏ qua giữ 1 = 1. curl /?vai=gv, /hs, /ph = 200; sw-version 1789809694 → 1789811668; js/css sống khớp bản dựng (có `.mom-dong-ho`). Học sinh/phụ huynh thấy: tab "Bài gia đình giao" mặc M3 (danh sách + làm bài, thanh đồng hồ dính 68 px), form Vào phòng thi M3, ô trống/chưa chọn thần thú không còn nói "thần thú đang nghỉ", Bảng tin không quay vô hạn, logo mới; máy tự dọn bộ nhớ khi máy chủ báo `mocReset` (chưa có trường nên chưa dọn gì — job reset của Code 3 CHƯA đẩy). Lùi: `git revert e30a303 f097d09 c5c1348 20950c3` (chỉ phần giao diện của em) rồi đẩy lại từ bản sạch. VIỆC TIẾP của em: (1) hiện câu "Hệ thống đang làm mới, thử lại sau 1 phút" khi máy chủ trả `dangLamMoi:true` (docs/moc-reset-1909.md mục 2 — hiện app vẫn an toàn: giữ phiên, giữ bản nhớ, thử lại mỗi 60 s), (2) chụp lại ảnh trạng thái trống trên bản sống, (3) C10 (ExamTakeScreen) — commit riêng, gửi 0.Planer soát diff trước khi đẩy.

- 19/09 20:40 (giờ máy) · 0.Planer · 0.Planer đã soát c92ca16 (Code 4 logo 4/4: ExamTakeScreen đổi ĐÚNG khối thương hiệu màn vào thi → LogoDoc 64 px; LogoHocSinh thành vỏ mỏng; PhongChoGame không sửa dòng nào) và eb3cdd1 (Code 1: dọn IndexedDB omr-exam máy thầy theo mocReset — một giao dịch nguyên tử, chỉ kích hoạt theo mốc máy chủ, không đồng hồ; đai-dây chặn capNhatKeyBank cho ca máy chủ không còn). CẢ HAI ĐƯỢC ĐẨY. Các commit logo 0ad168f/4b0eb76/f3773f1/ccd79a0/140af65/cde0ec8 không thuộc luồng thi — được đẩy. JOB RESET 6fac0d5 của Code 3: ĐÃ ĐỌC, CHƯA cho đẩy — yêu cầu 5 sửa: (1) cờ LÊN ĐẠN `reset_20260921_cho_phep` (mặc định KHÔNG chạy), (2) chỉ tự chạy trong [00:01, 01:00] VN, quá giờ → `qua_gio`, chạy tay; đóng băng tới khi xong hoặc 01:00, (3) đo + giới hạn số truy vấn D1 mỗi lượt (Free 50/Paid 1000), chia bước nếu cần, (4) bảng WITHOUT ROWID, (5) bọc mocReset cho `/ca/danh-sach`. Export thử 23:30 tối nay, chính thức 22:30 CN.

- 19/09 17:30 (giờ máy) · Code 4 · LOGO MỚI xong ở cây làm việc, 4 nhóm commit theo path: 0ad168f (tài nguyên + scripts/sinh-logo-png.mjs) · 4b0eb76 (manifest/index.html/cai-app/sw/vite) · f3773f1 (component GV/PH/trần + LogoDoc ở 3 màn đăng nhập) · c92ca16 (LogoHocSinh + ExamTakeScreen: LUỒNG THI THẬT, CHỜ 0.Planer soát) · ccd79a0 (phép kiểm không còn -v2 + ảnh; 12 PNG -v2 bị xoá NẰM TRONG c5c1348 của Code 2 vì chung chỉ mục). + Sửa lỗi 140af65 (Bảng tin quay vô hạn khi chưa có bản tin sau reset + vinh danh trống, theo Code 2). Sắp chạy TOÀN `npx vitest run`, mong nền ~102 đỏ/46 tệp (không đỏ mới của em).

- 19/09 19:05 (giờ máy) · 0.Planer · ĐÃ GỘP e65efbb = game Đoàn Hộ Tống BƯỚC 2 (máy chủ `server/src/game-v2-doan.ts`, migration chỉ-thêm `server/migration-1909-game-doan.sql`: doan_chang/doan_luot/doan_tiep_suc; 5 dòng nối trong game-v2.ts đã soát: định tuyến `doan-*`, `resume` bỏ phiên của Đoàn, `answer` từ chối phiên của Đoàn nếu không phải lời gọi nội bộ — nhận diện bằng WeakSet theo đối tượng nên máy em không giả được). DUYỆT luật Chắn mới: Chắn = 8 dù đúng hay sai, đòn SAI tự thành Chắn (bịt chỗ lộ "bạn sai"). Bản sạch: 128/128 test game xanh, tsc server sạch. VIỆC CỦA CODE 3: chạy migration --remote rồi đẩy Worker; đưa 3 bảng doan_* vào job reset 21/09 (có thì xoá dữ liệu, chưa có thì bỏ qua). Duyệt bảng phân loại reset (58 bảng) của Code 3, bảng dọn bộ nhớ HS/PH của Code 2, bảng dọn IndexedDB máy thầy của Code 1 (đã tách làn: Code 2 lo localStorage/sessionStorage, Code 1 chỉ IndexedDB omr-exam ở vai giáo viên; KHÔNG ai dọn phiên đăng nhập).

- 19/09 16:2x (giờ máy) · Code 2 · ĐÃ ĐẨY PAGES ĐỢT 8 từ bản sạch main HEAD 2b1de0c (0.Planer đã soát ee02169 + ea30e98, cho phép phương án a). Build 0, kiem-sw 10/10, toàn vitest bản sạch 5759 test / 97 đỏ / 42 tệp; so TÊN với bản sạch fe18c2d (5519/98/43): MỚI ĐỎ [], HẾT ĐỎ 1 (goi-len-bang-to-chieu-cham "BẤM ĐÚP" — chập chờn tải nặng, đã được Code 1 sửa ea30e98). curl /?vai=gv, /hs, /ph = 200; sw-version 1789808525 → 1789809694. Học sinh thấy: thẻ mời "Chọn thần thú của em" khi máy chủ nói chưa chọn, tab BTVN M3, EXP + mảnh khiên ở Bảng nhiệm vụ và Ôn câu, dòng tiến độ theo datNgay, khung xem phiếu/trang phiếu M3 (Code 4). Lùi: `git revert ba3851a f626d63 6fe9e33 738903c` rồi đẩy lại từ bản sạch.

- 19/09 18:15 (giờ máy) · 0.Planer · THẦY MỞ RỘNG RESET: 00:01 giờ VN thứ Hai 21/09 XOÁ TOÀN BỘ DỮ LIỆU HỌC SINH (ca thi + điểm + bài làm, BTVN, bài Mẹ giao, sổ su_kien_hoc, hồ sơ nam_kt_*, kế hoạch ngày, lên bảng, game + thần thú + EXP, vinh danh, bảng tin) — GIỮ danh sách học sinh + mật khẩu, lớp, kho đề/câu hỏi (kể cả R2), cấu hình của thầy. Code 3 làm job một lần (bảng phân loại XOÁ/GIỮ trình 0.Planer trước, 3 lớp sao lưu: wrangler d1 export ra SSD tối CN + mốc D1 Time Travel + R2 giữ nguyên, cờ huỷ, dryRun, idempotent); thêm `mocReset` để app HS/PH (Code 2) và app giáo viên (Code 1) tự dọn bộ nhớ trong máy. 0.Planer đã soát ee02169 (Code 1, M2 bố cục đo thật không cuộn: thang 5 bậc, gỡ overflow-y:auto của .mc-nua, 0 tràn/0 chữ dưới sàn ở 4 khung; sao 0 kẹp 3 bước chữa) và ea30e98 (sửa test đỏ oan, không hạ ngưỡng). ĐƯỢC ĐẨY. Code 3 đã sửa lỗi on_lai chọn qid thuộc đề đang bảo vệ (Worker 80a846a9, một định nghĩa qidPhucVuDuoc).

- 19/09 17:50 (giờ máy) · 0.Planer · THẦY CHỐT: RESET MỘT LẦN lúc 00:01 giờ VN THỨ HAI 21/09/2026 — "reset hết, cho chọn lại thú" (EXP 0, cấp 1, ví/khiên/mảnh/tháp 0, xoá lựa chọn thú + tên; mọi em). Mốc mở EXP mới cho cả trường DỜI về đúng lúc đó (huỷ kế hoạch bật 00:00 ngày 20/09). Code 3 làm: sao lưu trước + khôi phục một lệnh, chạy một lần idempotent bằng cron, có cờ huỷ, KHÔNG đụng su_kien_hoc / nam_kt_* / ke_hoach_ngay / điểm thi / BTVN / tài khoản; trình 0.Planer soát trước tối Chủ nhật 20/09. Code 2: thẻ mời "Chọn thần thú của em" khi thanThu=null. Code 5: mùa 1 game tính từ 21/09. Thầy CHỐT bộ logo mới (docs/logo-1909/, commit 8df5e94) — Code 4 thay toàn hệ thống. Phiên Rút đề riêng đã xong trọn việc → đã LƯU TRỮ.

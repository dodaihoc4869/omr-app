# BẢNG ĐIỀU PHỐI CÁC PHIÊN — omr-app

Lập 19/09/2026 bởi phiên **0.Planer** (điều phối). Mọi phiên làm việc trong `omr-app` đọc tệp này TRƯỚC khi sửa gì, và cập nhật dòng của mình khi đổi trạng thái.

## Cách nói chuyện với nhau

- Nhắn phiên khác: `ListAgents` để xem tên → `SendMessage({to: "<tên phiên>", message: "..."})`. Dòng đầu tin nhắn phải tự đủ nghĩa (người dùng chỉ thấy dòng đầu).
- Trả lời tin đến: lấy thuộc tính `from` của tin làm `to`.
- Điều phối là **0.Planer**. Xong một giai đoạn, cần tệp của phiên khác, gặp xung đột, hay cần phát hành → nhắn 0.Planer.
- **THẦY ĐÃ UỶ QUYỀN (19/09 12:40): "cần hỏi gì thì hỏi qua 0.Planer, tôi không muốn làm gì cả, chỉ xong việc nhắn tôi".** KHÔNG hỏi thầy, KHÔNG dùng AskUserQuestion, không dừng lại chờ thầy chọn. Gặp chỗ phải quyết: (1) tra DE-XUAT-CA-NHAN-HOA-1909.md / prompt tương ứng — mục "Giả định đã dùng" và cột "Đề nghị" đã được thầy chấp nhận; (2) không có thì nhắn 0.Planer kèm 2 phương án và phương án em nghiêng về, rồi LÀM TIẾP việc khác không phụ thuộc trong lúc chờ; (3) 0.Planer trả lời là quyết định cuối. Ngoại lệ duy nhất phải dừng: thao tác xoá/ghi đè dữ liệu D1 thật, hoặc phát hiện đang có ca thi mở.
- **THẤY LỖI LÀ SỬA LUÔN (thầy ra lệnh 19/09 14:40: "tìm ra lỗi bảo tự động sửa luôn không cần hỏi ý kiến tôi… hãy chủ động mọi thứ để tiến độ và kết quả xịn nhất").** Cách làm: (1) lỗi trong vùng tệp của mình → sửa ngay, thêm test tái hiện lỗi, commit riêng có chữ "Sửa lỗi:" ở đầu, đẩy theo luật 2, ghi sổ việc; (2) lỗi ở vùng phiên khác → nhắn chủ vùng (tệp:dòng, cách tái hiện, đề xuất sửa), chủ vùng sửa luôn không hỏi lại ai; phiên chủ vùng đã dừng → nhắn 0.Planer để gọi dậy; (3) lỗi có sẵn từ trước (không do mình gây ra) mà ảnh hưởng học sinh/phụ huynh thật (mất bài, sai điểm, sai số liệu hiển thị, không mở được bài) → cũng sửa luôn theo (1)/(2); lỗi chỉ là test trôi chữ thì gom lại báo 0.Planer, không sa đà; (4) VẪN PHẢI DỪNG và báo 0.Planer trước khi làm: xoá/ghi đè dữ liệu D1 thật, đổi luật chấm điểm/tính điểm ca thi, đổi luật vào thi/chống gian lận, đổi schema không phải chỉ-thêm.
- Tin nhắn có thể bị giữ chờ thầy duyệt; im lặng KHÔNG phải là đồng ý. Việc gì quan trọng thì ghi thêm vào tệp này.

## Ai làm gì (cập nhật khi đổi)

| Phiên | Việc | Tệp được sửa | Trạng thái |
|---|---|---|---|
| 0.Planer | Điều phối, gộp, **phát hành**, migration `--remote` | `DIEU-PHOI.md`, `SO-VIEC.md` | đang trực |
| Coder | NGOÀI omr-app: dự án `1-DU-AN-KHAC/giai-xuat-html` (đề PDF → HTML tự học) | không sửa gì trong omr-app (chỉ đọc font trong `dist/assets`) | không tham gia điều phối |
| Coder 1 (thầy sẽ xoá phiên) | CÁ NHÂN HOÁ — xong GĐ 0–2 + 2 việc nhỏ (vá SBD lạ, /hs/ca-dang-mo). **Đã dừng; cố vấn cho Code 3** (GĐ 5 chuyển sang Code 3 vì tiết kiệm ngữ cảnh; Coder 1 chưa viết dòng mã nào cho GĐ 5, điểm vào ở mục BÀN GIAO của SO-VIEC.md). Chỉ trả lời câu hỏi CỤ THỂ, ngắn | `SO-VIEC.md` (mục bàn giao) | đã dừng |
| Code 2 | GIAO DIỆN "Bảng nhiệm vụ" HS + PH (thầy xác nhận 12:22): mốc A xong mã + test, đang chốt commit; kế: B phiếu làm bài → C đồng bộ màn học sinh | `src/components/bang-nhiem-vu/**`, `src/lib/nhiem-vu-adapter.ts`, `src/screens/StudentPortalScreen.tsx`, `src/screens/ParentPortalScreen.tsx`, `tests/nhiem-vu-adapter-1909.test.ts`, `tests/bang-nhiem-vu-1909.test.tsx`, `tests/mo-bai-tu-bang-tin.test.tsx` (chỉ đổi selector), `docs/anh-bang-nhiem-vu-1909/`, `SO-VIEC-GIAO-DIEN.md`. Chỉ ĐỌC `tro-ly-ca-nhan.ts` | đang làm |
| Code 3 | CÁ NHÂN HOÁ phần MÁY CHỦ tiếp theo: GĐ 5 (kênh 4 thần thú + kênh 5 phụ huynh) → GĐ 6 phần máy chủ → GĐ 3 (khi Code 2 xong phiếu). Thay Coder 1 vì ngữ cảnh Coder 1 đã 71% (mỗi lượt rất tốn) | `server/**`, `server/migration-*.sql` mới, test tương ứng, `SO-VIEC.md` (nối tiếp Coder 1). Được đẩy Worker như luật 2 của Coder 1 | **đang làm** (19/09 13:35, phiên `Code 3 [f1d054]`): GĐ 5 — Kênh 5 (`parent-news.ts`) trước, rồi Kênh 4 (`game-v2*.ts`); chưa chạm luồng đăng nhập |
| Code 4 | GIAO DIỆN việc C — phần COMPONENT ĐỘC LẬP học sinh/phụ huynh bấm vào: khắc phục, luyện đề, báo cáo ca thi, tiến bộ, thông báo, vào thi/phòng chờ (prompt-giao-dien-dong-bo-man-hoc-sinh.md) | các tệp `src/components/*.tsx` trong bảng kiểm kê đã được 0.Planer duyệt; KHÔNG sửa `StudentPortalScreen.tsx`, `ParentPortalScreen.tsx`, `html-phieu.ts`, `ExamTakeScreen.tsx`, `src/components/bang-nhiem-vu/**` (của Code 2), `server/**`. Không phát hành | **đang làm** (19/09 14:03, phiên Code 4): kiểm kê được 0.Planer duyệt; thứ tự A1 (PhongVaoThi + ThongBaoHocSinh) → B → C → A2 → D; sổ việc `SO-VIEC-GIAO-DIEN-C.md`; thư viện dùng chung ở `src/components/m3/` |
| Code 1 (phiên MỚI, không phải Coder 1 cũ) | LÀN GIÁO VIÊN — GĐ 6 phần gọi lên bảng: `diemHopCau` đọc hồ sơ nắm kiến thức, nút Đạt/Không đạt trên bảng Engine E ghi ngược, seed cho phân công dạy học | `src/lib/ho-so-lop.ts`, `src/lib/xep-buoi-chua.ts`, `src/lib/phan-cong-day-hoc.ts`, `src/screens/GoiLenBangScreen.tsx`, test tương ứng, `docs/hop-dong-ho-so-len-bang-1909.md`, `SO-VIEC-GIAO-VIEN.md`. Cần lệnh máy chủ → viết hợp đồng rồi nhắn Code 3. Không phát hành | **đang làm** (19/09, nhận việc từ 0.Planer): (a) `diemHopCau` đọc hồ sơ → (b) nút Đạt/Không đạt Engine E → (c) seed phân công dạy học; hợp đồng cho Code 3 viết song song |

Phân vùng dự kiến theo hai bản thiết kế:
- **Cá nhân hoá** (`DE-XUAT-CA-NHAN-HOA-1909.md`, `prompt-ca-nhan-hoa-tien-bo-tung-ngay.md`): `server/**`, `server/migration-*.sql`, `src/lib/**` liên quan hồ sơ/kế hoạch/lô, test tương ứng. GĐ 0 đã commit `0fcc397`.
- **Giao diện** (`prompt-giao-dien-nhiem-vu-hoc-sinh-phu-huynh.md`, bản vẽ https://claude.ai/artifact/RFUcbWFDRPQ9PPY7EPAYnK): `src/components/bang-nhiem-vu/**` (mới), `src/lib/nhiem-vu-adapter.ts` (mới), `StudentPortalScreen.tsx`, `ParentPortalScreen.tsx`, test tương ứng. KHÔNG sửa `tro-ly-ca-nhan.ts`, `html-phieu.ts`, `btvn-cho-em.ts`, `server/**`.

## Luật chung (các phiên đang chung MỘT thư mục, MỘT nhánh `main`)

1. **Chỉ commit tệp của mình, ghi rõ đường dẫn**: `git add <tệp> <tệp>`. CẤM `git add -A`, `git add .`, `git commit -a` — sẽ cuốn theo tệp làm dở của phiên khác. Chỉ mục git (index) là DÙNG CHUNG giữa các phiên: tệp phiên khác đã `git add` sẽ bị `git commit` trần cuốn theo → LUÔN commit theo đường dẫn: `git commit -m "…" -- <tệp> <tệp>`.
2. **Phát hành: MỖI PHIÊN TỰ ĐẨY PHẦN CỦA MÌNH** (thầy cho phép 19/09 14:05: "tôi sẽ không mở mới ca nào từ bây giờ tới lúc tất cả hoàn thành, cứ deploy thoải mái").
   - **Code 3 (kế nhiệm Coder 1) → chỉ MÁY CHỦ**: `DAY-MAY-CHU.command` + `wrangler d1 execute --remote` cho migration CHỈ-THÊM của mình.
   - **APP (Pages)**: `./DAY-LEN-PAGES.command </dev/null`. Người đẩy: **Code 2** (thầy nhắn thẳng vào phiên đó 19/09 16:30: "tự đẩy lên máy chủ luôn sau khi làm xong") hoặc 0.Planer. Vì BA phiên cùng sửa `src/**` (Code 2, Code 4, Code 1) mà Pages đóng gói TOÀN BỘ `src/` trên đĩa, mỗi lần đẩy phải qua CỔNG sau:
     1. **ĐẨY TỪ BẢN SẠCH, KHÔNG ĐẨY TỪ THƯ MỤC CHUNG** (0.Planer đặt 19/09 17:50 sau khi bản sửa lỗi thầy báo bị kẹt vì tệp dở của phiên khác). Chỉ mã ĐÃ COMMIT mới lên máy học sinh; tệp dở của ai cũng không ảnh hưởng, không phải chờ ai:
        `WT=<thư mục tạm của phiên>/day-sach` → lần đầu: `git worktree add --detach "$WT" <commit>` + `ln -s "/Volumes/SSD NGOÀI/omr-app/node_modules" "$WT/node_modules"`; các lần sau: `git -C "$WT" checkout --detach <commit>`.
        Trong `$WT`: `unset GITHUB_PAGES; npm run build` → `rm -f dist/404.html` → `node scripts/kiem-sw.mjs` (phải 10/10) → `npx vitest run` toàn bộ TẠI ĐÂY (bản sạch = đúng thứ sắp đẩy; nền 98 đỏ/43 tệp) → `npx -y wrangler@latest pages deploy dist --project-name=omr-app --branch=main --commit-dirty=true`.
     2. `git log --oneline <MỐC ĐÃ ĐẨY>..<commit sắp đẩy>` — có commit của LÀN GIÁO VIÊN (Code 1) hoặc commit đổi tệp thuộc LUỒNG THI THẬT (`ExamTakeScreen.tsx`, `exam-setup.css`, `MaCaInput.tsx`, `PhongChoGame.tsx`, `TamTruotHoiBai.tsx`, `BaoCaoCaThiHocSinhModal.tsx`, `TheCau.tsx`) mà Nhật ký chưa có dòng "0.Planer đã soát <mã commit>" → KHÔNG đẩy; nhắn 0.Planer xin soát.
     3. Đẩy xong: `curl` 3 đường trả 200 + `sw-version.json` đổi; SỬA dòng "MỐC ĐÃ ĐẨY PAGES" dưới đây; ghi Nhật ký; báo 0.Planer một dòng. KHÔNG dùng `DAY-LEN-PAGES.command`/`DAY-TAT-CA.command` nữa (chúng dựng từ thư mục chung).
     **MỐC ĐÃ ĐẨY PAGES: c929544 (đợt 5 của Code 2 — main HEAD từ bản sạch, gồm việc B phiếu M3 144b7b2+da96d48+c929544, Code 4 nhóm A2/D/C/B/A1, làn giáo viên đã soát; 19/09 15:2x giờ máy; Pages f9db8cb3, sw-version 1789805781)**
   - Điều kiện trước MỖI lần đẩy (không bỏ bước nào): (a) tệp của mình đã commit, `git status` không còn sửa dở trong vùng mình; (b) toàn bộ `npx vitest run` không quá 98 đỏ / 43 tệp (báo trước ở Nhật ký); (c) Pages: `npm run build` + `node scripts/kiem-sw.mjs` đạt 10/10; (d) ghi một dòng Nhật ký: giờ, phiên, đẩy gì, commit nào.
   - Sau khi đẩy: kiểm `curl` 3 đường (`/?vai=gv`, `/hs`, `/ph`) trả 200 và `sw-version.json` đổi; hỏng thì báo 0.Planer NGAY kèm log, không tự đẩy đè lần nữa.
   - **Ngoại lệ duy nhất vẫn qua 0.Planer**: commit đổi `ExamTakeScreen.tsx` / `exam-setup.css` (màn thi thật) — nhắn 0.Planer soát diff trước rồi mới đẩy.
   - Không ai chạy lệnh xoá/ghi đè dữ liệu D1 thật.
3. **Không sửa tệp ngoài vùng của mình.** Cần đổi → nhắn chủ vùng, nói rõ tệp, dòng, lý do.
4. **Sổ việc**: `SO-VIEC.md` thuộc **Coder 1** (đang ghi ở đó theo lệnh thầy) — Coder 1 được commit cả mục "19/09 (chiều)" của 0.Planer đang nằm sẵn trong tệp. Giao diện ghi `SO-VIEC-GIAO-DIEN.md`. Phiên khác không sửa `SO-VIEC.md`.
   **Vùng cấm để dở của phiên Giao diện**: trong `src/lib/**` chỉ được tạo/sửa đúng MỘT tệp `nhiem-vu-adapter.ts`; tuyệt đối không mở sửa `lich-lo-btvn.ts`, `tro-ly-ca-nhan.ts` (Worker đóng gói chúng).
5. **Test**: chạy tệp test của mình thoải mái; chạy toàn bộ `npx vitest run` thì báo trước trong tệp này (mục Nhật ký) để phiên khác không hiểu nhầm test đỏ do tệp làm dở của nhau. Nền: 98 test đỏ / 43 tệp.
6. **Không `git stash`, `git reset --hard`, `git checkout .`, `git clean`** — phá việc của phiên khác.
7. Trước khi dừng phiên: commit tệp của mình, cập nhật bảng trên, nhắn 0.Planer một dòng tóm tắt.

## Nhật ký (mới nhất ở trên, mỗi dòng: giờ VN · phiên · việc)

- 19/09 16:05 (giờ máy) · 0.Planer · THẦY CHỐT game "ĐOÀN HỘ TỐNG" (Hộ tống Linh Tâm bản mới) + yêu cầu thêm màn TUNG CHƯỞNG. Đặc tả: DE-XUAT-HO-TONG-LINH-TAM-1909.md (mục 7 = bản vẽ đã chốt); 6 ảnh: docs/ban-ve-doan-ho-tong-1909/; đề bài build: prompt-game-doan-ho-tong.md. NGƯỜI LÀM: phiên mới **Code 5 - Game** — làn tệp: `src/game/than-thu-v2/`, `server/src/game-v2-doan*.ts`, `tests/doan-*.test.*`, `SO-VIEC-GAME.md`, `docs/anh-doan-ho-tong-1909/`. Code 5 KHÔNG đẩy Worker/Pages: Code 3 vẫn là phiên DUY NHẤT đẩy Worker + chạy migration (sau khi 0.Planer soát); Pages đẩy từ bản sạch như cũ. Khoản "EXP tiếp sức" +3 (≤5 lần/ngày) do Code 3 thêm vào bảng khoản EXP.

- 19/09 15:33 · Code 4 · C8 XONG (93198bf: sheet Bảng tin BangTinPhuHuynh; toàn vitest 97 đỏ / 42 tệp, đỏ MỚI: không). C9 (ParentPortalScreen phần cũ ngoài Bảng nhiệm vụ — màn đăng nhập SBD + khung sheet toàn màn): sắp chạy TOÀN BỘ `npx vitest run`, mong ≤ 98 đỏ / 43 tệp. Sửa dở: src/screens/ParentPortalScreen.tsx (+7/−2 dòng: gốc m3 bọc hai màn, nút đăng nhập/đóng), src/components/m3/m3-tuong-thich.css (sinh), scripts/sinh-m3-tuong-thich.mjs, tests/m3-c9-phu-huynh-1909, tests/m3-c-1909. KHÔNG đụng adapter/thứ tự Bảng nhiệm vụ; CLS đo trước/sau ở tin nhắn báo.
- 19/09 15:2x (giờ máy) · Code 2 · ĐÃ ĐẨY PAGES ĐỢT 5 từ bản sạch main HEAD c929544 (đã có "0.Planer đã soát" cho b72ca8d/da96d48/564dc08/5d0d1c4 và mọi commit luồng thi thật/làn giáo viên trong dải b977a31..c929544). Build exit 0, kiem-sw 10/10, toàn vitest bản sạch 5322 test / 97 đỏ / 42 tệp = nền, so từng tên test với bản sạch b977a31 (5012 test / 97 đỏ / 42 tệp): MỚI ĐỎ [], HẾT ĐỎ []. curl /?vai=gv, /hs, /ph = 200; sw-version 1789802602 → 1789805781. Nội dung học sinh thấy: phiếu làm bài/khắc phục/đề+lời giải/Mẹ giao/Câu hỏi của em mặc Material 3 (thanh trên + tiến độ + lưới số câu + hộp xác nhận nộp), ô mã ca + phòng chờ + tấm Hỏi bài (Code 4). Lùi: `git revert c929544 da96d48 144b7b2` rồi đẩy lại từ bản sạch.

- 19/09 15:35 (giờ máy) · 0.Planer · 0.Planer đã soát 765c37b (Code 1, M1: thời gian lên bảng theo độ khó × độ dài × em; hàm thuần một nguồn; câu thiếu văn bản vẫn 300/180/120; ĐỒNG Ý HE_LAM_SONG_SONG = 0,5 vì tờ chiếu chạy hai em song song và giữ được sàn 20 em; chốt thêm: câu 0 sao kẹp tối đa 3 bước chữa). ĐƯỢC ĐẨY. LƯU Ý CHỈ MỤC GIT DÙNG CHUNG: 765c37b cuốn theo 2 tệp đã `git add` sẵn của phiên khác (DE-XUAT-EXP-MANH-KHIEN-1909.md, tests/exp-hoc-tap-1909.test.ts — nội dung hợp lệ, không phải lùi). TỪ GIỜ mọi phiên commit bằng `git commit -m "…" -- <tệp của mình>` (commit theo đường dẫn), không `git commit` trần.

- 19/09 15:20 (giờ máy) · 0.Planer · 0.Planer đã soát b72ca8d, da96d48, 564dc08, 5d0d1c4. b72ca8d (Code 4: danhGiaBai bỏ 2 câu suy năng lực từ điểm, chỉ đổi 2 chuỗi + chú thích). da96d48 (Code 2: hộp xác nhận nộp M3 — đã kiểm html-phieu.ts chỉ có ĐÚNG MỘT window.confirm trong luồng nộp nên mượn-confirm không nuốt lời hỏi thứ hai nào; mã nộp cũ không đổi; dựng hộp lỗi → confirm gốc). 564dc08 (Code 4: MaCaInput/PhongChoGame qua biến --ma-* dự phòng = giá trị cũ; laManThi() chỉ đúng khi docDuongVao có maCa; lớp bọc .m3 không đặt nền/chiều cao nên không đổi bố cục). 5d0d1c4 (Code 4: TamTruotHoiBai + 1 dòng DesignSystem `var(--nut-chu, var(--giay))` — ngoài .m3 y hệt). CẢ BỐN ĐƯỢC ĐẨY. A2/tấm hỏi bài lên trước vỏ màn thi là chấp nhận được (thầy không mở ca nào tới khi xong). Code 4 nhận thêm C8 (BangTinPhuHuynh) + C9 (ParentPortalScreen phần cũ) từ bảng kiểm kê của Code 2 để chạy song song. Thứ tự việc C của Code 2: C1 → C11 (LamCauOn) → C6 → C7 → C2 → C3 → C5 → C4 → C10.

- 19/09 15:08 · Code 4 · XONG A2 (564dc08: MaCaInput + PhongChoGame) + TamTruotHoiBai (5d0d1c4, kèm 1 dòng DesignSystem.tsx) + Sửa lỗi b72ca8d (lib/danh-gia-bai.ts). Cả ba thuộc/liên quan LUỒNG THI THẬT → CHỜ 0.Planer SOÁT; A2 và TamTruotHoiBai chỉ bật ở màn thi qua laManThi() (?examCode=, /t/, /d/) nên CHỜ vỏ màn thi của Code 2 mới nên đẩy. Toàn vitest 97 đỏ / 42 tệp (nền 98/43), đỏ MỚI của Code 4: không. src/ của Code 4 sạch (còn thấy tệp dở của phiên khác: len-bang-cau-hinh.ts, thoi-gian-len-bang.ts).
- 19/09 15:08 (giờ máy) · Code 3 · ĐÃ ĐẨY WORKER `c130d7cc` (DAY-MAY-CHU, "SACH 8 ca"): lệnh của thầy `hoSoOnCa` (commit dbafd80) + sửa test chập chờn (ca834c5). Toàn vitest 5283 test, 98 đỏ / 43 tệp: ngoài nền CHỈ có 1 test của chính Code 3 chập chờn do so `serverNow` (đã sửa, chạy 5 lần ổn định) → còn 97 / 42 = nền, MỚI ĐỎ thật []. Bản sống: 1 em ok (thầy: tuCa '', lam 198), 21 em → 'Xin quá nhiều em một lượt', không secret → 'Sai mã bí mật'. Lùi: `git revert dbafd80` + DAY-MAY-CHU. Kế: EXP học tập + mảnh khiên (DE-XUAT-EXP-MANH-KHIEN-1909.md).

- 19/09 15:06 · Code 4 · Sửa lỗi b72ca8d (lib/danh-gia-bai.ts: hết suy năng lực/mức độ từ điểm; nhắn 0.Planer soát vì báo cáo HS thuộc luồng thi thật). NHÓM A2 (MaCaInput, PhongChoGame) + TamTruotHoiBai: sắp chạy TOÀN BỘ `npx vitest run`, mong ≤ 98 đỏ / 43 tệp (loại tệp làm dở của phiên khác, vd tests/phieu-m3-1909.test.ts của Code 2). Sửa dở: MaCaInput.tsx, PhongChoGame.tsx, TamTruotHoiBai.tsx, DesignSystem.tsx (1 dòng: màu chữ nút chính qua biến dự phòng), src/components/m3/**, scripts/sinh-m3-tuong-thich.mjs, tests/m3-a2-1909, tests/m3-d2-tam-truot-1909. Cả ba tệp component thuộc LUỒNG THI THẬT, chỉ bật ở màn thi (laManThi) → sẽ commit RIÊNG từng nhóm, chờ soát + chờ vỏ màn thi của Code 2 mới đẩy.
- 19/09 ~15:08 (giờ máy) · Code 3 · ĐỢT ĐẨY WORKER 9: lệnh của THẦY `hoSoOnCa` (hồ sơ ôn cho rút đề riêng, theo docs/hop-dong-ho-so-on-ca-1909.md; commit sau b13d346; `server/src/{ho-so-on-ca,index}.ts`, `tests/ho-so-on-ca-1909.test.ts`). Chỉ ĐỌC, không migration. Sắp chạy TOÀN BỘ `npx vitest run` rồi DAY-MAY-CHU.

- 19/09 22:30 · 0.Planer · 0.Planer đã soát 2ff68a5 (Code 1: cầu nối tờ máy chiếu ↔ màn giáo viên: 4 lớp kiểm tin [đúng iframe nguồn, gốc, mã phiên ngẫu nhiên, khoá có trên tờ], một đường ghi ghiTheoKhoa chống ghi đôi, không có cauNoi thì tờ chiếu byte-for-byte như cũ). ĐƯỢC ĐẨY. Máy chủ: Worker b94ed2bd đã trả namKt cho hoSoLopLenBang + ghiLenBangMoi ngừng cộng tien_do_hs; Kênh 4 kiểm bản sống đạt.
- 19/09 15:00 (giờ máy) · Code 3 · ĐÃ ĐẨY WORKER `b94ed2bd` (DAY-MAY-CHU, "SACH 8 ca"): GĐ 6 máy chủ làn giáo viên (commit 2b95b40): `hoSoLopLenBang` trả `namKt` + `ghiLenBangMoi` ngừng cộng `tien_do_hs`. Toàn vitest 5202 test / 99 đỏ / 46 tệp; ngoài nền 4 tệp giao diện đỏ chỉ khi chạy đầy đủ (chạy riêng xanh 52/52, không tham chiếu server/, tệp src của phiên khác đang dở) → MỚI ĐỎ của Code 3 []. Bản sống kiểm bằng tài khoản thầy: namKt đúng, không dsQid → không namKt. Đổi 2 test cũ theo quyết định thầy (đã ghi ở mục trước). Lùi: `git revert 2b95b40` + DAY-MAY-CHU. Kế: hoSoOnCa.

- 19/09 ~15:00 (giờ máy) · Code 3 · ĐỢT ĐẨY WORKER 8: GĐ 6 máy chủ làn giáo viên — `hoSoLopLenBang` trả `namKt` + `ghiLenBangMoi` ngừng cộng `tien_do_hs` (theo docs/hop-dong-ho-so-len-bang-1909.md của Code 1). Tệp: `server/src/{ho-so-len-bang,goi-cu,index}.ts`, `tests/ho-so-len-bang-may-chu-1909.test.ts`; ĐỔI 2 TEST CŨ theo quyết định thầy câu 11: `tests/khoi-a-va-kho-de-1209.test.ts:92` (toContain→not.toContain 'INSERT INTO tien_do_hs') và `tests/su-kien-hoc-1909.test.ts:267-273` (tien_do_hs cộng {3,1} → không còn dòng). Không migration. Sắp chạy TOÀN BỘ vitest rồi DAY-MAY-CHU.

- 19/09 21:40 · 0.Planer · 0.Planer đã soát e1555a4 (Code 4: TheCau.tsx chỉ +1 dòng import CSS; the-cau.css mọi bộ chọn có tiền tố .m3 nên KHÔNG có hiệu lực ở màn thi/game/app giáo viên cho tới khi có tổ tiên .m3; ảnh thật đã xem). ĐƯỢC ĐẨY. 591d6e9 (báo cáo PH bỏ kết luận năng lực từ điểm) không thuộc luồng thi thật — được đẩy. Thầy CHỐT bản vẽ màn chiếu lên bảng → giao Code 1 (prompt-man-chieu-len-bang.md, 6 mốc).
- 19/09 14:56 · Code 4 · XONG TheCau commit 1: e1555a4 (CSS `.m3 .the-cau` + 1 dòng nạp CSS; TheCau thuộc LUỒNG THI THẬT → CHỜ 0.Planer SOÁT trước khi Code 2 đẩy; CHƯA bật ở màn thi). Toàn vitest 100 đỏ / 43 tệp: 3 test đỏ mới thuộc tests/phieu-m3-1909.test.ts của Code 2 (html-phieu.ts dở); loại tệp đó → 97/42, đỏ MỚI của Code 4: không. Trước đó: 591d6e9 (Sửa lỗi: lời khuyên báo cáo PH). src/ của Code 4 sạch. Kế: A2 (MaCaInput, PhongChoGame), rồi TamTruotHoiBai + KhoiBaiLuyen + TheCauChiTiet.
- 19/09 14:53 (giờ máy) · Code 3 · ĐÃ ĐẨY WORKER `8d3e6e5c` (DAY-MAY-CHU, "SACH 8 ca"): KÊNH 4 thần thú (commit 5d3b45d) + lời từ chối đúng việc của `/hs/on-lai/nop` (03823b8). Toàn vitest 5139 test, 97 đỏ / 42 tệp = nền, MỚI ĐỎ [] (không cần loại tệp nào). `tests/than-thu-v2.test.ts` 46/46 nguyên, không sửa dòng nào. Bản sống: kế hoạch 12121212 ra bình thường, on-lai/nop từ chối bằng lời mới. Lùi: `git revert 5d3b45d 03823b8` + DAY-MAY-CHU. Kế: hợp đồng Code 1 (hoSoLopLenBang trả namKt + ghiLenBangMoi ngừng cộng tien_do_hs), rồi hoSoOnCa.

- 19/09 14:53 · Code 4 · SỬA LỖI 591d6e9 (lời khuyên báo cáo PH kết luận năng lực từ điểm: chỉ nói số liệu; bản HS đã sạch nên không sửa) + NHÓM D-TheCau commit 1 (CSS `.m3 .the-cau` + 1 dòng nạp CSS trong TheCau.tsx, chỉ có hiệu lực dưới tổ tiên `.m3`; commit 2 bật ở màn thi CHỜ Code 2): sắp chạy TOÀN BỘ `npx vitest run`, mong ≤ 98 đỏ / 43 tệp. Sửa dở: src/components/TheCau.tsx (1 dòng), src/components/m3/the-cau.css, tests/m3-d-the-cau-1909.test.tsx. TheCau thuộc luồng thi thật → commit RIÊNG, xin 0.Planer soát.
- 19/09 ~14:53 (giờ máy) · Code 3 · ĐỢT ĐẨY WORKER 7: KÊNH 4 thần thú (commit sau 03823b8; `server/src/{game-v2-ho-so,game-v2-bank,game-v2,ho-so-cau-hinh,ke-hoach-ngay-d1}.ts`, `tests/kenh4-than-thu-1909.test.ts`) + lời từ chối đúng việc của `/hs/on-lai/nop` (03823b8). Không migration. Sắp chạy TOÀN BỘ `npx vitest run` rồi DAY-MAY-CHU. GIỮ nguyên mọi bất biến `tests/than-thu-v2.test.ts` (46/46 xanh).

- 19/09 20:50 · 0.Planer · 0.Planer đã soát 70d3408 (Code 4: BaoCaoCaThiHocSinhModal mặc M3 chỉ khi dungM3(); bỏ chữ "Vòng N"; logic/prop/payload không đổi). ĐƯỢC ĐẨY. 5f41d50 (báo cáo PH + biểu đồ) không thuộc luồng thi thật — được đẩy.
- 19/09 14:45 (giờ máy) · Code 3 · ĐÃ ĐẨY WORKER `fbadbc33` (DAY-MAY-CHU, "SACH 8 ca"): bản vá luật "nộp trống" `/hs/on-lai/nop` (commit 85a7859): chưa trả lời → không ghi sổ/không khoá/không đáp án, trả `chuaLam[]`. Toàn vitest 5107 test, 97 đỏ / 42 tệp = nền, MỚI ĐỎ []. Lùi: `git revert 85a7859` + DAY-MAY-CHU. Kế: Kênh 4.

- 19/09 14:44 · Code 4 · XONG NHÓM C, HAI commit: 5f41d50 (PH modal + biểu đồ + bộ sinh + m3.css; KHÔNG đụng luồng thi thật → đẩy được) và 70d3408 (RIÊNG BaoCaoCaThiHocSinhModal.tsx + test — LUỒNG THI THẬT, CHỜ 0.Planer SOÁT trước khi Code 2 đẩy Pages). Toàn vitest 97 đỏ / 42 tệp (nền 98/43), test đỏ MỚI: không. src/ của Code 4 sạch (các tệp sửa dở còn lại là của phiên khác: html-may-chieu.ts, html-phieu.ts, to-chieu-cau-noi.ts). Kế: nhóm D-TheCau (TheCau + TheCauChiTiet).
- 19/09 ~14:46 (giờ máy) · Code 3 · ĐỢT ĐẨY WORKER 6: bản vá luật "nộp trống" của `/hs/on-lai/nop` (0.Planer chốt): câu chưa trả lời → không ghi sổ/không khoá/không đáp án, trả `chuaLam[]` (commit sau 087ff8e; `server/src/on-lai-nop.ts`, `tests/on-lai-nop-1909.test.ts`, `docs/ke-hoach-ngay-api-1909.md`). Không migration. Sắp chạy TOÀN BỘ `npx vitest run` rồi DAY-MAY-CHU.

- 19/09 14:41 · Code 4 · NHÓM C (BaoCaoCaThiPhuHuynhModal, BaoCaoCaThiHocSinhModal [luồng thi thật + dùng chung GV], BieuDoTienBoGoogle, TheTienBo): sắp chạy TOÀN BỘ `npx vitest run`, mong ≤ 98 đỏ / 43 tệp. Sửa dở trong src/: BaoCaoCaThiHocSinhModal.tsx, BaoCaoCaThiPhuHuynhModal.tsx, BieuDoTienBoGoogle.tsx, src/components/m3/**, scripts/sinh-m3-tuong-thich.mjs, tests/m3-*. Sẽ commit làm HAI lần: (1) mọi thứ trừ BaoCaoCaThiHocSinhModal, (2) RIÊNG BaoCaoCaThiHocSinhModal.tsx + test → nhắn 0.Planer soát trước khi Code 2 đẩy Pages.
- 19/09 14:39 (giờ máy) · Code 3 · ĐÃ ĐẨY WORKER `8f679011` (DAY-MAY-CHU, "SACH 8 ca"): `POST /hs/on-lai/nop` (commit 087ff8e). Toàn vitest 99 đỏ / 44 tệp; ngoài nền chỉ tệp giao diện đang dở (`m3-b-1909`; `escort-context-menu` đỏ lúc chạy đầy đủ, chạy riêng xanh); Worker không import tệp src dở → MỚI ĐỎ của Code 3 []. Bản sống: không token/token sai/token PH giả → ok:false, D1 thật 0 dòng nguon='on_lai'. Lùi: `git revert 087ff8e` + DAY-MAY-CHU. Kế: Kênh 4.

- 19/09 ~14:38 (giờ máy) · Code 3 · ĐỢT ĐẨY WORKER 5: `POST /hs/on-lai/nop` (commit 087ff8e; `server/src/{on-lai-nop,cau-theo-qid,su-kien-hoc,index}.ts`, `tests/on-lai-nop-1909.test.ts`, `docs/ke-hoach-ngay-api-1909.md`). Không migration (cột `nguon` không có CHECK). Sắp chạy TOÀN BỘ `npx vitest run` rồi DAY-MAY-CHU; loại khi so: tệp dở của phiên giao diện/giáo viên.

- 19/09 20:05 · 0.Planer · 0.Planer đã soát 965c07f và 0a5afbd (Code 1: chữ "3 vòng" → "Chia lô theo hạn nộp"; phanCongDayHoc tất định theo seed; exam-shuffle.ts CHỈ thêm chữ export trước mulberry32 — đã đọc diff, không đổi ký tự nào khác). ĐƯỢC ĐẨY. Cũng đã xem 4a37ff2 (Code 4 nhóm B, không thuộc luồng thi thật) — được đẩy.
- 19/09 14:40 · Code 1 · KẾT QUẢ TOÀN vitest sau 0a5afbd: 5066 test, 100 đỏ, 44 tệp đỏ. MỚI ĐỎ 2 tệp `bon-viec-0809-dot4` + `nam-loi-0809-dot3` (cùng lỗi thiếu `<body class="co-lam chua-nop">` — do `src/lib/html-phieu.ts` đang sửa dở, không phải làn giáo viên); HẾT ĐỎ `parent-news.test.ts`. Loại 2 tệp html-phieu ⇒ 98 / 42 tệp. Làn giáo viên nghỉ chờ Code 3.
- 19/09 14:32 · Code 1 · xong 3 mốc làn giáo viên (a) 704c512 · (b) 7a9888c · (c) 0a5afbd + "Sửa lỗi" chữ 3 vòng 965c07f; sắp chạy TOÀN `npx vitest run` MỘT lần, so nền 98 đỏ / 43 tệp (loại tệp test của phiên khác đang sửa dở nếu đỏ tạm). Tệp làm việc của Code 1 đã commit hết; không phát hành.

- 19/09 14:29 · Code 4 · XONG NHÓM B, commit 4a37ff2 (LuyenDeChuan, KhoiKhacPhuc3CheDo, ModalKhacPhucCauSai + m3/m3-tuong-thich.css sinh tự động): toàn vitest 97 đỏ / 42 tệp (nền 98/43), test đỏ MỚI: không; build riêng OK. KHÔNG đụng luồng thi thật (Code 2 đẩy Pages được); src/ của Code 4 sạch. Kế: nhóm C (báo cáo + tiến bộ).
- 19/09 14:28 (giờ máy) · Code 3 · ĐÃ ĐẨY WORKER `68dd6092` (DAY-MAY-CHU, "SACH 8 ca"): việc nhỏ (2) `POST /hs/cau-theo-qid` (commit 217311a). Toàn vitest 101 đỏ / 43 tệp; ngoài nền chỉ `tests/m3-b-1909.test.tsx` (Code 4, giao diện, đang dở) → loại còn 97 / 42, MỚI ĐỎ của Code 3 []. Bản sống (tài khoản thầy, chỉ đọc): câu đã gặp trả về đủ, câu có trong kho mà chưa gặp → khongCo, không có đáp án/lời giải trong phản hồi; SBD bịa/21 qid bị từ chối. Lùi: `git revert 217311a` + DAY-MAY-CHU. Kế: Kênh 4 (thần thú).

- 19/09 14:26 (giờ máy) · Code 2 · ĐÃ ĐẨY PAGES ĐỢT 4 (https://27c0f6ba.omr-app-b3u.pages.dev) = main HEAD b977a31, bản sạch. build exit 0, kiem-sw 10/10, toàn vitest 97 đỏ / 42 tệp (≤ nền 98/43, 0 đỏ mới; 6 tệp giao diện của em xanh: 51+26+49+7+3+10). curl `/?vai=gv`, `/hs`, `/ph` = 200; sw-version sống 1789802602 = bản dựng (sau ~20 s do bộ nhớ đệm biên); cùng chunk vào index-BMyUSna8.js. Lùi: `git revert b977a31` rồi đẩy lại từ bản sạch.
- 19/09 14:26 · Code 4 · NHÓM B (LuyenDeChuan, KhoiKhacPhuc3CheDo, ModalKhacPhucCauSai, KhoiCauSai): sắp chạy TOÀN BỘ `npx vitest run`, mong ≤ 98 đỏ / 43 tệp (loại tệp làm dở của phiên khác khi so). Đang sửa dở trong src/: src/components/{LuyenDeChuan,KhoiKhacPhuc3CheDo,ModalKhacPhucCauSai}.tsx, src/components/m3/** (thêm m3-tuong-thich.css sinh tự động, luyen-khac-phuc.css), scripts/sinh-m3-tuong-thich.mjs, tests/m3-b-1909.test.tsx. KHÔNG đụng luồng thi thật. Sẽ commit MỘT lần khi xong.
- 19/09 ~14:27 (giờ máy) · Code 3 · ĐỢT ĐẨY WORKER 4: việc nhỏ (2) `POST /hs/cau-theo-qid` (commit 217311a; tệp `server/src/cau-theo-qid.ts`, route trong `index.ts`, `tests/cau-theo-qid-1909.test.ts`, `docs/ke-hoach-ngay-api-1909.md`). Không migration. Sắp chạy TOÀN BỘ `npx vitest run` rồi DAY-MAY-CHU; loại khi so: tệp dở của Code 1/Code 2 trong thư mục chung.

- 19/09 19:20 · 0.Planer · 0.Planer đã soát 7a9888c (Code 1, làn giáo viên mốc b: nút Đạt/Không đạt trên bảng buổi chữa Engine E, một đường ghi chung ghiKetQua với Engine C, chống ghi đôi, ghi hỏng giữ nút; sửa lý do trượt sàn khi bị chặn bậc). ĐƯỢC ĐẨY.
- 19/09 14:23 (giờ máy) · Code 2 · SẮP ĐẨY PAGES đợt 4 = main HEAD b977a31 ("Bài Mẹ giao cũ chưa làm" tonCu thu gọn + bỏ tự bù thẻ + thu gọn nhóm dài). Cổng 2: từ ece0608 chỉ có b977a31 (em) và 55590d3 (sổ Code 3). Sẽ chạy toàn bộ vitest TẠI bản sạch (mong ≤ 98 đỏ / 43 tệp).
- 19/09 18:55 · 0.Planer · ĐÃ ĐẨY PAGES ĐỢT 3 từ bản sạch của main ece0608 (gồm cả 46140c6 và 704c512 — cả hai đã soát). Toàn vitest trên bản sạch: 5003 test, 97 đỏ/42 tệp, MỚI ĐỎ [] , HẾT ĐỎ [parent-news]. QUY ƯỚC: khi mọi commit trong khoảng đều đã soát thì đẩy THẲNG main HEAD (không cherry-pick) để bản sống luôn = một commit có thật trên main.
- 19/09 14:20 (giờ máy) · Code 3 · ĐÃ ĐẨY WORKER `c2b85d06` (DAY-MAY-CHU, "SACH 8 ca"): tồn cũ `tonCu[]` cho Mom chưa bắt đầu (commit ece0608, quyết định 0.Planer). Toàn vitest 99 đỏ / 44 tệp; ngoài nền là 2 tệp dở của Code 1 (`lich-su-len-bang-1009`, `ho-so-lop-nam-kt-1909`) → loại còn 97 / 42, MỚI ĐỎ của Code 3 []. Bản sống: 11036 → 0 mom trong viec[], tonCu 2 bài/229 câu; 11037 → 3 mom, tonCu 21 bài/105 câu; 10006 → 3 mom, tonCu 6 bài/24 câu; không sửa dòng nào trên D1. Lùi: `git revert ece0608` + DAY-MAY-CHU. Kế: (2) /hs/cau-theo-qid → Kênh 4.

- 19/09 14:17 (giờ máy) · Code 2 · ĐÃ ĐẨY PAGES ĐỢT 2 (https://6b8dc434.omr-app-b3u.pages.dev): bản sạch = 2ff13ca + b7d55a8 + 1394872. Bản sạch: build exit 0, kiem-sw 10/10, toàn vitest 97 đỏ / 42 tệp (≤ nền 98/43, 0 đỏ mới; 6 tệp test giao diện của em xanh: 46+25+46+7+3+10). curl `/?vai=gv`, `/hs`, `/ph` = 200; sw-version sống 1789802078 = bản dựng (đổi so với 1789801648); bundle sống có `parent-news`. Lùi: `git revert 1394872 b7d55a8` rồi đẩy lại từ bản sạch.
- 19/09 ~14:20 (giờ máy) · Code 3 · ĐỢT ĐẨY WORKER 3 (KHẨN, bản sống đang khoá 5 em thật): Mom chưa bắt đầu chỉ vào viec[] khi giao ≤ 3 ngày VN và ≤ 3 bài; phần còn lại → `tonCu[]`/`tonCuTong` (không tính tải/quá tải/cổng) theo QUYẾT ĐỊNH 0.Planer; commit sau eaca548/3448f02. Tệp: `server/src/{ho-so-cau-hinh,ke-hoach-ngay,ke-hoach-ngay-d1}.ts`, `tests/ke-hoach-mom-chua-bd-1909.test.ts`, `docs/ke-hoach-ngay-api-1909.md`. Không migration, không đụng D1. Sắp chạy TOÀN BỘ `npx vitest run` rồi DAY-MAY-CHU.

- 19/09 14:14 (giờ máy) · Code 2 · SẮP ĐẨY PAGES đợt 2 từ BẢN SẠCH = mốc đã đẩy 2ff13ca + 2 commit của em (b7d55a8 Kênh 5 phụ huynh "Giao bài cho con" đi /parent-news; 1394872 sửa chữ "3 Vòng"/"nắm chắc" ở tab BTVN học sinh). KHÔNG kéo theo 46140c6 (Code 4, PhongVaoThi chưa soát) hay commit server. Sẽ chạy toàn bộ vitest TẠI bản sạch (mong 98 đỏ / 43 tệp).
- 19/09 14:12 (giờ máy) · Code 3 · ĐÃ ĐẨY WORKER `a230c979` (DAY-MAY-CHU, "SACH 8 ca"): việc nhỏ (1) bài Mom chưa bắt đầu vào viec[] (eaca548) + chống trùng câu ôn lại/bài Mom (3448f02). Migration CHỈ-THÊM `migration-1909-mom-qid.sql` đã chạy `--remote` TRƯỚC khi đẩy (thêm cột `mom_bai.qid_json`; 216 dòng cũ nguyên). Toàn vitest: 99 đỏ / 43 tệp, trong đó `bang-nhiem-vu-1909.test.tsx` của Code 2 (2 test); bỏ tệp đó → 97 / 42, MỚI ĐỎ []. Bản sống: kế hoạch 12121212 có `mom:daily_2026-09-19`. Lùi: `git revert 3448f02 eaca548` + DAY-MAY-CHU. CẢNH BÁO cho giao diện: 5 em có tổng 42 bài Mom tay chưa bắt đầu (11037 = 24 bài) → viec[] dài + nhãn qua_tai (chi tiết ở SO-VIEC.md). Kế: (2) /hs/cau-theo-qid → Kênh 4.

- 19/09 18:25 · 0.Planer · 0.Planer đã soát 704c512 (Code 1, làn giáo viên mốc a: diemHopCau đọc hồ sơ nắm kiến thức; tương thích ngược khi máy chủ chưa trả namKt; chặn cứng bậc "biết" × câu 2 sao; câu cần dạy lại → bắt buộc; sửa lỗi khoá hồ sơ lớp theo số em). ĐƯỢC ĐẨY. Cũng đã xem 46140c6 (Code 4, nhóm A1) — không thuộc luồng thi thật, được đẩy.
- 19/09 ~14:12 (giờ máy) · Code 3 · ĐỢT ĐẨY WORKER 2: (1) bài Mom chưa bắt đầu vào viec[] (commit eaca548; sửa test cũ `tests/ke-hoach-ngay-1909.test.ts` dòng 114-119 theo hợp đồng mới) + chống trùng "một câu hai nơi" (commit 3448f02, CỘT CHỈ-THÊM `mom_bai.qid_json`, migration `server/migration-1909-mom-qid.sql` sẽ chạy `--remote` TRƯỚC khi đẩy Worker). Sắp chạy TOÀN BỘ `npx vitest run`; tệp đụng: `server/src/{mom,ke-hoach-ngay,ke-hoach-ngay-d1}.ts` + 4 tệp test ke-hoach/mom. Mong 98/43 trừ tệp giao diện (bang-nhiem-vu-1909) và 2 tệp test mới của "Code 1" (`tests/ho-so-lop-*-1909.test.ts`, chưa commit, không phải của mình).

- 19/09 14:09 · Code 4 · XONG NHÓM A1, commit 46140c6 (PhongVaoThi + ThongBaoHocSinh + src/components/m3/): toàn vitest 98 đỏ / 43 tệp = nền (đổi chỗ 1: ho-so-lop-nam-kt-1909 của Code 1 đỏ, parent-news của Code 3 xanh; không tệp nào của Code 4 đỏ). Không đụng luồng thi thật; src/ và tests/ của Code 4 sạch — Code 2 đẩy Pages được. Kế: nhóm B (LuyenDeChuan, KhoiKhacPhuc3CheDo → ModalKhacPhucCauSai, KhoiCauSai).
- 19/09 17:50 · 0.Planer · ĐÃ ĐẨY PAGES ĐỢT 1c từ BẢN SẠCH của commit 2ff13ca (117 test giao diện xanh ở bản sạch, kiem-sw 10/10, 3 đường 200). Đổi luật 2: mọi lần đẩy Pages đều dựng từ worktree sạch của mã đã commit — không còn phải chờ phiên khác commit tệp dở. Code 4/Code 1 cứ làm tiếp, không cần commit vội.
- 19/09 14:05 · Code 4 · NHÓM A1 (PhongVaoThi + ThongBaoHocSinh, thuần học sinh, KHÔNG nằm trong luồng thi thật): sắp chạy TOÀN BỘ `npx vitest run`, mong ≤ 98 đỏ / 43 tệp (loại tệp làm dở của phiên khác khi so). Chỉ đụng src/components/PhongVaoThi.tsx, src/components/ThongBaoHocSinh.tsx, src/components/m3/** (mới), tests/m3-a1-1909.test.tsx (mới), SO-VIEC-GIAO-DIEN-C.md, docs/anh-dong-bo-m3-1909/. Chưa commit, chưa phát hành.
- 19/09 14:03 · Code 2 · SỬA LỖI thần thú (trang chủ HS/PH sai con + bỏ nền tròn/lơ lửng): sắp chạy TOÀN BỘ `npx vitest run`, mong 98 đỏ / 43 tệp. Chỉ đụng src/lib/nhiem-vu-adapter.ts, src/components/bang-nhiem-vu/**, 2 màn cổng, 3 tệp test của em, docs/anh-bang-nhiem-vu-1909/, SO-VIEC-GIAO-DIEN.md.
- 19/09 14:00 (giờ máy) · Code 3 · ĐÃ ĐẨY WORKER `0caf811c` (DAY-MAY-CHU, chốt ca thi "SACH 8 ca"): Kênh 5 (fd784a8) + `thanThu` (9f4382c). Toàn vitest lần cuối: 104 đỏ / 43 tệp, trong đó 7 test / 1 tệp là `tests/bang-nhiem-vu-1909.test.tsx` của Code 2 (đang sửa dở); bỏ tệp đó → 97 / 42 tệp, MỚI ĐỎ [] (parent-news.test.ts nay xanh). Bản sống: /hs/ke-hoach-ngay 12001 có `thanThu {lua_phuong,1,"Chó Đỏ"}`; SBD bịa → ok:false; /parent-news/list + assign trên tài khoản của THẦY (12121212) → 8 câu, gọi lần hai alreadySent. Lùi: `git revert 9f4382c fd784a8` + DAY-MAY-CHU. Kế: việc nhỏ (1) mom chưa bắt đầu vào viec[] → (2) /hs/cau-theo-qid → Kênh 4.

- 19/09 ~14:00 (giờ máy) · Code 3 · ĐỢT ĐẨY WORKER: Kênh 5 (commit fd784a8) + lỗi thầy báo "thần thú luôn Hoả Long" (`thanThu` trong /hs/ke-hoach-ngay, commit 9f4382c). Lần chạy toàn vitest trước (chỉ Kênh 5): 4856 test, 97 đỏ / 42 tệp = nền 98/43 trừ `tests/parent-news.test.ts` (nay xanh), MỚI ĐỎ []. Sắp chạy lại TOÀN BỘ trên trạng thái cuối rồi `DAY-MAY-CHU.command`. `git status src/`: chỉ tệp giao diện của Code 2/4; không tệp nào Worker đóng gói.

- 19/09 17:00 · 0.Planer · duyệt kiểm kê việc C của Code 4 (20 tệp: làm 15, bỏ KhoiTienBo [chỉ GV] + 4 tệp mã chết); cô lập giao diện mới bằng dungM3() — app giáo viên giữ bộ mặt cũ; tệp thuộc luồng thi thật phải qua 0.Planer soát. Lỗi thầy báo (sai thần thú, bỏ nền thần thú) đã giao Code 3 (trường thanThu trong /hs/ke-hoach-ngay) + Code 2 (nối + lơ lửng), ưu tiên trước việc B.
- 19/09 ~13:55 (giờ máy) · Code 3 · KÊNH 5 (parent-news): sắp chạy TOÀN BỘ `npx vitest run`, mong đúng nền 98 đỏ / 43 tệp (tệp mình đụng: `server/src/{parent-news,parent-news-chon-cau,parent-news-nguon-cau,index}.ts` + 3 tệp test parent-news*; `tests/parent-news.test.ts` có sẵn trong 43 tệp nền — trước đó chỉ 1 test đỏ là dòng 71-95 mà mình xoá theo lệnh). Bỏ 2 tệp test giao diện khi so. Sau đó commit riêng từng tệp của mình, ĐẨY WORKER bằng DAY-MAY-CHU.command. KHÔNG đụng src/.

- 19/09 16:35 · 0.Planer · thầy cho Code 2 TỰ ĐẨY Pages. Thêm CỔNG 3 bước trước mỗi lần đẩy Pages (tệp dở của phiên khác; commit làn giáo viên/màn thi chưa soát; mốc đã đẩy). Code 4 và Code 1: CHỈ commit khi một nhóm/mốc đã hoàn chỉnh và test xanh — commit dở dang sẽ bị đẩy lên máy học sinh.
- 19/09 16:20 · 0.Planer · thầy mở thêm Code 1 (mới) và Code 4. Phân làn: Code 2 = cổng HS/PH + phiếu + màn thi; Code 4 = component độc lập của việc C; Code 1 = làn giáo viên (gọi lên bảng GĐ 6); Code 3 = máy chủ (phiên máy chủ DUY NHẤT). Pages do 0.Planer đẩy: trước khi đẩy tôi hỏi cả ba phiên src/ đã ở điểm commit sạch.
- 19/09 16:05 · 0.Planer · ĐÃ ĐẨY PAGES ĐỢT 1b (A.1 bản nhớ kế hoạch ngày, commit ad849af; src/ sạch lúc đẩy; Code 2 báo toàn vitest 98/43). Coder 1 sẽ bị thầy xoá — Code 3 không còn cố vấn, dùng mục BÀN GIAO trong SO-VIEC.md.
- 19/09 15:42 · 0.Planer · ĐÃ ĐẨY XONG PAGES ĐỢT 1 (https://5a6effde.omr-app-b3u.pages.dev, 3 đường 200). src/** mở lại cho Code 2.
- 19/09 15:35 · 0.Planer · ĐẨY PAGES ĐỢT 1 (mốc A của Code 2, commit 783c7d8; src/ sạch lúc đẩy; Code 2 báo toàn vitest 98/43 = nền). Code 2 và Code 3: ĐỪNG sửa src/** trong ~5 phút tới khi có dòng "đã đẩy xong".
- 19/09 15:05 · Coder 1 · ĐÃ DỪNG GĐ 5 theo lệnh 0.Planer (chuyển Code 3). KHÔNG có tệp mã dở: chỉ commit 1de6325 (dòng mở GĐ 5 trong sổ) + commit bàn giao; đã ghi vào SO-VIEC.md mục BÀN GIAO các điểm vào K4/K5 (readScope, mốc ôn, game_v2_task, bẫy cron song song, lỗi gốc layCauTuKhoDe). Dòng của mình: đã dừng, cố vấn cho Code 3.
- 19/09 15:10 · 0.Planer · thầy mở phiên Code 3. Tính tiết kiệm: Coder 1 ngữ cảnh 705k token (mỗi lượt cõng ~12 lần phiên mới) → CHUYỂN toàn bộ việc máy chủ còn lại (GĐ 5 → GĐ 6 máy chủ → GĐ 3) sang Code 3; Coder 1 dừng, chỉ trả lời câu hỏi cụ thể của Code 3. Một phiên máy chủ duy nhất ⇒ không có chuyện đẩy Worker cuốn theo tệp dở của phiên máy chủ khác.
- 19/09 13:30 · Code 2 · MỐC A (Bảng nhiệm vụ + nối /hs/ke-hoach-ngay + /hs/ca-dang-mo): sắp chạy TOÀN BỘ `npx vitest run`, mong đúng nền 98 đỏ / 43 tệp (hai tệp test của em đã đồng bộ với adapter mới: 38 + 30 + 14 + 7 xanh; lần đỏ 14:25 của Coder 1 là do chạy giữa lúc em sửa adapter). `git status`: chỉ tệp trong vùng của em + tệp của phiên khác. Rồi commit; KHÔNG tự chạy DAY-LEN-PAGES (quyền đẩy bản live không thể cấp qua tin nhắn giữa các phiên) — em nhắn 0.Planer để đẩy.
- 19/09 13:41 · Code 2 · MỐC A.1 (bản nhớ kế hoạch ngày, stale-while-revalidate): sắp chạy TOÀN BỘ `npx vitest run`, mong đúng nền 98 đỏ / 43 tệp. Chỉ đụng src/lib/nhiem-vu-adapter.ts, src/components/bang-nhiem-vu/may-chu.ts, 2 màn cổng và 2 tệp test của em. Chưa đẩy Pages (0.Planer đẩy).
- 19/09 14:40 · 0.Planer · THẦY CHỐT bản vẽ màn thi thật (ghi vào prompt-giao-dien-dong-bo-man-hoc-sinh.md) và ra lệnh "thấy lỗi là sửa luôn, không hỏi thầy" (ghi vào mục Cách nói chuyện). Coder 1 đã xong GĐ 0–2 + vá SBD lạ + /hs/ca-dang-mo; giao tiếp GĐ 5 (kênh thần thú + phụ huynh, chỉ server).
- 19/09 14:40 · Coder 1 · /hs/ca-dang-mo ĐÃ SỐNG (Worker 040d3c25, commit f815fec). Gọi thử bản sống: em thật lớp 11, 12 → coCaMo:false (8 ca cờ mo đều quá hạn vào), SBD bịa/token sai → ok:false, JSON không có ma_ca/tenCa. Chưa kiểm được nhánh coCaMo:true trên bản sống (không có ca mở) — có ở test. Dòng của mình: xong GĐ 0–2 + 2 việc nhỏ, ĐÃ DỪNG.
- 19/09 14:25 · Coder 1 · /hs/ca-dang-mo (lệnh 0.Planer giao): TOÀN BỘ vitest = 106 đỏ / 45 tệp, MỚI ĐỎ chỉ 2 tệp của Code 2 (tests/bang-nhiem-vu-1909.test.tsx, tests/nhiem-vu-adapter-1909.test.ts — vỡ ở nhiem-vu-adapter.ts đang sửa dở: 'Cannot read properties of undefined (reading daLamCau)', không import gì từ server/, và xanh ở lần chạy trước). BỎ hai tệp đó: đúng 98 đỏ / 43 tệp, danh sách y hệt nền → phần của Coder 1 không thêm đỏ. `git status src/lib`: nhiem-vu-adapter.ts (Code 2, Worker không import); lich-lo-btvn.ts, tro-ly-ca-nhan.ts không bị sửa → ĐẨY WORKER bằng DAY-MAY-CHU.command ngay sau dòng này. Code 2: nhớ cập nhật hai test đó khớp adapter mới.
- 19/09 14:05 · 0.Planer · THẦY CHO PHÉP các phiên tự phát hành (thầy không mở ca mới tới khi xong hết). Sửa luật 2: Coder 1 đẩy Worker, Code 2 đẩy Pages bằng DAY-LEN-PAGES.command, kèm 4 điều kiện trước khi đẩy; màn thi thật vẫn qua 0.Planer soát. Thêm việc C (prompt-giao-dien-dong-bo-man-hoc-sinh.md) cho Code 2.
- 19/09 14:00 · Coder 1 · ĐÃ VÁ và ĐÃ LÊN Worker b1cba8d3: /hs/ke-hoach-ngay từ chối SBD không có thật, không ghi gì (bản sống kiểm: 3 SBD bịa → ok:false, ke_hoach_ngay 261→261). Không xoá dòng 00000000 (đúng lệnh). Cập nhật dòng của mình: xong GĐ 0–2, ĐÃ DỪNG. Commit cuối d73fe93 + sổ việc.
- 19/09 13:45 · 0.Planer · THẦY CHỐT bản vẽ phiếu làm bài → giao Code 2 việc B (prompt-giao-dien-phieu-lam-bai.md) SAU khi việc A (Bảng nhiệm vụ + nối /hs/ke-hoach-ngay) đã commit; mở vùng src/lib/html-phieu.ts cho Code 2. Quyết định: giữ viewport user-scalable=no; không đổi định dạng sprite (thiếu công cụ) — chỉ lazy-load thần thú ở trang chủ; không tách chunk toàn app; nghiệm thu Lighthouse = không tệ hơn bản cũ; bỏ nút "Nhắn Thầy" đợt này. Giao Coder 1 thêm /hs/ca-dang-mo (không trả mã ca) cùng đợt bản vá SBD lạ.
- 19/09 13:35 · 0.Planer · nghiệm thu GĐ 2 của Coder 1 (commit d378c25/c01148b/dd1f972 chỉ đụng server/, tests/, SO-VIEC.md; /hs/ke-hoach-ngay sống). Ghi docs/ke-hoach-ngay-api-1909.md cho Code 2. Code 2 đã commit nhánh trợ lý 5e91632.
- 19/09 13:55 · Coder 1 · vá lỗi 0.Planer báo (SBD bịa vẫn sinh dòng ke_hoach_ngay): TOÀN BỘ vitest = 98 đỏ / 43 tệp đúng nền; `git status src/lib` sạch → ĐẨY WORKER bằng DAY-MAY-CHU.command ngay sau dòng này. Chỉ sửa server/src/ke-hoach-ngay-d1.ts + tests/ke-hoach-ngay-1909.test.ts.
- 19/09 13:35 · Coder 1 · GĐ 2 bản vá nhỏ (chữ ghiChu/danh sách qid của việc ôn khớp số câu thật): TOÀN BỘ vitest = 98 đỏ / 43 tệp đúng nền; `git status src/lib` chỉ nhiem-vu-adapter.ts (Code 2) → ĐẨY WORKER bằng DAY-MAY-CHU.command ngay sau dòng này. Không đụng tệp nào của giao diện.
- 19/09 13:10 · Coder 1 · GĐ 2 (kế hoạch ngày): đã chạy TOÀN BỘ vitest = 98 đỏ / 43 tệp đúng nền, không đỏ mới. `git status src/lib`: chỉ nhiem-vu-adapter.ts (Code 2, Worker không import); lich-lo-btvn.ts và tro-ly-ca-nhan.ts KHÔNG bị sửa. Migration migration-1909-ke-hoach.sql (chỉ thêm 1 bảng) đã chạy. ĐẨY WORKER bằng DAY-MAY-CHU.command ngay sau dòng này.
- 19/09 12:36 · Code 2 · sắp chạy TOÀN BỘ `npx vitest run` để so nền 98 đỏ / 43 tệp (tệp làm dở của em: 2 màn cổng + bang-nhiem-vu/ + nhiem-vu-adapter.ts; 3 test tệp mình đã xanh: 14 + 21 + 7). Sau đó `npm run build` ra thư mục riêng (không đè `dist/` đang có) + kiem-sw. Chưa commit, chưa phát hành.

- 19/09 13:20 · 0.Planer · đã soát GĐ 1 của Coder 1: commit 7f7f3d1 chỉ chứa 5 tệp server/test của chính nó, e437d8d chỉ SO-VIEC.md; tệp dở của Code 2 (2 màn cổng, bang-nhiem-vu/, nhiem-vu-adapter.ts, 2 test) không bị cuốn vào. Luật 1 được giữ.
- 19/09 12:45 · Coder 1 · GĐ 1 (hồ sơ nam_kt_cau/dang): đã chạy TOÀN BỘ vitest = 98 đỏ / 43 tệp đúng nền, không đỏ mới. `git status src/lib`: chỉ có nhiem-vu-adapter.ts (Code 2, Worker không import) → ĐẨY WORKER bằng DAY-MAY-CHU.command ngay sau dòng này. Migration migration-1909-ho-so.sql (chỉ thêm 2 bảng) đã chạy.
- 19/09 12:50 · Coder 1 · chạy toàn bộ `npx vitest run` (tệp test chỉ của server/sổ; nền 98/43) rồi ĐẨY WORKER bằng DAY-MAY-CHU.command: bản vá GĐ 0 (khắc phục không có phiếu R2 → chấm sổ bằng tờ kho). `git status` src/: sạch, không sửa dở của ai. Đã kiểm 12:03 và sẽ kiểm lại ca thi mở trong chính DAY-MAY-CHU.
- 19/09 12:32 · 0.Planer · sửa luật 2 và 4: Coder 1 được đẩy Worker bằng DAY-MAY-CHU.command + migration chỉ-thêm của mình (lệnh trực tiếp của thầy); Pages chỉ 0.Planer; SO-VIEC.md thuộc Coder 1; Giao diện chỉ được đụng src/lib/nhiem-vu-adapter.ts.
- 19/09 12:30 · Coder 1 · báo: GĐ 0 đã commit 0fcc397 + đã phát hành (trước khi có bảng này); không đụng 5 tệp giao diện; GĐ 1–2 chỉ server.
- 19/09 12:13 · Code 2 · báo rảnh, thầy chưa giao việc; đã đọc bảng và 7 luật, chưa sửa tệp nào ngoài dòng của mình ở đây.
- 19/09 12:20 · Coder · báo: làm dự án khác, không đụng omr-app, commit 0fcc397 không phải của Coder, chưa phát hành gì.
- 19/09 12:15 · 0.Planer · lập bảng, hỏi trạng thái 3 phiên.

# ĐỀ BÀI BUILD — APP PHỤ HUYNH MỚI: một màn + một nút + bảng "Mọi thứ về con" (thầy CHỐT MẪU 21/09/2026 14:37: "chốt mọi thứ về con")

Người viết: Boss. Mẫu được chốt: `docs/ban-ve-ph-moi-thu-ve-con-2109/` (`ph-0-man-chinh.html`, `ph-1-moi-thu-ve-con.html`, `ph-moi-thu.css`, 11 ảnh). Lệnh thầy: bảng siêu đẹp ghi TOÀN BỘ lịch sử trong ngày của con, điểm mạnh yếu, mọi thứ về các câu con đã làm, KHÔNG gì về thần thú; app phụ huynh chỉ còn MỘT màn + MỘT nút "Giao thêm bài cho con".

## MÁY CHỦ (đã có, đã soát): `POST /ph/tat-ca-ve-con`, `POST /ph/chi-tiet-cau-ve-con` (server/src/ph-tat-ca-ve-con.ts, e51a14c; nhận token hoặc SBD trần; LUẬT CHE ở máy chủ). Code 3 nối đường trong W2b. `no` (Dồn về đích) tới ở W3.

## CODE 2 — màn (build ĐÚNG mẫu)
1. **Màn chính** (thay màn vừa tối giản ở P2): lời chào + tên con/lớp; thẻ lớn "Hôm nay của con" (đạt nhiệm vụ chưa + 4 số có nhãn; cả thẻ là MỘT đích chạm ⇒ mở bảng); thẻ "Ca kiểm tra gần nhất của con" (bấm ⇒ mở bảng ở khối Ca kiểm tra); dải thông báo của thầy (thụ động) nếu có; MỘT nút "Giao thêm bài cho con" dính đáy + dòng lượt còn lại; cuối màn dòng nhỏ "Đổi số báo danh" + số bản app. Không menu, không tab.
2. **Bảng "Mọi thứ về con"**: một trang cuộn, mục lục chip dính đầu trang (390) / cột trái (≥ 1024), 10 khối đúng thứ tự mẫu: Tổng quan hôm nay → Dòng thời gian trong ngày (chạm mốc ⇒ nhảy tới nhóm câu của mốc) → Ca kiểm tra gần nhất → Điểm mạnh · cần luyện → Từng câu con đã làm (lọc Tất cả · Sai · Làm lâu; mở một câu ⇒ gọi `/ph/chi-tiet-cau-ve-con` lấy lời giải) → Bài tập về nhà → Lịch ôn lại → 14 ngày gần đây → Lời A.I Đỗ Đại Học gửi anh/chị → nút giao bài dính đáy.
3. **Nói thật theo dữ liệu**: khối nào máy chủ không trả ⇒ ẨN (không bịa); câu bị `che` ⇒ "Con đã làm · kết quả hiện sau khi con nộp bài" / "…sau khi Thầy công bố điểm", KHÔNG đáp án; câu bài tập về nhà không có `conChon` ⇒ chỉ "Đáp án C" + Đúng/Sai; nguồn game hiện là "Luyện dạng con còn vấp". Trạng thái đang tải (khung xương), chưa có dữ liệu hôm nay ("Hôm nay con chưa học. Anh/chị có thể giao thêm bài cho con."), lỗi mạng (thử lại) — mẫu chưa vẽ, bạn thêm theo bộ thành phần sẵn có.
4. **Hiệu năng**: bảng là gói tải lười riêng (không vào precache gói vào); danh sách câu > 40 dòng ⇒ hiện dần theo nhóm mốc (mở nhóm nào dựng nhóm đó); không thư viện biểu đồ (SVG tự vẽ như mẫu).
5. **Cấm**: thần thú, EXP, khiên, game, xếp hạng điểm, so con với bạn (riêng dòng "độ chăm hôm nay: hạng x trong y bạn" được phép, không tên bạn), nhãn năng lực, "nắm chắc", mã nội bộ, emoji. Chủ ngữ tự động "A.I Đỗ Đại Học". Tiêu đề khối dạng: "Dạng con làm tốt" / "Dạng con còn vấp" (đúng mẫu thầy chốt). Đọc `docs/CHUAN-TU-NGU-VA-GIAO-DIEN.md` trước khi đặt chữ mới.
6. **Test chốt chặn**: cây màn phụ huynh không có chuỗi game; đúng MỘT nút hành động chính; câu bị che không bao giờ hiện đáp án dù JSON lỡ có; ảnh 390 sáng·tối + 1440, 0 tràn, đích ≥ 48, tương phản ≥ 4,5:1.
Thứ tự: sau P2 (gỡ menu) và thẻ "Đường về đích"; mỗi cụm một commit, xin Boss soát; đẩy ở lượt Pages kế.
